# Generated by SnakeBridge v0.17.0 - DO NOT EDIT MANUALLY
# Regenerate with: mix compile
# Library: dspy 3.4.0
# Python module: dspy

defmodule Dspy do
  @moduledoc """
  SnakeBridge bindings for `dspy`.

  ## Version

  - Requested: 3.4.0
  - Observed at generation: 3.4.0

  ## Runtime Options

  All functions accept a `__runtime__` option for controlling execution behavior:

      Dspy.some_function(args, __runtime__: [timeout: 120_000])

  ### Supported runtime options

  - `:timeout` - Call timeout in milliseconds (default: 120,000ms / 2 minutes)
  - `:timeout_profile` - Use a named profile (`:default`, `:ml_inference`, `:batch_job`, `:streaming`)
  - `:stream_timeout` - Timeout for streaming operations (default: 1,800,000ms / 30 minutes)
  - `:session_id` - Override the session ID for this call
  - `:pool_name` - Target a specific Snakepit pool (multi-pool setups)
  - `:affinity` - Override session affinity (`:hint`, `:strict_queue`, `:strict_fail_fast`)

  ### Timeout Profiles

  - `:default` - 2 minute timeout for regular calls
  - `:ml_inference` - 10 minute timeout for ML/LLM workloads
  - `:batch_job` - Unlimited timeout for long-running jobs
  - `:streaming` - 2 minute timeout, 30 minute stream_timeout

  ### Example with timeout override

      # For a long-running ML inference call
      Dspy.predict(data, __runtime__: [timeout_profile: :ml_inference])

      # Or explicit timeout
      Dspy.predict(data, __runtime__: [timeout: 600_000])

      # Route to a pool and enforce strict affinity
      Dspy.predict(data, __runtime__: [pool_name: :strict_pool, affinity: :strict_queue])

  See `SnakeBridge.Defaults` for global timeout configuration.

  """

  @doc false
  def __snakebridge_python_name__, do: "dspy"
  @doc false
  def __snakebridge_library__, do: "dspy"

  @doc """
  Wraps a DSPy program so that it can be called asynchronously. This is useful for running a

  program in parallel with another task (e.g., another DSPy program).

  This implementation propagates the current thread's configuration context to the worker thread.

  ## Parameters

  - `program` - The DSPy program to be wrapped for asynchronous execution.

  ## Returns

  - `term()`
  """
  @spec asyncify(term(), keyword()) :: {:ok, term()} | {:error, Snakepit.Error.t()}
  def asyncify(program, opts \\ []) do
    SnakeBridge.Runtime.call(__MODULE__, :asyncify, [program], opts)
  end

  @doc """
  Python binding for `dspy.bootstrap_trace_data`.

  ## Parameters

  - `program` (term())
  - `dataset` (list(term()))
  - `metric` (term() | nil default: None)
  - `num_threads` (integer() | nil default: None)
  - `raise_on_error` (term() default: True)
  - `capture_failed_parses` (term() default: False)
  - `failure_score` (float() default: 0)
  - `format_failure_score` (float() default: -1)
  - `log_format_failures` (boolean() default: False)
  - `callback_metadata` (%{optional(String.t()) => term()} | nil default: None)
  - `capture_crashes` (boolean() default: False)

  ## Returns

  - `list(term())`
  """
  @spec bootstrap_trace_data(term(), list(term())) ::
          {:ok, list(term())} | {:error, Snakepit.Error.t()}
  @spec bootstrap_trace_data(term(), list(term()), keyword()) ::
          {:ok, list(term())} | {:error, Snakepit.Error.t()}
  @spec bootstrap_trace_data(term(), list(term()), term() | nil) ::
          {:ok, list(term())} | {:error, Snakepit.Error.t()}
  @spec bootstrap_trace_data(term(), list(term()), term() | nil, keyword()) ::
          {:ok, list(term())} | {:error, Snakepit.Error.t()}
  @spec bootstrap_trace_data(term(), list(term()), term() | nil, integer() | nil) ::
          {:ok, list(term())} | {:error, Snakepit.Error.t()}
  @spec bootstrap_trace_data(term(), list(term()), term() | nil, integer() | nil, keyword()) ::
          {:ok, list(term())} | {:error, Snakepit.Error.t()}
  @spec bootstrap_trace_data(term(), list(term()), term() | nil, integer() | nil, term()) ::
          {:ok, list(term())} | {:error, Snakepit.Error.t()}
  @spec bootstrap_trace_data(
          term(),
          list(term()),
          term() | nil,
          integer() | nil,
          term(),
          keyword()
        ) :: {:ok, list(term())} | {:error, Snakepit.Error.t()}
  @spec bootstrap_trace_data(term(), list(term()), term() | nil, integer() | nil, term(), term()) ::
          {:ok, list(term())} | {:error, Snakepit.Error.t()}
  @spec bootstrap_trace_data(
          term(),
          list(term()),
          term() | nil,
          integer() | nil,
          term(),
          term(),
          keyword()
        ) :: {:ok, list(term())} | {:error, Snakepit.Error.t()}
  @spec bootstrap_trace_data(
          term(),
          list(term()),
          term() | nil,
          integer() | nil,
          term(),
          term(),
          float()
        ) :: {:ok, list(term())} | {:error, Snakepit.Error.t()}
  @spec bootstrap_trace_data(
          term(),
          list(term()),
          term() | nil,
          integer() | nil,
          term(),
          term(),
          float(),
          keyword()
        ) :: {:ok, list(term())} | {:error, Snakepit.Error.t()}
  @spec bootstrap_trace_data(
          term(),
          list(term()),
          term() | nil,
          integer() | nil,
          term(),
          term(),
          float(),
          float()
        ) :: {:ok, list(term())} | {:error, Snakepit.Error.t()}
  @spec bootstrap_trace_data(
          term(),
          list(term()),
          term() | nil,
          integer() | nil,
          term(),
          term(),
          float(),
          float(),
          keyword()
        ) :: {:ok, list(term())} | {:error, Snakepit.Error.t()}
  @spec bootstrap_trace_data(
          term(),
          list(term()),
          term() | nil,
          integer() | nil,
          term(),
          term(),
          float(),
          float(),
          boolean()
        ) :: {:ok, list(term())} | {:error, Snakepit.Error.t()}
  @spec bootstrap_trace_data(
          term(),
          list(term()),
          term() | nil,
          integer() | nil,
          term(),
          term(),
          float(),
          float(),
          boolean(),
          keyword()
        ) :: {:ok, list(term())} | {:error, Snakepit.Error.t()}
  @spec bootstrap_trace_data(
          term(),
          list(term()),
          term() | nil,
          integer() | nil,
          term(),
          term(),
          float(),
          float(),
          boolean(),
          %{optional(String.t()) => term()} | nil
        ) :: {:ok, list(term())} | {:error, Snakepit.Error.t()}
  @spec bootstrap_trace_data(
          term(),
          list(term()),
          term() | nil,
          integer() | nil,
          term(),
          term(),
          float(),
          float(),
          boolean(),
          %{optional(String.t()) => term()} | nil,
          keyword()
        ) :: {:ok, list(term())} | {:error, Snakepit.Error.t()}
  @spec bootstrap_trace_data(
          term(),
          list(term()),
          term() | nil,
          integer() | nil,
          term(),
          term(),
          float(),
          float(),
          boolean(),
          %{optional(String.t()) => term()} | nil,
          boolean()
        ) :: {:ok, list(term())} | {:error, Snakepit.Error.t()}
  @spec bootstrap_trace_data(
          term(),
          list(term()),
          term() | nil,
          integer() | nil,
          term(),
          term(),
          float(),
          float(),
          boolean(),
          %{optional(String.t()) => term()} | nil,
          boolean(),
          keyword()
        ) :: {:ok, list(term())} | {:error, Snakepit.Error.t()}
  def bootstrap_trace_data(program, dataset) do
    SnakeBridge.Runtime.call(__MODULE__, :bootstrap_trace_data, [program, dataset], [])
  end

  def bootstrap_trace_data(program, dataset, opts)
      when is_list(opts) and
             (opts == [] or
                (is_tuple(hd(opts)) and tuple_size(hd(opts)) == 2 and is_atom(elem(hd(opts), 0)))) do
    SnakeBridge.Runtime.call(__MODULE__, :bootstrap_trace_data, [program, dataset], opts)
  end

  def bootstrap_trace_data(program, dataset, metric) do
    SnakeBridge.Runtime.call(__MODULE__, :bootstrap_trace_data, [program, dataset, metric], [])
  end

  def bootstrap_trace_data(program, dataset, metric, opts)
      when is_list(opts) and
             (opts == [] or
                (is_tuple(hd(opts)) and tuple_size(hd(opts)) == 2 and is_atom(elem(hd(opts), 0)))) do
    SnakeBridge.Runtime.call(__MODULE__, :bootstrap_trace_data, [program, dataset, metric], opts)
  end

  def bootstrap_trace_data(program, dataset, metric, num_threads) do
    SnakeBridge.Runtime.call(
      __MODULE__,
      :bootstrap_trace_data,
      [program, dataset, metric, num_threads],
      []
    )
  end

  def bootstrap_trace_data(program, dataset, metric, num_threads, opts)
      when is_list(opts) and
             (opts == [] or
                (is_tuple(hd(opts)) and tuple_size(hd(opts)) == 2 and is_atom(elem(hd(opts), 0)))) do
    SnakeBridge.Runtime.call(
      __MODULE__,
      :bootstrap_trace_data,
      [program, dataset, metric, num_threads],
      opts
    )
  end

  def bootstrap_trace_data(program, dataset, metric, num_threads, raise_on_error) do
    SnakeBridge.Runtime.call(
      __MODULE__,
      :bootstrap_trace_data,
      [program, dataset, metric, num_threads, raise_on_error],
      []
    )
  end

  def bootstrap_trace_data(program, dataset, metric, num_threads, raise_on_error, opts)
      when is_list(opts) and
             (opts == [] or
                (is_tuple(hd(opts)) and tuple_size(hd(opts)) == 2 and is_atom(elem(hd(opts), 0)))) do
    SnakeBridge.Runtime.call(
      __MODULE__,
      :bootstrap_trace_data,
      [program, dataset, metric, num_threads, raise_on_error],
      opts
    )
  end

  def bootstrap_trace_data(
        program,
        dataset,
        metric,
        num_threads,
        raise_on_error,
        capture_failed_parses
      ) do
    SnakeBridge.Runtime.call(
      __MODULE__,
      :bootstrap_trace_data,
      [program, dataset, metric, num_threads, raise_on_error, capture_failed_parses],
      []
    )
  end

  def bootstrap_trace_data(
        program,
        dataset,
        metric,
        num_threads,
        raise_on_error,
        capture_failed_parses,
        opts
      )
      when is_list(opts) and
             (opts == [] or
                (is_tuple(hd(opts)) and tuple_size(hd(opts)) == 2 and is_atom(elem(hd(opts), 0)))) do
    SnakeBridge.Runtime.call(
      __MODULE__,
      :bootstrap_trace_data,
      [program, dataset, metric, num_threads, raise_on_error, capture_failed_parses],
      opts
    )
  end

  def bootstrap_trace_data(
        program,
        dataset,
        metric,
        num_threads,
        raise_on_error,
        capture_failed_parses,
        failure_score
      ) do
    SnakeBridge.Runtime.call(
      __MODULE__,
      :bootstrap_trace_data,
      [
        program,
        dataset,
        metric,
        num_threads,
        raise_on_error,
        capture_failed_parses,
        failure_score
      ],
      []
    )
  end

  def bootstrap_trace_data(
        program,
        dataset,
        metric,
        num_threads,
        raise_on_error,
        capture_failed_parses,
        failure_score,
        opts
      )
      when is_list(opts) and
             (opts == [] or
                (is_tuple(hd(opts)) and tuple_size(hd(opts)) == 2 and is_atom(elem(hd(opts), 0)))) do
    SnakeBridge.Runtime.call(
      __MODULE__,
      :bootstrap_trace_data,
      [
        program,
        dataset,
        metric,
        num_threads,
        raise_on_error,
        capture_failed_parses,
        failure_score
      ],
      opts
    )
  end

  def bootstrap_trace_data(
        program,
        dataset,
        metric,
        num_threads,
        raise_on_error,
        capture_failed_parses,
        failure_score,
        format_failure_score
      ) do
    SnakeBridge.Runtime.call(
      __MODULE__,
      :bootstrap_trace_data,
      [
        program,
        dataset,
        metric,
        num_threads,
        raise_on_error,
        capture_failed_parses,
        failure_score,
        format_failure_score
      ],
      []
    )
  end

  def bootstrap_trace_data(
        program,
        dataset,
        metric,
        num_threads,
        raise_on_error,
        capture_failed_parses,
        failure_score,
        format_failure_score,
        opts
      )
      when is_list(opts) and
             (opts == [] or
                (is_tuple(hd(opts)) and tuple_size(hd(opts)) == 2 and is_atom(elem(hd(opts), 0)))) do
    SnakeBridge.Runtime.call(
      __MODULE__,
      :bootstrap_trace_data,
      [
        program,
        dataset,
        metric,
        num_threads,
        raise_on_error,
        capture_failed_parses,
        failure_score,
        format_failure_score
      ],
      opts
    )
  end

  def bootstrap_trace_data(
        program,
        dataset,
        metric,
        num_threads,
        raise_on_error,
        capture_failed_parses,
        failure_score,
        format_failure_score,
        log_format_failures
      ) do
    SnakeBridge.Runtime.call(
      __MODULE__,
      :bootstrap_trace_data,
      [
        program,
        dataset,
        metric,
        num_threads,
        raise_on_error,
        capture_failed_parses,
        failure_score,
        format_failure_score,
        log_format_failures
      ],
      []
    )
  end

  def bootstrap_trace_data(
        program,
        dataset,
        metric,
        num_threads,
        raise_on_error,
        capture_failed_parses,
        failure_score,
        format_failure_score,
        log_format_failures,
        opts
      )
      when is_list(opts) and
             (opts == [] or
                (is_tuple(hd(opts)) and tuple_size(hd(opts)) == 2 and is_atom(elem(hd(opts), 0)))) do
    SnakeBridge.Runtime.call(
      __MODULE__,
      :bootstrap_trace_data,
      [
        program,
        dataset,
        metric,
        num_threads,
        raise_on_error,
        capture_failed_parses,
        failure_score,
        format_failure_score,
        log_format_failures
      ],
      opts
    )
  end

  def bootstrap_trace_data(
        program,
        dataset,
        metric,
        num_threads,
        raise_on_error,
        capture_failed_parses,
        failure_score,
        format_failure_score,
        log_format_failures,
        callback_metadata
      ) do
    SnakeBridge.Runtime.call(
      __MODULE__,
      :bootstrap_trace_data,
      [
        program,
        dataset,
        metric,
        num_threads,
        raise_on_error,
        capture_failed_parses,
        failure_score,
        format_failure_score,
        log_format_failures,
        callback_metadata
      ],
      []
    )
  end

  def bootstrap_trace_data(
        program,
        dataset,
        metric,
        num_threads,
        raise_on_error,
        capture_failed_parses,
        failure_score,
        format_failure_score,
        log_format_failures,
        callback_metadata,
        opts
      )
      when is_list(opts) and
             (opts == [] or
                (is_tuple(hd(opts)) and tuple_size(hd(opts)) == 2 and is_atom(elem(hd(opts), 0)))) do
    SnakeBridge.Runtime.call(
      __MODULE__,
      :bootstrap_trace_data,
      [
        program,
        dataset,
        metric,
        num_threads,
        raise_on_error,
        capture_failed_parses,
        failure_score,
        format_failure_score,
        log_format_failures,
        callback_metadata
      ],
      opts
    )
  end

  def bootstrap_trace_data(
        program,
        dataset,
        metric,
        num_threads,
        raise_on_error,
        capture_failed_parses,
        failure_score,
        format_failure_score,
        log_format_failures,
        callback_metadata,
        capture_crashes
      ) do
    SnakeBridge.Runtime.call(
      __MODULE__,
      :bootstrap_trace_data,
      [
        program,
        dataset,
        metric,
        num_threads,
        raise_on_error,
        capture_failed_parses,
        failure_score,
        format_failure_score,
        log_format_failures,
        callback_metadata,
        capture_crashes
      ],
      []
    )
  end

  def bootstrap_trace_data(
        program,
        dataset,
        metric,
        num_threads,
        raise_on_error,
        capture_failed_parses,
        failure_score,
        format_failure_score,
        log_format_failures,
        callback_metadata,
        capture_crashes,
        opts
      )
      when is_list(opts) and
             (opts == [] or
                (is_tuple(hd(opts)) and tuple_size(hd(opts)) == 2 and is_atom(elem(hd(opts), 0)))) do
    SnakeBridge.Runtime.call(
      __MODULE__,
      :bootstrap_trace_data,
      [
        program,
        dataset,
        metric,
        num_threads,
        raise_on_error,
        capture_failed_parses,
        failure_score,
        format_failure_score,
        log_format_failures,
        callback_metadata,
        capture_crashes
      ],
      opts
    )
  end

  @doc """
  Set the default language model, adapter, and other settings for DSPy.

  Call `dspy.configure(...)` once near the top of your script. Every
  DSPy module will use these defaults unless you override them with
  `dspy.context(...)`. The values persist until you call
  `dspy.configure(...)` again.

  Only the thread that first calls `dspy.configure(...)` may call it
  again. Use `dspy.context(...)` for temporary overrides in other
  threads or async tasks.

  ## Examples

    Set a default LM:
    ```python
    import dspy

    dspy.configure(lm=dspy.LM("openai/gpt-5-mini"))
    ```

    Set multiple defaults at once:
    ```python
    import dspy

    dspy.configure(
    lm=dspy.LM("anthropic/claude-sonnet-4-6"),
    adapter=dspy.JSONAdapter(),
    track_usage=True,
    )
    ```

  ## Parameters

  - `kwargs` (term())

  ## Returns

  - `term()`
  """
  @spec configure(keyword()) :: {:ok, term()} | {:error, Snakepit.Error.t()}
  def configure(opts \\ []) do
    SnakeBridge.Runtime.call(__MODULE__, :configure, [], opts)
  end

  @doc """
  Configure the cache for DSPy.



  ## Parameters

  - `enable_disk_cache` - Whether to enable on-disk cache.
  - `enable_memory_cache` - Whether to enable in-memory cache.
  - `disk_cache_dir` - The directory to store the on-disk cache.
  - `disk_size_limit_bytes` - The size limit of the on-disk cache.
  - `memory_max_entries` - The maximum number of entries in the in-memory cache. To allow the cache to grow without bounds, set this parameter to `math.inf` or a similar value.
  - `restrict_pickle` - When True, restrict pickle deserialization to a known-safe set of types. When False (default), use unrestricted pickle.
  - `safe_types` - Additional types to allow when restrict_pickle is True.

  ## Returns

  - `term()`
  """
  @spec configure_cache() :: {:ok, term()} | {:error, Snakepit.Error.t()}
  @spec configure_cache(keyword()) :: {:ok, term()} | {:error, Snakepit.Error.t()}
  @spec configure_cache(boolean() | nil) :: {:ok, term()} | {:error, Snakepit.Error.t()}
  @spec configure_cache(boolean() | nil, keyword()) ::
          {:ok, term()} | {:error, Snakepit.Error.t()}
  @spec configure_cache(boolean() | nil, boolean() | nil) ::
          {:ok, term()} | {:error, Snakepit.Error.t()}
  @spec configure_cache(boolean() | nil, boolean() | nil, keyword()) ::
          {:ok, term()} | {:error, Snakepit.Error.t()}
  @spec configure_cache(boolean() | nil, boolean() | nil, String.t() | nil) ::
          {:ok, term()} | {:error, Snakepit.Error.t()}
  @spec configure_cache(boolean() | nil, boolean() | nil, String.t() | nil, keyword()) ::
          {:ok, term()} | {:error, Snakepit.Error.t()}
  @spec configure_cache(boolean() | nil, boolean() | nil, String.t() | nil, integer() | nil) ::
          {:ok, term()} | {:error, Snakepit.Error.t()}
  @spec configure_cache(
          boolean() | nil,
          boolean() | nil,
          String.t() | nil,
          integer() | nil,
          keyword()
        ) :: {:ok, term()} | {:error, Snakepit.Error.t()}
  @spec configure_cache(
          boolean() | nil,
          boolean() | nil,
          String.t() | nil,
          integer() | nil,
          integer()
        ) :: {:ok, term()} | {:error, Snakepit.Error.t()}
  @spec configure_cache(
          boolean() | nil,
          boolean() | nil,
          String.t() | nil,
          integer() | nil,
          integer(),
          keyword()
        ) :: {:ok, term()} | {:error, Snakepit.Error.t()}
  @spec configure_cache(
          boolean() | nil,
          boolean() | nil,
          String.t() | nil,
          integer() | nil,
          integer(),
          boolean()
        ) :: {:ok, term()} | {:error, Snakepit.Error.t()}
  @spec configure_cache(
          boolean() | nil,
          boolean() | nil,
          String.t() | nil,
          integer() | nil,
          integer(),
          boolean(),
          keyword()
        ) :: {:ok, term()} | {:error, Snakepit.Error.t()}
  @spec configure_cache(
          boolean() | nil,
          boolean() | nil,
          String.t() | nil,
          integer() | nil,
          integer(),
          boolean(),
          list(term()) | nil
        ) :: {:ok, term()} | {:error, Snakepit.Error.t()}
  @spec configure_cache(
          boolean() | nil,
          boolean() | nil,
          String.t() | nil,
          integer() | nil,
          integer(),
          boolean(),
          list(term()) | nil,
          keyword()
        ) :: {:ok, term()} | {:error, Snakepit.Error.t()}
  def configure_cache() do
    SnakeBridge.Runtime.call(__MODULE__, :configure_cache, [], [])
  end

  def configure_cache(opts)
      when is_list(opts) and
             (opts == [] or
                (is_tuple(hd(opts)) and tuple_size(hd(opts)) == 2 and is_atom(elem(hd(opts), 0)))) do
    SnakeBridge.Runtime.call(__MODULE__, :configure_cache, [], opts)
  end

  def configure_cache(enable_disk_cache) do
    SnakeBridge.Runtime.call(__MODULE__, :configure_cache, [enable_disk_cache], [])
  end

  def configure_cache(enable_disk_cache, opts)
      when is_list(opts) and
             (opts == [] or
                (is_tuple(hd(opts)) and tuple_size(hd(opts)) == 2 and is_atom(elem(hd(opts), 0)))) do
    SnakeBridge.Runtime.call(__MODULE__, :configure_cache, [enable_disk_cache], opts)
  end

  def configure_cache(enable_disk_cache, enable_memory_cache) do
    SnakeBridge.Runtime.call(
      __MODULE__,
      :configure_cache,
      [enable_disk_cache, enable_memory_cache],
      []
    )
  end

  def configure_cache(enable_disk_cache, enable_memory_cache, opts)
      when is_list(opts) and
             (opts == [] or
                (is_tuple(hd(opts)) and tuple_size(hd(opts)) == 2 and is_atom(elem(hd(opts), 0)))) do
    SnakeBridge.Runtime.call(
      __MODULE__,
      :configure_cache,
      [enable_disk_cache, enable_memory_cache],
      opts
    )
  end

  def configure_cache(enable_disk_cache, enable_memory_cache, disk_cache_dir) do
    SnakeBridge.Runtime.call(
      __MODULE__,
      :configure_cache,
      [enable_disk_cache, enable_memory_cache, disk_cache_dir],
      []
    )
  end

  def configure_cache(enable_disk_cache, enable_memory_cache, disk_cache_dir, opts)
      when is_list(opts) and
             (opts == [] or
                (is_tuple(hd(opts)) and tuple_size(hd(opts)) == 2 and is_atom(elem(hd(opts), 0)))) do
    SnakeBridge.Runtime.call(
      __MODULE__,
      :configure_cache,
      [enable_disk_cache, enable_memory_cache, disk_cache_dir],
      opts
    )
  end

  def configure_cache(
        enable_disk_cache,
        enable_memory_cache,
        disk_cache_dir,
        disk_size_limit_bytes
      ) do
    SnakeBridge.Runtime.call(
      __MODULE__,
      :configure_cache,
      [enable_disk_cache, enable_memory_cache, disk_cache_dir, disk_size_limit_bytes],
      []
    )
  end

  def configure_cache(
        enable_disk_cache,
        enable_memory_cache,
        disk_cache_dir,
        disk_size_limit_bytes,
        opts
      )
      when is_list(opts) and
             (opts == [] or
                (is_tuple(hd(opts)) and tuple_size(hd(opts)) == 2 and is_atom(elem(hd(opts), 0)))) do
    SnakeBridge.Runtime.call(
      __MODULE__,
      :configure_cache,
      [enable_disk_cache, enable_memory_cache, disk_cache_dir, disk_size_limit_bytes],
      opts
    )
  end

  def configure_cache(
        enable_disk_cache,
        enable_memory_cache,
        disk_cache_dir,
        disk_size_limit_bytes,
        memory_max_entries
      ) do
    SnakeBridge.Runtime.call(
      __MODULE__,
      :configure_cache,
      [
        enable_disk_cache,
        enable_memory_cache,
        disk_cache_dir,
        disk_size_limit_bytes,
        memory_max_entries
      ],
      []
    )
  end

  def configure_cache(
        enable_disk_cache,
        enable_memory_cache,
        disk_cache_dir,
        disk_size_limit_bytes,
        memory_max_entries,
        opts
      )
      when is_list(opts) and
             (opts == [] or
                (is_tuple(hd(opts)) and tuple_size(hd(opts)) == 2 and is_atom(elem(hd(opts), 0)))) do
    SnakeBridge.Runtime.call(
      __MODULE__,
      :configure_cache,
      [
        enable_disk_cache,
        enable_memory_cache,
        disk_cache_dir,
        disk_size_limit_bytes,
        memory_max_entries
      ],
      opts
    )
  end

  def configure_cache(
        enable_disk_cache,
        enable_memory_cache,
        disk_cache_dir,
        disk_size_limit_bytes,
        memory_max_entries,
        restrict_pickle
      ) do
    SnakeBridge.Runtime.call(
      __MODULE__,
      :configure_cache,
      [
        enable_disk_cache,
        enable_memory_cache,
        disk_cache_dir,
        disk_size_limit_bytes,
        memory_max_entries,
        restrict_pickle
      ],
      []
    )
  end

  def configure_cache(
        enable_disk_cache,
        enable_memory_cache,
        disk_cache_dir,
        disk_size_limit_bytes,
        memory_max_entries,
        restrict_pickle,
        opts
      )
      when is_list(opts) and
             (opts == [] or
                (is_tuple(hd(opts)) and tuple_size(hd(opts)) == 2 and is_atom(elem(hd(opts), 0)))) do
    SnakeBridge.Runtime.call(
      __MODULE__,
      :configure_cache,
      [
        enable_disk_cache,
        enable_memory_cache,
        disk_cache_dir,
        disk_size_limit_bytes,
        memory_max_entries,
        restrict_pickle
      ],
      opts
    )
  end

  def configure_cache(
        enable_disk_cache,
        enable_memory_cache,
        disk_cache_dir,
        disk_size_limit_bytes,
        memory_max_entries,
        restrict_pickle,
        safe_types
      ) do
    SnakeBridge.Runtime.call(
      __MODULE__,
      :configure_cache,
      [
        enable_disk_cache,
        enable_memory_cache,
        disk_cache_dir,
        disk_size_limit_bytes,
        memory_max_entries,
        restrict_pickle,
        safe_types
      ],
      []
    )
  end

  def configure_cache(
        enable_disk_cache,
        enable_memory_cache,
        disk_cache_dir,
        disk_size_limit_bytes,
        memory_max_entries,
        restrict_pickle,
        safe_types,
        opts
      )
      when is_list(opts) and
             (opts == [] or
                (is_tuple(hd(opts)) and tuple_size(hd(opts)) == 2 and is_atom(elem(hd(opts), 0)))) do
    SnakeBridge.Runtime.call(
      __MODULE__,
      :configure_cache,
      [
        enable_disk_cache,
        enable_memory_cache,
        disk_cache_dir,
        disk_size_limit_bytes,
        memory_max_entries,
        restrict_pickle,
        safe_types
      ],
      opts
    )
  end

  @doc """
  Python binding for `dspy.configure_dspy_loggers`.

  ## Parameters

  - `root_module_name` (term())

  ## Returns

  - `term()`
  """
  @spec configure_dspy_loggers(term(), keyword()) :: {:ok, term()} | {:error, Snakepit.Error.t()}
  def configure_dspy_loggers(root_module_name, opts \\ []) do
    SnakeBridge.Runtime.call(__MODULE__, :configure_dspy_loggers, [root_module_name], opts)
  end

  @doc """
  Override DSPy settings for one `with` block.

  Use `dspy.context(...)` when you need temporary settings—a different
  LM, adapter, or flag—without changing the process-wide defaults from
  `dspy.configure(...)`. The block inherits every current setting,
  overrides only the keys you pass, and restores the originals on exit.

  Unlike `dspy.configure(...)`, you can call `dspy.context(...)` from
  any thread or async task.

  ## Examples

    Use a different LM for one call:
    ```python
    import dspy

    dspy.configure(lm=dspy.LM("openai/gpt-5-mini"))
    qa = dspy.Predict("question -> answer")

    with dspy.context(lm=dspy.LM("anthropic/claude-sonnet-4-6")):
    result = qa(question="What is the capital of France?")
    # uses claude-sonnet-4-6 inside this block
    # back to gpt-5-mini here
    ```

  ## Parameters

  - `kwargs` (term())

  ## Returns

  - `term()`
  """
  @spec context(keyword()) :: {:ok, term()} | {:error, Snakepit.Error.t()}
  def context(opts \\ []) do
    SnakeBridge.Runtime.call(__MODULE__, :context, [], opts)
  end

  @doc """
  Python binding for `dspy.disable_litellm_logging`.

  ## Returns

  - `term()`
  """
  @spec disable_litellm_logging(keyword()) :: {:ok, term()} | {:error, Snakepit.Error.t()}
  def disable_litellm_logging(opts \\ []) do
    SnakeBridge.Runtime.call(__MODULE__, :disable_litellm_logging, [], opts)
  end

  @doc """
  Disables the `DSPyLoggingStream` used by event logging APIs throughout DSPy

  (`eprint()`, `logger.info()`, etc), silencing all subsequent event logs.

  ## Returns

  - `term()`
  """
  @spec disable_logging(keyword()) :: {:ok, term()} | {:error, Snakepit.Error.t()}
  def disable_logging(opts \\ []) do
    SnakeBridge.Runtime.call(__MODULE__, :disable_logging, [], opts)
  end

  @doc """
  Python binding for `dspy.enable_litellm_logging`.

  ## Returns

  - `term()`
  """
  @spec enable_litellm_logging(keyword()) :: {:ok, term()} | {:error, Snakepit.Error.t()}
  def enable_litellm_logging(opts \\ []) do
    SnakeBridge.Runtime.call(__MODULE__, :enable_litellm_logging, [], opts)
  end

  @doc """
  Enables the `DSPyLoggingStream` used by event logging APIs throughout DSPy

  (`eprint()`, `logger.info()`, etc), emitting all subsequent event logs. This
  reverses the effects of `disable_logging()`.

  ## Returns

  - `term()`
  """
  @spec enable_logging(keyword()) :: {:ok, term()} | {:error, Snakepit.Error.t()}
  def enable_logging(opts \\ []) do
    SnakeBridge.Runtime.call(__MODULE__, :enable_logging, [], opts)
  end

  @doc """
  Python binding for `dspy.ensure_signature`.

  ## Parameters

  - `signature` (String.t() | term())
  - `instructions` (term() default: None)

  ## Returns

  - `term() | nil`
  """
  @spec ensure_signature(String.t() | term()) ::
          {:ok, term() | nil} | {:error, Snakepit.Error.t()}
  @spec ensure_signature(String.t() | term(), keyword()) ::
          {:ok, term() | nil} | {:error, Snakepit.Error.t()}
  @spec ensure_signature(String.t() | term(), term()) ::
          {:ok, term() | nil} | {:error, Snakepit.Error.t()}
  @spec ensure_signature(String.t() | term(), term(), keyword()) ::
          {:ok, term() | nil} | {:error, Snakepit.Error.t()}
  def ensure_signature(signature) do
    SnakeBridge.Runtime.call(__MODULE__, :ensure_signature, [signature], [])
  end

  def ensure_signature(signature, opts)
      when is_list(opts) and
             (opts == [] or
                (is_tuple(hd(opts)) and tuple_size(hd(opts)) == 2 and is_atom(elem(hd(opts), 0)))) do
    SnakeBridge.Runtime.call(__MODULE__, :ensure_signature, [signature], opts)
  end

  def ensure_signature(signature, instructions) do
    SnakeBridge.Runtime.call(__MODULE__, :ensure_signature, [signature, instructions], [])
  end

  def ensure_signature(signature, instructions, opts)
      when is_list(opts) and
             (opts == [] or
                (is_tuple(hd(opts)) and tuple_size(hd(opts)) == 2 and is_atom(elem(hd(opts), 0)))) do
    SnakeBridge.Runtime.call(__MODULE__, :ensure_signature, [signature, instructions], opts)
  end

  @doc """
  Infer a prefix from an attribute name by converting it to a human-readable format.



  ## Examples

    "camelCaseText" -> "Camel Case Text"
    "snake_case_text" -> "Snake Case Text"
    "text2number" -> "Text 2 Number"
    "HTMLParser" -> "HTML Parser"

  ## Parameters

  - `attribute_name` (String.t())

  ## Returns

  - `String.t()`
  """
  @spec infer_prefix(String.t(), keyword()) :: {:ok, String.t()} | {:error, Snakepit.Error.t()}
  def infer_prefix(attribute_name, opts \\ []) do
    SnakeBridge.Runtime.call(__MODULE__, :infer_prefix, [attribute_name], opts)
  end

  @doc """
  Python binding for `dspy.InputField`.

  ## Parameters

  - `kwargs` (term())

  ## Returns

  - `term()`
  """
  @spec input_field(keyword()) :: {:ok, term()} | {:error, Snakepit.Error.t()}
  def input_field(opts \\ []) do
    SnakeBridge.Runtime.call(__MODULE__, "InputField", [], opts)
  end

  @doc """
  The global history shared across all LMs.



  ## Parameters

  - `n` - Number of recent entries to display. Defaults to 1.
  - `file` - An optional file-like object to write output to. When provided, ANSI color codes are automatically disabled. Defaults to `None` (prints to stdout).

  ## Returns

  - `nil`
  """
  @spec inspect_history() :: {:ok, nil} | {:error, Snakepit.Error.t()}
  @spec inspect_history(keyword()) :: {:ok, nil} | {:error, Snakepit.Error.t()}
  @spec inspect_history(integer()) :: {:ok, nil} | {:error, Snakepit.Error.t()}
  @spec inspect_history(integer(), keyword()) :: {:ok, nil} | {:error, Snakepit.Error.t()}
  @spec inspect_history(integer(), term() | nil) :: {:ok, nil} | {:error, Snakepit.Error.t()}
  @spec inspect_history(integer(), term() | nil, keyword()) ::
          {:ok, nil} | {:error, Snakepit.Error.t()}
  def inspect_history() do
    SnakeBridge.Runtime.call(__MODULE__, :inspect_history, [], [])
  end

  def inspect_history(opts)
      when is_list(opts) and
             (opts == [] or
                (is_tuple(hd(opts)) and tuple_size(hd(opts)) == 2 and is_atom(elem(hd(opts), 0)))) do
    SnakeBridge.Runtime.call(__MODULE__, :inspect_history, [], opts)
  end

  def inspect_history(n) do
    SnakeBridge.Runtime.call(__MODULE__, :inspect_history, [n], [])
  end

  def inspect_history(n, opts)
      when is_list(opts) and
             (opts == [] or
                (is_tuple(hd(opts)) and tuple_size(hd(opts)) == 2 and is_atom(elem(hd(opts), 0)))) do
    SnakeBridge.Runtime.call(__MODULE__, :inspect_history, [n], opts)
  end

  def inspect_history(n, file) do
    SnakeBridge.Runtime.call(__MODULE__, :inspect_history, [n, file], [])
  end

  def inspect_history(n, file, opts)
      when is_list(opts) and
             (opts == [] or
                (is_tuple(hd(opts)) and tuple_size(hd(opts)) == 2 and is_atom(elem(hd(opts), 0)))) do
    SnakeBridge.Runtime.call(__MODULE__, :inspect_history, [n, file], opts)
  end

  @doc """
  Return whether an LM error is generally safe to retry.

  DSPy owns retries; managed engines perform one attempt. This classification
  describes transient failures, not proof that the provider did no work.
  Never replay after visible stream output or a completed generation, and
  respect provider policy and valid `retry_after` hints. Network retries can
  still repeat a request already processed or billed by the provider.

  ## Parameters

  - `error` - The exception to classify.

  ## Returns

  - `boolean()`
  """
  @spec is_retryable_lm_error(term(), keyword()) ::
          {:ok, boolean()} | {:error, Snakepit.Error.t()}
  def is_retryable_lm_error(error, opts \\ []) do
    SnakeBridge.Runtime.call(__MODULE__, :is_retryable_lm_error, [error], opts)
  end

  @doc """
  Load saved DSPy model.

  This method is used to load a saved DSPy model with `save_program=True`, i.e., the model is saved with cloudpickle.

  ## Parameters

  - `path` - Path to the saved model. (type: `String.t()`)
  - `allow_pickle` - Whether to allow loading the model with pickle. This is dangerous and should only be used if you are sure you trust the source of the model. (type: `boolean()`)

  ## Returns

  - `term()`
  """
  @spec load(String.t()) :: {:ok, term()} | {:error, Snakepit.Error.t()}
  @spec load(String.t(), keyword()) :: {:ok, term()} | {:error, Snakepit.Error.t()}
  @spec load(String.t(), boolean()) :: {:ok, term()} | {:error, Snakepit.Error.t()}
  @spec load(String.t(), boolean(), keyword()) :: {:ok, term()} | {:error, Snakepit.Error.t()}
  def load(path) do
    SnakeBridge.Runtime.call(__MODULE__, :load, [path], [])
  end

  def load(path, opts)
      when is_list(opts) and
             (opts == [] or
                (is_tuple(hd(opts)) and tuple_size(hd(opts)) == 2 and is_atom(elem(hd(opts), 0)))) do
    SnakeBridge.Runtime.call(__MODULE__, :load, [path], opts)
  end

  def load(path, allow_pickle) do
    SnakeBridge.Runtime.call(__MODULE__, :load, [path, allow_pickle], [])
  end

  def load(path, allow_pickle, opts)
      when is_list(opts) and
             (opts == [] or
                (is_tuple(hd(opts)) and tuple_size(hd(opts)) == 2 and is_atom(elem(hd(opts), 0)))) do
    SnakeBridge.Runtime.call(__MODULE__, :load, [path, allow_pickle], opts)
  end

  @doc """
  Load the settings from a file using cloudpickle.



  ## Parameters

  - `path` - The file path to load the settings from.
  - `allow_pickle` - Whether to allow loading with pickle. Loading untrusted .pkl files can run arbitrary code. Set to True only if you trust the source of the file.

  ## Returns

  - `%{optional(String.t()) => term()}`
  """
  @spec load_settings(String.t()) ::
          {:ok, %{optional(String.t()) => term()}} | {:error, Snakepit.Error.t()}
  @spec load_settings(String.t(), keyword()) ::
          {:ok, %{optional(String.t()) => term()}} | {:error, Snakepit.Error.t()}
  @spec load_settings(String.t(), boolean()) ::
          {:ok, %{optional(String.t()) => term()}} | {:error, Snakepit.Error.t()}
  @spec load_settings(String.t(), boolean(), keyword()) ::
          {:ok, %{optional(String.t()) => term()}} | {:error, Snakepit.Error.t()}
  def load_settings(path) do
    SnakeBridge.Runtime.call(__MODULE__, :load_settings, [path], [])
  end

  def load_settings(path, opts)
      when is_list(opts) and
             (opts == [] or
                (is_tuple(hd(opts)) and tuple_size(hd(opts)) == 2 and is_atom(elem(hd(opts), 0)))) do
    SnakeBridge.Runtime.call(__MODULE__, :load_settings, [path], opts)
  end

  def load_settings(path, allow_pickle) do
    SnakeBridge.Runtime.call(__MODULE__, :load_settings, [path, allow_pickle], [])
  end

  def load_settings(path, allow_pickle, opts)
      when is_list(opts) and
             (opts == [] or
                (is_tuple(hd(opts)) and tuple_size(hd(opts)) == 2 and is_atom(elem(hd(opts), 0)))) do
    SnakeBridge.Runtime.call(__MODULE__, :load_settings, [path, allow_pickle], opts)
  end

  @doc """
  Returns the most common completion for the target field (or the last field) in the signature.

  When normalize returns None, that completion is ignored.
  In case of a tie, earlier completion are prioritized.

  ## Parameters

  - `prediction_or_completions` (term())
  - `normalize` (term() default: <function default_normalize at 0x71ae10de6a30>)
  - `field` (term() default: None)

  ## Returns

  - `term()`
  """
  @spec majority(term()) :: {:ok, term()} | {:error, Snakepit.Error.t()}
  @spec majority(term(), keyword()) :: {:ok, term()} | {:error, Snakepit.Error.t()}
  @spec majority(term(), term()) :: {:ok, term()} | {:error, Snakepit.Error.t()}
  @spec majority(term(), term(), keyword()) :: {:ok, term()} | {:error, Snakepit.Error.t()}
  @spec majority(term(), term(), term()) :: {:ok, term()} | {:error, Snakepit.Error.t()}
  @spec majority(term(), term(), term(), keyword()) ::
          {:ok, term()} | {:error, Snakepit.Error.t()}
  def majority(prediction_or_completions) do
    SnakeBridge.Runtime.call(__MODULE__, :majority, [prediction_or_completions], [])
  end

  def majority(prediction_or_completions, opts)
      when is_list(opts) and
             (opts == [] or
                (is_tuple(hd(opts)) and tuple_size(hd(opts)) == 2 and is_atom(elem(hd(opts), 0)))) do
    SnakeBridge.Runtime.call(__MODULE__, :majority, [prediction_or_completions], opts)
  end

  def majority(prediction_or_completions, normalize) do
    SnakeBridge.Runtime.call(__MODULE__, :majority, [prediction_or_completions, normalize], [])
  end

  def majority(prediction_or_completions, normalize, opts)
      when is_list(opts) and
             (opts == [] or
                (is_tuple(hd(opts)) and tuple_size(hd(opts)) == 2 and is_atom(elem(hd(opts), 0)))) do
    SnakeBridge.Runtime.call(__MODULE__, :majority, [prediction_or_completions, normalize], opts)
  end

  def majority(prediction_or_completions, normalize, field) do
    SnakeBridge.Runtime.call(
      __MODULE__,
      :majority,
      [prediction_or_completions, normalize, field],
      []
    )
  end

  def majority(prediction_or_completions, normalize, field, opts)
      when is_list(opts) and
             (opts == [] or
                (is_tuple(hd(opts)) and tuple_size(hd(opts)) == 2 and is_atom(elem(hd(opts), 0)))) do
    SnakeBridge.Runtime.call(
      __MODULE__,
      :majority,
      [prediction_or_completions, normalize, field],
      opts
    )
  end

  @doc """
  Create a new Signature subclass with the specified fields and instructions.



  ## Parameters

  - `signature` - Either a string in the format "input1, input2 -> output1, output2" or a dictionary mapping field names to tuples of (type, FieldInfo).
  - `instructions` - Optional string containing instructions/prompt for the signature. If not provided, defaults to a basic description of inputs and outputs.
  - `signature_name` - Optional string to name the generated Signature subclass. Defaults to "StringSignature".
  - `custom_types` - Optional dictionary mapping type names to their actual type objects. Useful for resolving custom types that aren't built-ins or in the typing module.

  ## Returns

  - `term()`
  """
  @spec make_signature(String.t() | %{optional(String.t()) => {term(), term()}}) ::
          {:ok, term()} | {:error, Snakepit.Error.t()}
  @spec make_signature(String.t() | %{optional(String.t()) => {term(), term()}}, keyword()) ::
          {:ok, term()} | {:error, Snakepit.Error.t()}
  @spec make_signature(String.t() | %{optional(String.t()) => {term(), term()}}, String.t() | nil) ::
          {:ok, term()} | {:error, Snakepit.Error.t()}
  @spec make_signature(
          String.t() | %{optional(String.t()) => {term(), term()}},
          String.t() | nil,
          keyword()
        ) :: {:ok, term()} | {:error, Snakepit.Error.t()}
  @spec make_signature(
          String.t() | %{optional(String.t()) => {term(), term()}},
          String.t() | nil,
          String.t()
        ) :: {:ok, term()} | {:error, Snakepit.Error.t()}
  @spec make_signature(
          String.t() | %{optional(String.t()) => {term(), term()}},
          String.t() | nil,
          String.t(),
          keyword()
        ) :: {:ok, term()} | {:error, Snakepit.Error.t()}
  @spec make_signature(
          String.t() | %{optional(String.t()) => {term(), term()}},
          String.t() | nil,
          String.t(),
          %{optional(String.t()) => term()} | nil
        ) :: {:ok, term()} | {:error, Snakepit.Error.t()}
  @spec make_signature(
          String.t() | %{optional(String.t()) => {term(), term()}},
          String.t() | nil,
          String.t(),
          %{optional(String.t()) => term()} | nil,
          keyword()
        ) :: {:ok, term()} | {:error, Snakepit.Error.t()}
  def make_signature(signature) do
    SnakeBridge.Runtime.call(__MODULE__, :make_signature, [signature], [])
  end

  def make_signature(signature, opts)
      when is_list(opts) and
             (opts == [] or
                (is_tuple(hd(opts)) and tuple_size(hd(opts)) == 2 and is_atom(elem(hd(opts), 0)))) do
    SnakeBridge.Runtime.call(__MODULE__, :make_signature, [signature], opts)
  end

  def make_signature(signature, instructions) do
    SnakeBridge.Runtime.call(__MODULE__, :make_signature, [signature, instructions], [])
  end

  def make_signature(signature, instructions, opts)
      when is_list(opts) and
             (opts == [] or
                (is_tuple(hd(opts)) and tuple_size(hd(opts)) == 2 and is_atom(elem(hd(opts), 0)))) do
    SnakeBridge.Runtime.call(__MODULE__, :make_signature, [signature, instructions], opts)
  end

  def make_signature(signature, instructions, signature_name) do
    SnakeBridge.Runtime.call(
      __MODULE__,
      :make_signature,
      [signature, instructions, signature_name],
      []
    )
  end

  def make_signature(signature, instructions, signature_name, opts)
      when is_list(opts) and
             (opts == [] or
                (is_tuple(hd(opts)) and tuple_size(hd(opts)) == 2 and is_atom(elem(hd(opts), 0)))) do
    SnakeBridge.Runtime.call(
      __MODULE__,
      :make_signature,
      [signature, instructions, signature_name],
      opts
    )
  end

  def make_signature(signature, instructions, signature_name, custom_types) do
    SnakeBridge.Runtime.call(
      __MODULE__,
      :make_signature,
      [signature, instructions, signature_name, custom_types],
      []
    )
  end

  def make_signature(signature, instructions, signature_name, custom_types, opts)
      when is_list(opts) and
             (opts == [] or
                (is_tuple(hd(opts)) and tuple_size(hd(opts)) == 2 and is_atom(elem(hd(opts), 0)))) do
    SnakeBridge.Runtime.call(
      __MODULE__,
      :make_signature,
      [signature, instructions, signature_name, custom_types],
      opts
    )
  end

  @doc """
  Python binding for `dspy.OutputField`.

  ## Parameters

  - `kwargs` (term())

  ## Returns

  - `term()`
  """
  @spec output_field(keyword()) :: {:ok, term()} | {:error, Snakepit.Error.t()}
  def output_field(opts \\ []) do
    SnakeBridge.Runtime.call(__MODULE__, "OutputField", [], opts)
  end

  @doc """
  Return the factory that creates the next interpreter.

  The module's own factory wins, then `dspy.settings.interpreter_factory`, then
  `PythonInterpreter`. `None` and `PythonInterpreter` both mean "unset", since the
  code-executing modules carry `PythonInterpreter` as their literal default argument.

  Call this where you create an interpreter, not earlier, so that
  `dspy.context(interpreter_factory=...)` scopes the way callers expect.

  ## Raises

  - `ArgumentError` - If `dspy.settings.interpreter_factory` is not a zero-argument callable.

  ## Parameters

  - `factory` (term() | nil default: None)

  ## Returns

  - `term()`
  """
  @spec resolve_interpreter_factory() :: {:ok, term()} | {:error, Snakepit.Error.t()}
  @spec resolve_interpreter_factory(keyword()) :: {:ok, term()} | {:error, Snakepit.Error.t()}
  @spec resolve_interpreter_factory(term() | nil) :: {:ok, term()} | {:error, Snakepit.Error.t()}
  @spec resolve_interpreter_factory(term() | nil, keyword()) ::
          {:ok, term()} | {:error, Snakepit.Error.t()}
  def resolve_interpreter_factory() do
    SnakeBridge.Runtime.call(__MODULE__, :resolve_interpreter_factory, [], [])
  end

  def resolve_interpreter_factory(opts)
      when is_list(opts) and
             (opts == [] or
                (is_tuple(hd(opts)) and tuple_size(hd(opts)) == 2 and is_atom(elem(hd(opts), 0)))) do
    SnakeBridge.Runtime.call(__MODULE__, :resolve_interpreter_factory, [], opts)
  end

  def resolve_interpreter_factory(factory) do
    SnakeBridge.Runtime.call(__MODULE__, :resolve_interpreter_factory, [factory], [])
  end

  def resolve_interpreter_factory(factory, opts)
      when is_list(opts) and
             (opts == [] or
                (is_tuple(hd(opts)) and tuple_size(hd(opts)) == 2 and is_atom(elem(hd(opts), 0)))) do
    SnakeBridge.Runtime.call(__MODULE__, :resolve_interpreter_factory, [factory], opts)
  end

  @doc """
  Python module attribute `dspy.settings`.

  ## Returns

  - `term()`
  """
  @spec settings() :: {:ok, term()} | {:error, Snakepit.Error.t()}
  def settings() do
    SnakeBridge.Runtime.get_module_attr(__MODULE__, :settings)
  end

  @doc """
  Wrap a DSPy program so that it streams its outputs incrementally, rather than returning them

  all at once. It also provides status messages to the user to indicate the progress of the program, and users
  can implement their own status message provider to customize the status messages and what module to generate
  status messages for.

  ## Parameters

  - `program` - The DSPy program to wrap with streaming functionality.
  - `status_message_provider` - A custom status message generator to use instead of the default one. Users can implement their own status message generator to customize the status messages and what module to generate status messages for.
  - `stream_listeners` - A list of stream listeners to capture the streaming output of specific fields of sub predicts in the program. When provided, only the target fields in the target predict will be streamed to the user.
  - `include_final_prediction_in_output_stream` - Whether to include the final prediction in the output stream, only useful when `stream_listeners` is provided. If `False`, the final prediction will not be included in the output stream. When the program hit cache, or no listeners captured anything, the final prediction will still be included in the output stream even if this is `False`.
  - `is_async_program` - Whether the program is async. If `False`, the program will be wrapped with `asyncify`, otherwise the program will be called with `acall`.
  - `async_streaming` - Whether to return an async generator or a sync generator. If `False`, the streaming will be converted to a sync generator.

  ## Returns

  - `term()`
  """
  @spec streamify(term()) :: {:ok, term()} | {:error, Snakepit.Error.t()}
  @spec streamify(term(), keyword()) :: {:ok, term()} | {:error, Snakepit.Error.t()}
  @spec streamify(term(), term() | nil) :: {:ok, term()} | {:error, Snakepit.Error.t()}
  @spec streamify(term(), term() | nil, keyword()) :: {:ok, term()} | {:error, Snakepit.Error.t()}
  @spec streamify(term(), term() | nil, list(term()) | nil) ::
          {:ok, term()} | {:error, Snakepit.Error.t()}
  @spec streamify(term(), term() | nil, list(term()) | nil, keyword()) ::
          {:ok, term()} | {:error, Snakepit.Error.t()}
  @spec streamify(term(), term() | nil, list(term()) | nil, boolean()) ::
          {:ok, term()} | {:error, Snakepit.Error.t()}
  @spec streamify(term(), term() | nil, list(term()) | nil, boolean(), keyword()) ::
          {:ok, term()} | {:error, Snakepit.Error.t()}
  @spec streamify(term(), term() | nil, list(term()) | nil, boolean(), boolean()) ::
          {:ok, term()} | {:error, Snakepit.Error.t()}
  @spec streamify(term(), term() | nil, list(term()) | nil, boolean(), boolean(), keyword()) ::
          {:ok, term()} | {:error, Snakepit.Error.t()}
  @spec streamify(term(), term() | nil, list(term()) | nil, boolean(), boolean(), boolean()) ::
          {:ok, term()} | {:error, Snakepit.Error.t()}
  @spec streamify(
          term(),
          term() | nil,
          list(term()) | nil,
          boolean(),
          boolean(),
          boolean(),
          keyword()
        ) :: {:ok, term()} | {:error, Snakepit.Error.t()}
  def streamify(program) do
    SnakeBridge.Runtime.call(__MODULE__, :streamify, [program], [])
  end

  def streamify(program, opts)
      when is_list(opts) and
             (opts == [] or
                (is_tuple(hd(opts)) and tuple_size(hd(opts)) == 2 and is_atom(elem(hd(opts), 0)))) do
    SnakeBridge.Runtime.call(__MODULE__, :streamify, [program], opts)
  end

  def streamify(program, status_message_provider) do
    SnakeBridge.Runtime.call(__MODULE__, :streamify, [program, status_message_provider], [])
  end

  def streamify(program, status_message_provider, opts)
      when is_list(opts) and
             (opts == [] or
                (is_tuple(hd(opts)) and tuple_size(hd(opts)) == 2 and is_atom(elem(hd(opts), 0)))) do
    SnakeBridge.Runtime.call(__MODULE__, :streamify, [program, status_message_provider], opts)
  end

  def streamify(program, status_message_provider, stream_listeners) do
    SnakeBridge.Runtime.call(
      __MODULE__,
      :streamify,
      [program, status_message_provider, stream_listeners],
      []
    )
  end

  def streamify(program, status_message_provider, stream_listeners, opts)
      when is_list(opts) and
             (opts == [] or
                (is_tuple(hd(opts)) and tuple_size(hd(opts)) == 2 and is_atom(elem(hd(opts), 0)))) do
    SnakeBridge.Runtime.call(
      __MODULE__,
      :streamify,
      [program, status_message_provider, stream_listeners],
      opts
    )
  end

  def streamify(
        program,
        status_message_provider,
        stream_listeners,
        include_final_prediction_in_output_stream
      ) do
    SnakeBridge.Runtime.call(
      __MODULE__,
      :streamify,
      [
        program,
        status_message_provider,
        stream_listeners,
        include_final_prediction_in_output_stream
      ],
      []
    )
  end

  def streamify(
        program,
        status_message_provider,
        stream_listeners,
        include_final_prediction_in_output_stream,
        opts
      )
      when is_list(opts) and
             (opts == [] or
                (is_tuple(hd(opts)) and tuple_size(hd(opts)) == 2 and is_atom(elem(hd(opts), 0)))) do
    SnakeBridge.Runtime.call(
      __MODULE__,
      :streamify,
      [
        program,
        status_message_provider,
        stream_listeners,
        include_final_prediction_in_output_stream
      ],
      opts
    )
  end

  def streamify(
        program,
        status_message_provider,
        stream_listeners,
        include_final_prediction_in_output_stream,
        is_async_program
      ) do
    SnakeBridge.Runtime.call(
      __MODULE__,
      :streamify,
      [
        program,
        status_message_provider,
        stream_listeners,
        include_final_prediction_in_output_stream,
        is_async_program
      ],
      []
    )
  end

  def streamify(
        program,
        status_message_provider,
        stream_listeners,
        include_final_prediction_in_output_stream,
        is_async_program,
        opts
      )
      when is_list(opts) and
             (opts == [] or
                (is_tuple(hd(opts)) and tuple_size(hd(opts)) == 2 and is_atom(elem(hd(opts), 0)))) do
    SnakeBridge.Runtime.call(
      __MODULE__,
      :streamify,
      [
        program,
        status_message_provider,
        stream_listeners,
        include_final_prediction_in_output_stream,
        is_async_program
      ],
      opts
    )
  end

  def streamify(
        program,
        status_message_provider,
        stream_listeners,
        include_final_prediction_in_output_stream,
        is_async_program,
        async_streaming
      ) do
    SnakeBridge.Runtime.call(
      __MODULE__,
      :streamify,
      [
        program,
        status_message_provider,
        stream_listeners,
        include_final_prediction_in_output_stream,
        is_async_program,
        async_streaming
      ],
      []
    )
  end

  def streamify(
        program,
        status_message_provider,
        stream_listeners,
        include_final_prediction_in_output_stream,
        is_async_program,
        async_streaming,
        opts
      )
      when is_list(opts) and
             (opts == [] or
                (is_tuple(hd(opts)) and tuple_size(hd(opts)) == 2 and is_atom(elem(hd(opts), 0)))) do
    SnakeBridge.Runtime.call(
      __MODULE__,
      :streamify,
      [
        program,
        status_message_provider,
        stream_listeners,
        include_final_prediction_in_output_stream,
        is_async_program,
        async_streaming
      ],
      opts
    )
  end

  @doc """
  Convert an async DSPy module to a sync program.

  There are two modes of this function:

  - `in_place=True` (recommended): Modify the module in place. But this may not work if you already have a `forward`
    method which does different things from `aforward`.
  - `in_place=False`: Return a wrapper module. This changes the module's architecture, but it's more robust.

  ## Parameters

  - `program` - The async program to convert, must have an `aforward` method implemented.
  - `in_place` - If True, modify the module in place. Otherwise, return a wrapper module.

  ## Returns

  - `term()`
  """
  @spec syncify(term()) :: {:ok, term()} | {:error, Snakepit.Error.t()}
  @spec syncify(term(), keyword()) :: {:ok, term()} | {:error, Snakepit.Error.t()}
  @spec syncify(term(), boolean()) :: {:ok, term()} | {:error, Snakepit.Error.t()}
  @spec syncify(term(), boolean(), keyword()) :: {:ok, term()} | {:error, Snakepit.Error.t()}
  def syncify(program) do
    SnakeBridge.Runtime.call(__MODULE__, :syncify, [program], [])
  end

  def syncify(program, opts)
      when is_list(opts) and
             (opts == [] or
                (is_tuple(hd(opts)) and tuple_size(hd(opts)) == 2 and is_atom(elem(hd(opts), 0)))) do
    SnakeBridge.Runtime.call(__MODULE__, :syncify, [program], opts)
  end

  def syncify(program, in_place) do
    SnakeBridge.Runtime.call(__MODULE__, :syncify, [program, in_place], [])
  end

  def syncify(program, in_place, opts)
      when is_list(opts) and
             (opts == [] or
                (is_tuple(hd(opts)) and tuple_size(hd(opts)) == 2 and is_atom(elem(hd(opts), 0)))) do
    SnakeBridge.Runtime.call(__MODULE__, :syncify, [program, in_place], opts)
  end

  @doc """
  Context manager for tracking LM usage.

  When nested, the child will roll-up recorded usage into the parent upon exit.
  Note that usage added after the scope exit will not be recorded in the parent.

  ## Returns

  - `term()`
  """
  @spec track_usage(keyword()) :: {:ok, term()} | {:error, Snakepit.Error.t()}
  def track_usage(opts \\ []) do
    SnakeBridge.Runtime.call(__MODULE__, :track_usage, [], opts)
  end

  @doc false
  def __functions__ do
    [
      {:apply_sync_streaming, 1, __MODULE__,
       "Convert the async streaming generator to a sync generator."},
      {:enable_logging, 0, __MODULE__,
       "Enables the `DSPyLoggingStream` used by event logging APIs throughout DSPy"},
      {:infer_prefix, 1, __MODULE__,
       "Infer a prefix from an attribute name by converting it to a human-readable format."},
      {:input_field, 0, __MODULE__, ""},
      {:disable_logging, 0, __MODULE__,
       "Disables the `DSPyLoggingStream` used by event logging APIs throughout DSPy"},
      {:track_usage, 0, __MODULE__, "Context manager for tracking LM usage."},
      {:experimental, 0, __MODULE__,
       "Decorator / decorator creator for marking APIs experimental in the docstring."},
      {:streamify, 1, __MODULE__,
       "Wrap a DSPy program so that it streams its outputs incrementally, rather than returning them"},
      {:stream_event_type, 0, __MODULE__, "Use the lm15 types and providers bundled with DSPy."},
      {:configure_cache, 0, __MODULE__, "Configure the cache for DSPy."},
      {:data, 1, __MODULE__, "Create a data part (structured input, or a judged answer)."},
      {:role_values, 0, __MODULE__, ""},
      {:em, 2, __MODULE__,
       "Compute the Exact Match (EM) metric between a prediction and reference answers."},
      {:answer_passage_match, 2, __MODULE__,
       "Return True if any passage in `pred.context` contains the answer(s)."},
      {:materialize_response, 2, __MODULE__,
       "Consume a complete stream, requiring a final end event and closing its source."},
      {:streaming_response, 1, __MODULE__,
       "Convert a DSPy program output stream to an OpenAI-compatible output stream that can be"},
      {:context, 0, __MODULE__, "Override DSPy settings for one `with` block."},
      {:input_field, 0, __MODULE__, ""},
      {:asyncify, 1, __MODULE__,
       "Wraps a DSPy program so that it can be called asynchronously. This is useful for running a"},
      {:configure, 0, __MODULE__,
       "Set the default language model, adapter, and other settings for DSPy."},
      {:configure_dspy_loggers, 1, __MODULE__, ""},
      {:settings, 0, __MODULE__, ""},
      {:make_signature, 1, __MODULE__,
       "Create a new Signature subclass with the specified fields and instructions."},
      {:streamify, 1, __MODULE__,
       "Wrap a DSPy program so that it streams its outputs incrementally, rather than returning them"},
      {:with_callbacks, 1, __MODULE__,
       "Decorator to add callback functionality to instance methods."},
      {:disable_litellm_logging, 0, __MODULE__, ""},
      {:finish_reasons, 0, __MODULE__, ""},
      {:resolve_interpreter_factory, 0, __MODULE__,
       "Return the factory that creates the next interpreter."},
      {:response_to_events, 1, __MODULE__, "Convert a complete Response to stream events."},
      {:make_signature, 1, __MODULE__,
       "Create a new Signature subclass with the specified fields and instructions."},
      {:choice, 2, __MODULE__,
       "A choice judgment property: ``{key: description-or-None}`` or a list of keys."},
      {:judgments, 0, __MODULE__,
       "A ``response_format`` declaring the given judgment properties."},
      {:enable_litellm_logging, 0, __MODULE__, ""},
      {:register_provider, 1, __MODULE__,
       "Declare a provider for the native ``dspy.LM``s constructed after this call."},
      {:request_from_openai_chat, 1, __MODULE__,
       "A Chat Completions request body → the canonical :class:`Request` (MAP-12)."},
      {:pretty_print_history, 1, __MODULE__, "Print the last n prompts and their completions."},
      {:infer_prefix, 1, __MODULE__,
       "Infer a prefix from an attribute name by converting it to a human-readable format."},
      {:disable_litellm_logging, 0, __MODULE__, ""},
      {:syncify, 1, __MODULE__, "Convert an async DSPy module to a sync program."},
      {:reasoning_effort, 0, __MODULE__, "Use the lm15 types and providers bundled with DSPy."},
      {:ensure_signature, 1, __MODULE__, ""},
      {:role, 0, __MODULE__, "Use the lm15 types and providers bundled with DSPy."},
      {:score, 2, __MODULE__,
       "An ordered judgment: levels low → high; ``{name: description}`` or descriptions."},
      {:registered_providers, 0, __MODULE__,
       "The providers registered in this process, in registration order —"},
      {:tool_result, 2, __MODULE__, "Create a tool result part."},
      {:dummy_rm, 0, __MODULE__, ""},
      {:majority, 1, __MODULE__,
       "Returns the most common completion for the target field (or the last field) in the signature."},
      {:ensure_signature, 1, __MODULE__, ""},
      {:unregister_provider, 1, __MODULE__,
       "Forget a registered provider. Unknown ids are ignored. LMs that"},
      {:output_field, 0, __MODULE__, ""},
      {:finish_reason, 0, __MODULE__, "Use the lm15 types and providers bundled with DSPy."},
      {:bootstrap_trace_data, 2, __MODULE__, ""},
      {:majority, 1, __MODULE__,
       "Returns the most common completion for the target field (or the last field) in the signature."},
      {:yes_no, 1, __MODULE__, "Use the lm15 types and providers bundled with DSPy."},
      {:retryable_errors, 0, __MODULE__, ""},
      {:amaterialize_response, 2, __MODULE__, "Async mirror of :func:`materialize_response`."},
      {:is_retryable_lm_error, 1, __MODULE__,
       "Return whether an LM error is generally safe to retry."},
      {:build_repl_variable, 3, __MODULE__,
       "Build a :class:`REPLVariable` for a SandboxSerializable value."},
      {:inspect_history, 0, __MODULE__, "The global history shared across all LMs."},
      {:inspect_history, 0, __MODULE__, "The global history shared across all LMs."},
      {:tool, 1, __MODULE__,
       "Derive a FunctionTool from *fn*. Pure; returns a plain FunctionTool."},
      {:output_field, 0, __MODULE__, ""},
      {:load_settings, 1, __MODULE__, "Load the settings from a file using cloudpickle."},
      {:normalize_text, 1, __MODULE__, "Normalize text for string and token comparisons."},
      {:part_type, 0, __MODULE__, "Use the lm15 types and providers bundled with DSPy."},
      {:adaptation_policy, 0, __MODULE__, "Use the lm15 types and providers bundled with DSPy."},
      {:bootstrap_trace_data, 2, __MODULE__, ""},
      {:response_from_openai_chat, 1, __MODULE__,
       "A Chat Completions response body → the canonical :class:`Response`."},
      {:load, 1, __MODULE__, "Load saved DSPy model."},
      {:answer_exact_match, 2, __MODULE__,
       "Evaluate exact match or F1-thresholded match for an example/prediction pair."},
      {:error_code, 0, __MODULE__, "Use the lm15 types and providers bundled with DSPy."},
      {:error_codes, 0, __MODULE__, ""},
      {:download, 1, __MODULE__, ""},
      {:enable_litellm_logging, 0, __MODULE__, ""},
      {:derive_tool, 1, __MODULE__,
       "Derive a FunctionTool from *fn* with a full, typed account."},
      {:resolve_interpreter_factory, 0, __MODULE__,
       "Return the factory that creates the next interpreter."},
      {:configure_cache, 0, __MODULE__, "Configure the cache for DSPy."}
    ]
  end

  @doc false
  def __classes__ do
    [
      {Dspy.Lm15.BuiltinTool, "A provider-native tool (web search, code execution, etc.)."},
      {Dspy.LMConfigurationError, "The LM or provider client is not configured correctly."},
      {Dspy.XMLAdapter,
       "Default Adapter for most language models.\n\nThe ChatAdapter formats DSPy signatures into a format compatible with most language models.\nIt uses delimiter patterns like `[[ ## field_name ## ]]` to clearly separate input and output fields in\nthe message content.\n\nKey features:\n    - Structures inputs and outputs using field header markers for clear field delineation.\n    - Provides automatic fallback to JSONAdapter if the chat format fails."},
      {Dspy.Lm15.UnsupportedFeatureError, "Feature not supported by this provider adapter."},
      {Dspy.Lm15.TextPart, "A block of text content."},
      {Dspy.Module,
       "Base class for all DSPy modules (programs).\n\nA Module is a building block for DSPy programs that can contain predictors,\nsub-modules, and custom logic. Modules can be composed together to create\ncomplex pipelines and can be optimized using DSPy's teleprompters.\n\nAll DSPy programs should inherit from this class and implement a ``forward``\nmethod that defines the program's logic.\n\nArgs:\n    callbacks: Optional list of callback handlers for instrumentation\n        and monitoring.\n\nAttributes:\n    callbacks: List of registered callback handlers.\n    history: List of LM call history for this module.\n\nExamples:\n    >>> import dspy\n    >>> class MyProgram(dspy.Module):\n    ...     def __init__(self):\n    ...         super().__init__()\n    ...         self.predictor = dspy.Predict(\"question -> answer\")\n    ...\n    ...     def forward(self, question):\n    ...         return self.predictor(question=question)"},
      {Dspy.KNNFewShot, ""},
      {Dspy.Primitives.BaseModule, ""},
      {Dspy.Lm15.LockTimeoutError,
       "The credential-file lock (spec/auth.md AUTH-4) could not be taken\nwithin the timeout: another lm15 process is refreshing the same\ncredential (``lock_timeout``, 2026-09-08).\n\nLocal and transient — a root-level class beside :class:`TransportError`,\nnever a :class:`ProviderError` (no provider was asked) and never an\n:class:`AuthError` (nothing is wrong with the credential; AUTH-6).\nRetryable. Carries ``path`` (the guarded file) and ``lock_path``.\n:class:`lm15.auth.CredentialLockTimeout` is this class and the builtin\n``TimeoutError`` at once, so ``except TimeoutError`` keeps working."},
      {Dspy.BestOfN,
       "Runs a module up to N times and returns the highest-scoring prediction.\n\nAt each attempt, the module is called with a different rollout ID at\n``temperature=1.0`` to encourage output diversity. The attempt with the\nhighest reward is returned, or the first attempt whose reward meets or\nexceeds ``threshold`` (whichever comes first).\n\nArgs:\n    module: The DSPy module to run repeatedly.\n    N: Maximum number of attempts.\n    reward_fn: A callable that takes the input kwargs dict and a\n        ``Prediction``, and returns a scalar float reward score.\n    threshold: If an attempt's reward is at or above this value, that\n        prediction is returned immediately without further attempts.\n    fail_count: Number of allowed failures before raising an exception.\n        Defaults to ``N`` if not provided.\n\nExample:\n    >>> import dspy\n    >>> dspy.configure(lm=dspy.LM(\"openai/gpt-4o-mini\"))\n    >>> qa = dspy.ChainOfThought(\"question -> answer\")\n    >>> def one_word_answer(args, pred):\n    ...     return 1.0 if len(pred.answer.split()) == 1 else 0.0\n    >>> best_of_3 = dspy.BestOfN(module=qa, N=3, reward_fn=one_word_answer, threshold=1.0)\n    >>> result = best_of_3(question=\"What is the capital of Belgium?\")\n    >>> print(result.answer)  # Brussels"},
      {Dspy.Teleprompt.GEPAClass,
       "Experimental: This class may change or be removed in a future release without warning (introduced in v3.0.0).\n\n\nGEPA is an evolutionary optimizer, which uses reflection to evolve text components\nof complex systems. GEPA is proposed in the paper [GEPA: Reflective Prompt Evolution Can Outperform Reinforcement Learning](https://arxiv.org/abs/2507.19457).\nThe GEPA optimization engine is provided by the `gepa` package, available from [https://github.com/gepa-ai/gepa](https://github.com/gepa-ai/gepa).\n\nGEPA captures full traces of the DSPy module's execution, identifies the parts of the trace\ncorresponding to a specific predictor, and reflects on the behaviour of the predictor to\npropose a new instruction for the predictor. GEPA allows users to provide textual feedback\nto the optimizer, which is used to guide the evolution of the predictor. The textual feedback\ncan be provided at the granularity of individual predictors, or at the level of the entire system's\nexecution.\n\nTo provide feedback to the GEPA optimizer, implement a metric as follows:\n```\ndef metric(\n    gold: Example,\n    pred: Prediction,\n    trace: Optional[DSPyTrace] = None,\n    pred_name: Optional[str] = None,\n    pred_trace: Optional[DSPyTrace] = None,\n    program_trace: Optional[DSPyTrace] = None,\n) -> float | ScoreWithFeedback:\n    \"\"\"\n    This function is called with the following arguments:\n    - gold: The gold example.\n    - pred: The predicted output.\n    - trace: Optional. The trace of the program's execution.\n    - pred_name: Optional. The name of the target predictor currently being optimized by GEPA, for which\n        the feedback is being requested.\n    - pred_trace: Optional. The trace of the target predictor's execution GEPA is seeking feedback for.\n    - program_trace: Optional. The program's execution trace, supplied at scoring time when a `dspy.Flex`\n        submodule is being optimized. Declare it to score against how the answer was produced (e.g. an\n        LM-call penalty). Defaults to None.\n\n    Note the `pred_name` and `pred_trace` arguments. During optimization, GEPA will call the metric to obtain\n    feedback for individual predictors being optimized. GEPA provides the name of the predictor in `pred_name`\n    and the sub-trace (of the trace) corresponding to the predictor in `pred_trace`.\n    If available at the predictor level, the metric should return {'score': float, 'feedback': str} corresponding\n    to the predictor.\n    If not available at the predictor level, the metric can also return a text feedback at the program level\n    (using just the gold, pred and trace).\n    If no feedback is returned, GEPA will use a simple text feedback consisting of just the score:\n    f\"This trajectory got a score of {score}.\"\n    \"\"\"\n    ...\n```\n\nGEPA can also be used as a batch inference-time search strategy, by passing `valset=trainset, track_stats=True, track_best_outputs=True`, and using the\n`detailed_results` attribute of the optimized program (returned by `compile`) to get the Pareto frontier of the batch. `optimized_program.detailed_results.best_outputs_valset` will contain the best outputs for each task in the batch.\n\nExamples:\n```\ngepa = GEPA(metric=metric, track_stats=True)\nbatch_of_tasks = [dspy.Example(...) for task in tasks]\nnew_prog = gepa.compile(student, trainset=trainset, valset=batch_of_tasks)\npareto_frontier = new_prog.detailed_results.val_aggregate_scores\n# pareto_frontier is a list of scores, one for each task in the batch.\n```\n\nArgs:\n    metric: The metric function to use for feedback and evaluation.\n    auto: The auto budget to use for the run. Options: \"light\", \"medium\", \"heavy\".\n    max_full_evals: The maximum number of full evaluations to perform.\n    max_metric_calls: The maximum number of metric calls to perform.\n    reflection_minibatch_size: The number of examples to use for reflection in a single GEPA step. Default is 3.\n    candidate_selection_strategy: The strategy to use for candidate selection. Default is \"pareto\",\n        which stochastically selects candidates from the Pareto frontier of all validation scores.\n        Options: \"pareto\", \"current_best\".\n    reflection_lm: The language model to use for reflection. Required parameter. GEPA benefits from\n        a strong reflection model. Consider using `dspy.LM(model='gpt-5', temperature=1.0, max_tokens=32000)`\n        for optimal performance.\n    skip_perfect_score: Whether to skip examples with perfect scores during reflection. Default is True.\n    instruction_proposer: Optional custom instruction proposer implementing GEPA's ProposalFn protocol.\n        **Default: None (recommended for most users)** - Uses GEPA's proven instruction proposer from\n        the [GEPA library](https://github.com/gepa-ai/gepa), which implements the\n        [`ProposalFn`](https://github.com/gepa-ai/gepa/blob/main/src/gepa/core/adapter.py). This default\n        proposer is highly capable and was validated across diverse experiments reported in the GEPA\n        paper and tutorials.\n\n        See documentation on custom instruction proposers\n        [here](https://dspy.ai/api/optimizers/GEPA/GEPA_Advanced/#custom-instruction-proposers).\n\n        **Advanced Feature**: Only needed for specialized scenarios:\n        - **Multi-modal handling**: Processing dspy.Image inputs alongside textual information\n        - **Nuanced control over constraints**: Fine-grained control over instruction length, format,\n          and structural requirements beyond standard feedback mechanisms\n        - **Domain-specific knowledge injection**: Specialized terminology or context that cannot be\n          provided through feedback_func alone\n        - **Provider-specific prompting**: Optimizations for specific LLM providers (OpenAI, Anthropic)\n          with unique formatting preferences\n        - **Coupled component updates**: Coordinated updates of multiple components together rather\n          than independent optimization\n        - **External knowledge integration**: Runtime access to databases, APIs, or knowledge bases\n\n        The default proposer handles the vast majority of use cases effectively. Use\n        MultiModalInstructionProposer() from dspy.teleprompt.gepa.instruction_proposal for visual\n        content or implement custom ProposalFn for highly specialized requirements.\n\n        Note: When both instruction_proposer and reflection_lm are set, the instruction_proposer is called\n        in the reflection_lm context. However, reflection_lm is optional when using a custom instruction_proposer.\n        Custom instruction proposers can invoke their own LLMs if needed.\n    code_proposer: Optional custom proposer for `dspy.Flex` code components, implementing the\n        `CodeProposalFn` protocol from `dspy.teleprompt.gepa.gepa_utils`.\n        **Default: None** - Uses the built-in code proposer, which rewrites each Flex\n        submodule's full `module_src` from the failing examples and feedback.\n\n        A custom code proposer is called once per reflection round with the code components to\n        update, the current candidate, the reflective dataset, and per-component task\n        descriptions (the rendered Flex signature) and context blurbs (available tools and\n        style notes). It must return a complete replacement `dspy.Module` subclass source per\n        component. It is invoked inside the reflection_lm context, so predictors it creates\n        use the reflection LM unless it selects a different one with `dspy.context(lm=...)`.\n        Unlike instruction_proposer, it does not satisfy the requirement for a reflection\n        provider: GEPA still requires reflection_lm or instruction_proposer to be set.\n\n        Only Flex components are routed to it; regular predictors keep going to\n        instruction_proposer (or the default instruction proposer). This parameter has no\n        effect on programs without `dspy.Flex` submodules.\n    component_selector: Custom component selector implementing the [ReflectionComponentSelector](https:/"},
      {Dspy.Code,
       "Code type in DSPy.\n\nThis type is useful for code generation and code analysis.\n\nExample 1: dspy.Code as output type in code generation:\n\n```python\nimport dspy\n\ndspy.configure(lm=dspy.LM(\"openai/gpt-4o-mini\"))\n\n\nclass CodeGeneration(dspy.Signature):\n    '''Generate python code to answer the question.'''\n\n    question: str = dspy.InputField(description=\"The question to answer\")\n    code: dspy.Code[\"java\"] = dspy.OutputField(description=\"The code to execute\")\n\n\npredict = dspy.Predict(CodeGeneration)\n\nresult = predict(question=\"Given an array, find if any of the two numbers sum up to 10\")\nprint(result.code)\n```\n\nExample 2: dspy.Code as input type in code analysis:\n\n```python\nimport dspy\nimport inspect\n\ndspy.configure(lm=dspy.LM(\"openai/gpt-4o-mini\"))\n\nclass CodeAnalysis(dspy.Signature):\n    '''Analyze the time complexity of the function.'''\n\n    code: dspy.Code[\"python\"] = dspy.InputField(description=\"The function to analyze\")\n    result: str = dspy.OutputField(description=\"The time complexity of the function\")\n\n\npredict = dspy.Predict(CodeAnalysis)\n\n\ndef sleepsort(x):\n    import time\n\n    for i in x:\n        time.sleep(i)\n        print(i)\n\nresult = predict(code=inspect.getsource(sleepsort))\nprint(result.result)\n```"},
      {Dspy.Type,
       "Base class to support creating custom types for DSPy signatures.\n\nThis is the parent class of DSPy custom types, e.g, dspy.Image. Subclasses must implement the `format` method to\nreturn a list of dictionaries (same as the Array of content parts in the OpenAI API user message's content field).\n\nExamples:\n\n    ```python\n    class Image(Type):\n        url: str\n\n        def format(self) -> list[dict[str, Any]]:\n            return [{\"type\": \"image_url\", \"image_url\": {\"url\": self.url}}]\n    ```"},
      {Dspy.Signatures.OldOutputField,
       "A more ergonomic datatype that infers prefix and desc if omitted."},
      {Dspy.Teleprompt.LabeledFewShot, ""},
      {Dspy.Lm15.ToolCallPart, "The model requests an external computation."},
      {Dspy.Datasets.DataLoader, ""},
      {Dspy.Adapters.Types.Audio,
       "An audio input type for DSPy.\n\nConstruct from in-memory values only: a data URI string, raw ``bytes`` (with\n``audio_format=``), a numpy-like array, a ``{\"data\", \"audio_format\"}`` dict, or another\n``Audio``. Raw base64 is passed as ``Audio(data=..., audio_format=...)``. Construction and\nadapter parsing never access the filesystem or network; use :meth:`from_path` to read a local\nfile, :meth:`from_url` to download a remote resource, or :meth:`from_array` for array data."},
      {Dspy.Lm15.OpenAICodexLM,
       "OpenAI Responses adapter authenticated with local Codex CLI OAuth."},
      {Dspy.Lm15.TimeoutError,
       "Provider request timed out.\n\nAlso subclasses the builtin ``TimeoutError`` so a user's bare\n``except TimeoutError:`` catches lm15 timeouts. ``ProviderError`` comes\nfirst in the MRO, so lm15 metadata (``code``, ``status``, ...) wins."},
      {Dspy.MIPROv2, ""},
      {Dspy.Lm15.AsyncLMRouter,
       "Async mirror of :class:`LMRouter`.\n\nSame four methods; ``lm()`` returns Async* mirrors; ``complete`` is\na coroutine and ``stream`` returns an async iterator.  ``resolve()``\nstays sync (it is pure)."},
      {Dspy.Embeddings,
       "DSPy Embeddings retriever.\n\nThis class retrieves the top-k most similar passages from a corpus using embedding-based similarity search.\nFor large corpora, a FAISS index is built for fast approximate candidate retrieval, followed by exact\nre-ranking. For small corpora, brute-force search is used."},
      {Dspy.GEPA,
       "Experimental: This class may change or be removed in a future release without warning (introduced in v3.0.0).\n\n\nGEPA is an evolutionary optimizer, which uses reflection to evolve text components\nof complex systems. GEPA is proposed in the paper [GEPA: Reflective Prompt Evolution Can Outperform Reinforcement Learning](https://arxiv.org/abs/2507.19457).\nThe GEPA optimization engine is provided by the `gepa` package, available from [https://github.com/gepa-ai/gepa](https://github.com/gepa-ai/gepa).\n\nGEPA captures full traces of the DSPy module's execution, identifies the parts of the trace\ncorresponding to a specific predictor, and reflects on the behaviour of the predictor to\npropose a new instruction for the predictor. GEPA allows users to provide textual feedback\nto the optimizer, which is used to guide the evolution of the predictor. The textual feedback\ncan be provided at the granularity of individual predictors, or at the level of the entire system's\nexecution.\n\nTo provide feedback to the GEPA optimizer, implement a metric as follows:\n```\ndef metric(\n    gold: Example,\n    pred: Prediction,\n    trace: Optional[DSPyTrace] = None,\n    pred_name: Optional[str] = None,\n    pred_trace: Optional[DSPyTrace] = None,\n    program_trace: Optional[DSPyTrace] = None,\n) -> float | ScoreWithFeedback:\n    \"\"\"\n    This function is called with the following arguments:\n    - gold: The gold example.\n    - pred: The predicted output.\n    - trace: Optional. The trace of the program's execution.\n    - pred_name: Optional. The name of the target predictor currently being optimized by GEPA, for which\n        the feedback is being requested.\n    - pred_trace: Optional. The trace of the target predictor's execution GEPA is seeking feedback for.\n    - program_trace: Optional. The program's execution trace, supplied at scoring time when a `dspy.Flex`\n        submodule is being optimized. Declare it to score against how the answer was produced (e.g. an\n        LM-call penalty). Defaults to None.\n\n    Note the `pred_name` and `pred_trace` arguments. During optimization, GEPA will call the metric to obtain\n    feedback for individual predictors being optimized. GEPA provides the name of the predictor in `pred_name`\n    and the sub-trace (of the trace) corresponding to the predictor in `pred_trace`.\n    If available at the predictor level, the metric should return {'score': float, 'feedback': str} corresponding\n    to the predictor.\n    If not available at the predictor level, the metric can also return a text feedback at the program level\n    (using just the gold, pred and trace).\n    If no feedback is returned, GEPA will use a simple text feedback consisting of just the score:\n    f\"This trajectory got a score of {score}.\"\n    \"\"\"\n    ...\n```\n\nGEPA can also be used as a batch inference-time search strategy, by passing `valset=trainset, track_stats=True, track_best_outputs=True`, and using the\n`detailed_results` attribute of the optimized program (returned by `compile`) to get the Pareto frontier of the batch. `optimized_program.detailed_results.best_outputs_valset` will contain the best outputs for each task in the batch.\n\nExamples:\n```\ngepa = GEPA(metric=metric, track_stats=True)\nbatch_of_tasks = [dspy.Example(...) for task in tasks]\nnew_prog = gepa.compile(student, trainset=trainset, valset=batch_of_tasks)\npareto_frontier = new_prog.detailed_results.val_aggregate_scores\n# pareto_frontier is a list of scores, one for each task in the batch.\n```\n\nArgs:\n    metric: The metric function to use for feedback and evaluation.\n    auto: The auto budget to use for the run. Options: \"light\", \"medium\", \"heavy\".\n    max_full_evals: The maximum number of full evaluations to perform.\n    max_metric_calls: The maximum number of metric calls to perform.\n    reflection_minibatch_size: The number of examples to use for reflection in a single GEPA step. Default is 3.\n    candidate_selection_strategy: The strategy to use for candidate selection. Default is \"pareto\",\n        which stochastically selects candidates from the Pareto frontier of all validation scores.\n        Options: \"pareto\", \"current_best\".\n    reflection_lm: The language model to use for reflection. Required parameter. GEPA benefits from\n        a strong reflection model. Consider using `dspy.LM(model='gpt-5', temperature=1.0, max_tokens=32000)`\n        for optimal performance.\n    skip_perfect_score: Whether to skip examples with perfect scores during reflection. Default is True.\n    instruction_proposer: Optional custom instruction proposer implementing GEPA's ProposalFn protocol.\n        **Default: None (recommended for most users)** - Uses GEPA's proven instruction proposer from\n        the [GEPA library](https://github.com/gepa-ai/gepa), which implements the\n        [`ProposalFn`](https://github.com/gepa-ai/gepa/blob/main/src/gepa/core/adapter.py). This default\n        proposer is highly capable and was validated across diverse experiments reported in the GEPA\n        paper and tutorials.\n\n        See documentation on custom instruction proposers\n        [here](https://dspy.ai/api/optimizers/GEPA/GEPA_Advanced/#custom-instruction-proposers).\n\n        **Advanced Feature**: Only needed for specialized scenarios:\n        - **Multi-modal handling**: Processing dspy.Image inputs alongside textual information\n        - **Nuanced control over constraints**: Fine-grained control over instruction length, format,\n          and structural requirements beyond standard feedback mechanisms\n        - **Domain-specific knowledge injection**: Specialized terminology or context that cannot be\n          provided through feedback_func alone\n        - **Provider-specific prompting**: Optimizations for specific LLM providers (OpenAI, Anthropic)\n          with unique formatting preferences\n        - **Coupled component updates**: Coordinated updates of multiple components together rather\n          than independent optimization\n        - **External knowledge integration**: Runtime access to databases, APIs, or knowledge bases\n\n        The default proposer handles the vast majority of use cases effectively. Use\n        MultiModalInstructionProposer() from dspy.teleprompt.gepa.instruction_proposal for visual\n        content or implement custom ProposalFn for highly specialized requirements.\n\n        Note: When both instruction_proposer and reflection_lm are set, the instruction_proposer is called\n        in the reflection_lm context. However, reflection_lm is optional when using a custom instruction_proposer.\n        Custom instruction proposers can invoke their own LLMs if needed.\n    code_proposer: Optional custom proposer for `dspy.Flex` code components, implementing the\n        `CodeProposalFn` protocol from `dspy.teleprompt.gepa.gepa_utils`.\n        **Default: None** - Uses the built-in code proposer, which rewrites each Flex\n        submodule's full `module_src` from the failing examples and feedback.\n\n        A custom code proposer is called once per reflection round with the code components to\n        update, the current candidate, the reflective dataset, and per-component task\n        descriptions (the rendered Flex signature) and context blurbs (available tools and\n        style notes). It must return a complete replacement `dspy.Module` subclass source per\n        component. It is invoked inside the reflection_lm context, so predictors it creates\n        use the reflection LM unless it selects a different one with `dspy.context(lm=...)`.\n        Unlike instruction_proposer, it does not satisfy the requirement for a reflection\n        provider: GEPA still requires reflection_lm or instruction_proposer to be set.\n\n        Only Flex components are routed to it; regular predictors keep going to\n        instruction_proposer (or the default instruction proposer). This parameter has no\n        effect on programs without `dspy.Flex` submodules.\n    component_selector: Custom component selector implementing the [ReflectionComponentSelector](https:/"},
      {Dspy.Parallel, ""},
      {Dspy.OldInputField, "A more ergonomic datatype that infers prefix and desc if omitted."},
      {Dspy.SIMBA,
       "SIMBA (Stochastic Introspective Mini-Batch Ascent) optimizer for DSPy.\n\nSIMBA is a DSPy optimizer that uses the LLM to analyze its own performance and \ngenerate improvement rules. It samples mini-batches, identifies challenging examples \nwith high output variability, then either creates self-reflective rules or adds \nsuccessful examples as demonstrations.\n\nFor more details, see: https://dspy.ai/api/optimizers/SIMBA/"},
      {Dspy.TrainingJob,
       "A future representing an asynchronous model fine-tuning job.\n\nThis class extends `concurrent.futures.Future` to represent a fine-tuning job\nthat can be monitored and cancelled. Subclasses should implement the `status`\nmethod to provide job status information specific to their provider.\n\nAttributes:\n    thread: The thread running the training job, if applicable.\n    model: The model identifier being fine-tuned.\n    train_data: The training data used for fine-tuning.\n    train_data_format: The format of the training data.\n    train_kwargs: Additional keyword arguments for training configuration.\n\nExamples:\n    ```python\n    from dspy.clients.provider import TrainingJob\n\n    job = TrainingJob(\n        model=\"gpt-3.5-turbo\",\n        train_data=[{\"messages\": [...]}],\n        train_data_format=TrainDataFormat.CHAT\n    )\n    # Check if job is done\n    if job.done():\n        result = job.result()\n    ```"},
      {Dspy.Lm15.VideoPart, "Video content, addressed by exactly one of data/url/file_id/path."},
      {Dspy.Lm15.AccessPolicy,
       "How an adapter reaches a backend. Pure data; ports copy it as a table.\n\nSee ``lm15.access`` for the table of policies and the rationale\n(spec/auth.md AUTH-9).\n\nThe constructor retains the original positional ``auth_header`` slot.\nOmitted (``None``), it leaves ``auth_scheme`` unchanged; explicitly supplied,\nit replaces the schemes with that single header, including in ``replace``.\n``auth_scheme`` and ``host`` are keyword-only.\n\n``provider``        canonical provider string (errors, routing, doctor).\n``supports``        endpoint surfaces this access path carries; a\n                    dialect that implements a surface still RAISES when\n                    the policy does not carry it (a subscription token\n                    has no files or batch).\n``credential_policy`` spec/auth.md AUTH-1: key | oauth |\n                    oauth-unless-explicit.\n``auth_modes``, ``env_keys``, ``enterprise_variants``: as before\n                    (support-matrix pinned).\n``auth_scheme``     the schemes this door accepts, in preference order\n                    (``bearer``, ``x-api-key``, ``api-key``,\n                    ``query-key``, ``sigv4``); the credential kind\n                    selects one.  ``auth_header`` is the first header\n                    scheme, kept for the dialects that spell it.\n``host``            the cloud door (``HostSpec``), or None for the\n                    dialect's public API.\n``headers``         static headers on every request, in order. A header\n                    the dialect also sets is merged by the dialect's\n                    stated rule (Anthropic joins ``anthropic-beta``).\n``login_hint``      appended to auth errors when the credential is a\n                    local login and there is no env var to set.\n``backend``         dialect-consulted variant name; ``\"api\"`` is the\n                    provider's public API.\n``backend_options`` string knobs the backend variant needs (the Codex\n                    models endpoint wants a ``client_version``).\n``system_prefix``   text the backend requires first in the system\n                    prompt / instructions (Claude Code, Codex).\n``base_url``        this access path's default base URL, when it is\n                    not the dialect's."},
      {Dspy.Teleprompt.SIMBA,
       "SIMBA (Stochastic Introspective Mini-Batch Ascent) optimizer for DSPy.\n\nSIMBA is a DSPy optimizer that uses the LLM to analyze its own performance and \ngenerate improvement rules. It samples mini-batches, identifies challenging examples \nwith high output variability, then either creates self-reflective rules or adds \nsuccessful examples as demonstrations.\n\nFor more details, see: https://dspy.ai/api/optimizers/SIMBA/"},
      {Dspy.Adapters.Code,
       "Code type in DSPy.\n\nThis type is useful for code generation and code analysis.\n\nExample 1: dspy.Code as output type in code generation:\n\n```python\nimport dspy\n\ndspy.configure(lm=dspy.LM(\"openai/gpt-4o-mini\"))\n\n\nclass CodeGeneration(dspy.Signature):\n    '''Generate python code to answer the question.'''\n\n    question: str = dspy.InputField(description=\"The question to answer\")\n    code: dspy.Code[\"java\"] = dspy.OutputField(description=\"The code to execute\")\n\n\npredict = dspy.Predict(CodeGeneration)\n\nresult = predict(question=\"Given an array, find if any of the two numbers sum up to 10\")\nprint(result.code)\n```\n\nExample 2: dspy.Code as input type in code analysis:\n\n```python\nimport dspy\nimport inspect\n\ndspy.configure(lm=dspy.LM(\"openai/gpt-4o-mini\"))\n\nclass CodeAnalysis(dspy.Signature):\n    '''Analyze the time complexity of the function.'''\n\n    code: dspy.Code[\"python\"] = dspy.InputField(description=\"The function to analyze\")\n    result: str = dspy.OutputField(description=\"The time complexity of the function\")\n\n\npredict = dspy.Predict(CodeAnalysis)\n\n\ndef sleepsort(x):\n    import time\n\n    for i in x:\n        time.sleep(i)\n        print(i)\n\nresult = predict(code=inspect.getsource(sleepsort))\nprint(result.result)\n```"},
      {Dspy.Adapters.ToolCallResults, ""},
      {Dspy.Lm15.CollectionLimitError,
       "A local turn collector reached its budget, not a provider failure.\n\nNon-retryable. ``partial_events`` preserves the accepted events without\ncopying their payloads. ``partial`` materializes them on demand as an\nincomplete Turn. A byte-limit failure also exposes ``rejected_event``:\nit was received but not yielded or added to the collection. Process it\nbefore resuming raw session reads if that content is needed."},
      {Dspy.CodeAct,
       ".. deprecated:: 3.4\n    CodeAct is deprecated. RLM is the preferred replacement.\n\nCodeAct is a module that utilizes the Code Interpreter and predefined tools to solve the problem."},
      {Dspy.Primitives.ReplTypes.REPLVariable,
       "Metadata about a variable available in the REPL environment."},
      {Dspy.Lm15.UnsupportedModelError,
       "Model not found, unavailable, or unsupported by the provider."},
      {Dspy.Lm15.ConfigurationError, "Local SDK or provider-adapter configuration failure."},
      {Dspy.BootstrapFewShotWithOptuna, ""},
      {Dspy.Adapters.JSONAdapter,
       "Default Adapter for most language models.\n\nThe ChatAdapter formats DSPy signatures into a format compatible with most language models.\nIt uses delimiter patterns like `[[ ## field_name ## ]]` to clearly separate input and output fields in\nthe message content.\n\nKey features:\n    - Structures inputs and outputs using field header markers for clear field delineation.\n    - Provides automatic fallback to JSONAdapter if the chat format fails."},
      {Dspy.Lm15.ContinuationDelta,
       "Opaque provider continuation state arriving during streaming.\n\npart_index=None attaches the state to the assistant message; otherwise it\nattaches to the completed part with that index."},
      {Dspy.MultiChainComparison,
       "Base class for all DSPy modules (programs).\n\nA Module is a building block for DSPy programs that can contain predictors,\nsub-modules, and custom logic. Modules can be composed together to create\ncomplex pipelines and can be optimized using DSPy's teleprompters.\n\nAll DSPy programs should inherit from this class and implement a ``forward``\nmethod that defines the program's logic.\n\nArgs:\n    callbacks: Optional list of callback handlers for instrumentation\n        and monitoring.\n\nAttributes:\n    callbacks: List of registered callback handlers.\n    history: List of LM call history for this module.\n\nExamples:\n    >>> import dspy\n    >>> class MyProgram(dspy.Module):\n    ...     def __init__(self):\n    ...         super().__init__()\n    ...         self.predictor = dspy.Predict(\"question -> answer\")\n    ...\n    ...     def forward(self, question):\n    ...         return self.predictor(question=question)"},
      {Dspy.Lm15.Adaptation,
       "One thing the wire got that differs from what was asked.\n\n- ``field``: the config path (``config.seed``, ``config.temperature``,\n  ``config.reasoning.summary``, ``tools``).\n- ``action``: ``dropped`` (a hint with no home), ``clamped`` (a dial\n  to its nearest level), ``substituted`` (the closest spelling),\n  ``client_side`` (lm15 does it after the wire), ``satisfied`` (the\n  provider's default already is what was asked), ``defaulted`` (the\n  wire requires a value the caller did not set).\n- ``asked`` / ``applied``: what the caller set / what went to the wire.\n  ``asked`` is absent for ``defaulted``; ``applied`` is absent for\n  ``dropped`` and ``satisfied``.\n- ``reason``: one sentence naming the provider fact."},
      {Dspy.Evaluate.CompleteAndGrounded,
       "Combines answer completeness and groundedness into a single score.\n\nArgs:\n    threshold: Minimum score to accept during optimization. Defaults to 0.66."},
      {Dspy.Experimental.ReAnchor,
       "Experimental: This class may change or be removed in a future release without warning.\n\nCalibrate a program's decisions: fit every threshold, Score cut, and Choice weight against the metric.\n\nThe program can be a single `Predict` or any `dspy.Module` holding predictors with decision\noutputs, and the metric is the only supervision. Each parameter changes how `Predict` reads the\nbackend's probabilities without adding request parameters. Identical requests reuse cached\nanswers, but changed upstream decisions can produce new downstream requests.\n\nReAnchor enables probability-based execution for every compatible output through the predictor's\n`fields`. It keeps each fitted configuration only when it beats the original behavior and passes\na fold check; otherwise it restores the original configuration, including absent field entries.\n\nArgs:\n    metric: Per-example metric to maximize, as in `dspy.Evaluate`. It may return a number, or a\n        `dspy.Prediction` with a `score`.\n    num_threads: Evaluation concurrency, as in `dspy.Evaluate`.\n    log_dir: When set, report.json is written here.\n    require_cache: When True, check caching on statically bound or globally configured clients.\n        Set it to False for runtime client selection or to allow uncached requests.\n\nAfter `compile`, `report` holds the fitted parameters and the metric's mean before and after\ncalibration, on the training set and on the validation set when one is given."},
      {Dspy.ContextWindowExceededError,
       "Raised when the prompt exceeds the model's context window.\n\nCustom engines raise dspy.lm15.ContextLengthError; DSPy projects it to\nthis public type. Legacy 3.4 LM subclasses may still raise this error\ndirectly. Modules such as ReAct catch it to shorten an overlong history;\nit does not trigger a generation retry or adapter-format fallback.\n\nArgs:\n    model: The model identifier that rejected the request.\n    message: Description of the error. Defaults to `\"Context window exceeded\"`.\n    **kwargs: Structured error metadata such as `provider`, `status`, or\n        `request_id`."},
      {Dspy.Lm15.Resolution,
       "The complete answer to \"how did you route this string\".\n\n``resolve()`` returning this IS the explain() method — there is no\nseparate one."},
      {Dspy.BootstrapFinetune, ""},
      {Dspy.Prediction,
       "A prediction object that contains the output of a DSPy module.\n\nPrediction inherits from Example.\n\nTo allow feedback-augmented scores, Prediction supports comparison operations\n(<, >, <=, >=) for Predictions with a `score` field. The comparison operations\ncompare the 'score' values as floats. For equality comparison, Predictions are equal\nif their underlying data stores are equal (inherited from Example).\n\nArithmetic operations (+, /, etc.) are also supported for Predictions with a 'score'\nfield, operating on the score value."},
      {Dspy.Teleprompt.COPRO, ""},
      {Dspy.OldOutputField, "A more ergonomic datatype that infers prefix and desc if omitted."},
      {Dspy.Lm15.AsyncResponseStream,
       "Async mirror of :class:`ResponseStream`, same accumulator engine.\n\n    rs = AsyncResponseStream(lm.stream(request), request)\n    async for text in rs:\n        print(text, end=\"\")\n    response = await rs.response()\n\n``response()`` is a method (it may need to consume the stream, which\nis an awaitable operation in async code)."},
      {Dspy.Adapters.Types.ToolCallResults, ""},
      {Dspy.Experimental.Noul,
       "Experimental: This class may change or be removed in a future release without warning.\n\nA Boolean value with confidence and optional provider true-probability.\n\nOptionally declare ``Noul[(True, \"blocked\"), (False, \"usable\")]`` with one\nor both outcome descriptions. ``Annotated[bool, Noul[...]]`` retains these\ncriteria while returning a native bool. Thresholds belong to Predict,\nnot the type, and apply with both generative and decision backends.\n\nPredict obtains true-probability from the backend and derives value using\nits per-field threshold (default 0.5). Confidence is\n``abs(p - threshold) / max(threshold, 1 - threshold)``:\na distance from the decision boundary, not a calibrated probability."},
      {Dspy.Teleprompt.BootstrapFewShotWithRandomSearch, ""},
      {Dspy.LMLockTimeoutError,
       "Local credential-lock contention, not a provider or authentication failure."},
      {Dspy.Lm15.FileUploadRequest,
       "A file upload request.\n\nFiles are an ACCOUNT-scoped resource on every provider, so there is\nno ``model`` field.  Uploads can be backed by in-memory bytes or by a\nlocal path.  Path-backed uploads are lazy: LMs can stream from disk\ninstead of forcing the whole file into memory at construction time,\nand they are runtime-only — the canonical serializer rejects them\nbecause a local path is meaningless off this machine."},
      {Dspy.Lm15.ToolDerivationError,
       "Raised when a callable cannot be derived into a FunctionTool.\n\nAlways names the function and, when applicable, the offending parameter\nand the unsupported annotation, plus both escape hatches: a per-parameter\n``ToolConfig(overrides=...)`` entry, or a hand-written FunctionTool."},
      {Dspy.Signatures.Signature, ""},
      {Dspy.Lm15.ToolCallDelta, "A tool-call input fragment, optionally carrying call identity."},
      {Dspy.Flex,
       "Experimental: This class may change or be removed in a future release without warning (introduced in v3.3.0).\n\nA module whose implementation is optimizable code, not just a prompt.\n\nConstruct it like any module (``dspy.Flex(MySignature)``). It starts as a baseline that delegates\nto a single ``dspy.Predict`` over the signature — or ``dspy.RLM`` when ``tools`` are given, so the\nbaseline can call them. ``dspy.GEPA`` recognizes ``Flex`` instances by type and rewrites their\nsource — a single ``dspy.Module`` subclass, exposed as ``module_src`` — into decomposed predictors\nplus plain Python instead of only tuning instructions.\n\nThe optimizer-authored code runs inside an interpreter. ``Flex`` never\nruns it in the host Python process. ``interpreter_factory`` defaults to\n``dspy.PythonInterpreter`` (Deno/Pyodide) and must be a zero-argument callable returning a new\n``CodeInterpreter``. ``dspy.configure(interpreter_factory=...)`` replaces that default.\nFlex may validate or lower source and install its guest shim before execution; a custom\ninterpreter therefore defines the Python and standard-library subset available to that source.\nThe optimizer-authored glue runs isolated; only provided-tool calls, predictor construction,\nand predictor calls bridge back to the host, which makes the real LM calls.\n\nArgs:\n    signature: A ``dspy.Signature`` class or string declaring inputs/outputs.\n    tools: ``dspy.Tool`` instances or named callables.\n    interpreter_factory: Zero-argument callable returning a fresh ``CodeInterpreter`` for each\n        interpreter session. Defaults to ``dspy.PythonInterpreter`` (sandbox, requires Deno).\n        ``dspy.configure(interpreter_factory=...)`` replaces the default.\n    max_predictor_calls: Maximum number of predictor calls the optimizer-authored code can\n        make per ``forward`. ``None`` removes the limit."},
      {Dspy.Lm15.ImageGenerationResponse,
       "Generated images, plus any text the model said while drawing\n(Gemini routinely narrates; dropping it would be silent data loss)."},
      {Dspy.Lm15.ToolCallInfo, "Callback view of a tool call without the part discriminator."},
      {Dspy.Primitives.PythonInterpreter.FinalOutput,
       "Returned by interpreter.execute() when SUBMIT() is called.\n\nThis signals that the code execution loop should terminate and return\nthe contained output to the caller."},
      {Dspy.Lm15.TextDelta,
       "A text fragment arriving during streaming.\n\n``logprobs`` carries the token logprobs for exactly the tokens in this\nfragment, when the request asked for them (``Config.logprobs``) and the\nprovider streams them per chunk (verified live for OpenAI Responses,\n2026-09-01). Materialization concatenates fragment logprobs in arrival\norder into ``Response.logprobs``. ``logprobs_complete=False`` means\nlocal text editing left some retained text without its original score\n(for example, a stop inside a token). Scores always describe original,\nwhole provider tokens. True does not promise the provider supplied any."},
      {Dspy.Lm15.OpenAIChatCompat,
       "Partial compatibility policy for OpenAI Chat Completions-family APIs.\n\nThis class is consumed by OpenAIChatLM (lm15.providers.openai_chat); it is\nkept separate so profiles can describe chat-completions style endpoints\nwithout overloading OpenAIResponsesCompat."},
      {Dspy.Primitives.Prediction,
       "A prediction object that contains the output of a DSPy module.\n\nPrediction inherits from Example.\n\nTo allow feedback-augmented scores, Prediction supports comparison operations\n(<, >, <=, >=) for Predictions with a `score` field. The comparison operations\ncompare the 'score' values as floats. For equality comparison, Predictions are equal\nif their underlying data stores are equal (inherited from Example).\n\nArithmetic operations (+, /, etc.) are also supported for Predictions with a 'score'\nfield, operating on the score value."},
      {Dspy.Teleprompt.BootstrapFinetune, ""},
      {Dspy.LMTimeoutError, "The provider request timed out."},
      {Dspy.Predict.ProgramOfThought,
       ".. deprecated:: 3.4\n    ProgramOfThought is deprecated. RLM is the preferred replacement.\n\nA DSPy module that runs Python programs to solve a problem.\nThis module requires Deno. Install DSPy's managed runtime with ``pip install \"dspy[deno]\"``.\n\nExamples:\n```\nimport dspy\n\nlm = dspy.LM('openai/gpt-4o-mini')\ndspy.configure(lm=lm)\npot = dspy.ProgramOfThought(\"question -> answer\")\npot(question=\"what is 1+1?\")\n```"},
      {Dspy.ProgramOfThought,
       ".. deprecated:: 3.4\n    ProgramOfThought is deprecated. RLM is the preferred replacement.\n\nA DSPy module that runs Python programs to solve a problem.\nThis module requires Deno. Install DSPy's managed runtime with ``pip install \"dspy[deno]\"``.\n\nExamples:\n```\nimport dspy\n\nlm = dspy.LM('openai/gpt-4o-mini')\ndspy.configure(lm=lm)\npot = dspy.ProgramOfThought(\"question -> answer\")\npot(question=\"what is 1+1?\")\n```"},
      {Dspy.Signatures.OldField,
       "A more ergonomic datatype that infers prefix and desc if omitted."},
      {Dspy.Lm15.StreamEndEvent, "The response stream completed."},
      {Dspy.Lm15.LiveConfig,
       "LiveConfig(model: 'str', system: 'str | tuple[PromptPart, ...] | None' = None, tools: 'tuple[Tool, ...]' = (), voice: 'str | None' = None, input_format: 'AudioFormat | None' = None, output_format: 'AudioFormat | None' = None, extensions: 'Extensions | None' = None)"},
      {Dspy.Retrievers.Retrieve, ""},
      {Dspy.Lm15.NotConfiguredError, "No API key or required provider configuration was found."},
      {Dspy.Utils.BaseCallback,
       "A base class for defining callback handlers for DSPy components.\n\nTo use a callback, subclass this class and implement the desired handlers. Each handler\nwill be called at the appropriate time before/after the execution of the corresponding component.  For example, if\nyou want to print a message before and after an LM is called, implement `the on_llm_start` and `on_lm_end` handler.\nUsers can set the callback globally using `dspy.configure` or locally by passing it to the component\nconstructor.\n\n\nExample 1: Set a global callback using `dspy.configure`.\n\n```\nimport dspy\nfrom dspy.utils.callback import BaseCallback\n\nclass LoggingCallback(BaseCallback):\n\n    def on_lm_start(self, call_id, instance, inputs):\n        print(f\"LM is called with inputs: {inputs}\")\n\n    def on_lm_end(self, call_id, outputs, exception):\n        print(f\"LM is finished with outputs: {outputs}\")\n\ndspy.configure(\n    callbacks=[LoggingCallback()]\n)\n\ncot = dspy.ChainOfThought(\"question -> answer\")\ncot(question=\"What is the meaning of life?\")\n\n# > LM is called with inputs: {'question': 'What is the meaning of life?'}\n# > LM is finished with outputs: {'answer': '42'}\n```\n\nExample 2: Set a local callback by passing it to the component constructor.\n\n```\nlm_1 = dspy.LM(\"gpt-3.5-turbo\", callbacks=[LoggingCallback()])\nlm_1(question=\"What is the meaning of life?\")\n\n# > LM is called with inputs: {'question': 'What is the meaning of life?'}\n# > LM is finished with outputs: {'answer': '42'}\n\nlm_2 = dspy.LM(\"gpt-3.5-turbo\")\nlm_2(question=\"What is the meaning of life?\")\n# No logging here because only `lm_1` has the callback set.\n```"},
      {Dspy.Streaming.StreamResponse,
       "StreamResponse(predict_name: str, signature_field_name: str, chunk: str, is_last_chunk: bool)"},
      {Dspy.Teleprompt.Ensemble, ""},
      {Dspy.Lm15.CachePage,
       "One page of stored cache objects plus an opaque continuation cursor."},
      {Dspy.Predict.RLM,
       "Experimental: This class may change or be removed in a future release without warning.\n\nRecursive Language Model module.\n\nUses a sandboxed REPL to let the LLM programmatically explore large contexts\nthrough code execution. The LLM writes Python code to examine data, call\nsub-LLMs for semantic analysis, and build up answers iteratively.\n\n``interpreter_factory`` defaults to ``PythonInterpreter`` (Deno/Pyodide/WASM), and\n``dspy.configure(interpreter_factory=...)`` replaces that default. Either route\naccepts an adapter for a remote sandbox.\nRLM updates the interpreter's mutable ``tools`` dictionary with\ninvocation-scoped tools before execution. Pass a zero-argument factory via\n``interpreter_factory=`` at call time to override the runtime for one invocation.\nRLM shuts down every interpreter it creates.\n\nExamples:\n    ```python\n    # Basic usage\n    rlm = dspy.RLM(\"context, query -> output\", max_iters=10)\n    result = rlm(context=\"...very long text...\", query=\"What is the magic number?\")\n    print(result.output)\n    ```"},
      {Dspy.Lm15.DerivedParam, "Explainable record of how one parameter was derived."},
      {Dspy.Primitives.PythonInterpreter.CodeInterpreterError,
       "Base class for errors reported by a code interpreter.\n\nA bare instance indicates a failure that submitted code cannot repair, such\nas invalid host-side setup or a process/protocol failure. Recoverable\nsubmitted-code failures use :class:`CodeExecutionError`. Implementations\nshould make process/protocol failures terminal for that interpreter session."},
      {Dspy.Lm15.Config,
       "Generation parameters.\n\nUniversal fields are typed.  Provider-specific settings go in\n`extensions` — a clearly-separated namespace that never pretends\nto be part of the universal schema."},
      {Dspy.Primitives.Example,
       "A flexible data container for DSPy examples and training data with named fields.\n\nAn `Example` is roughly one row from a HuggingFace dataset or pandas\n`DataFrame`. It behaves a lot like a dictionary or dot-access record: you\ncan read fields with `example[\"question\"]` or `example.question`.\n\nIn DSPy, lists of `Example` objects are your trainset, devset, and testset.\nMost examples are built from keyword arguments or an existing record, then\ntagged with `with_inputs(...)` to say which fields should be fed into a\nmodule. The remaining fields are labels or metadata.\n\nWhen you write evaluation code, custom optimizers, or training loops, use\n`example.inputs()` for the fields you want to pass to a module, and use\n`example.labels()` for the fields you want to compare against the module's\noutput.\n\nExamples:\n    Build one from keyword arguments:\n\n    >>> import dspy\n    >>> example = dspy.Example(\n    ...     question=\"What is the capital of France?\",\n    ...     answer=\"Paris\",\n    ... ).with_inputs(\"question\")\n    >>> example.question\n    'What is the capital of France?'\n    >>> example.answer\n    'Paris'\n    >>> example.inputs().toDict()\n    {'question': 'What is the capital of France?'}\n\n    Build one from an existing record:\n\n    >>> record = {\"question\": \"What is 2+2?\", \"answer\": \"4\"}\n    >>> example = dspy.Example(**record).with_inputs(\"question\")\n    >>> example[\"question\"]\n    'What is 2+2?'\n    >>> example.labels().answer\n    '4'\n\n    Mark which fields are inputs:\n\n    >>> example = dspy.Example(\n    ...     question=\"What is the weather?\",\n    ...     answer=\"It's sunny\",\n    ... ).with_inputs(\"question\")\n    >>> example.inputs().question\n    'What is the weather?'\n    >>> example.labels().answer\n    \"It's sunny\"\n\n    Use examples in a trainset:\n\n    >>> trainset = [\n    ...     dspy.Example(question=\"What is 2+2?\", answer=\"4\").with_inputs(\"question\"),\n    ...     dspy.Example(question=\"What is 3+3?\", answer=\"6\").with_inputs(\"question\"),\n    ... ]\n    >>> trainset[0].inputs().toDict()\n    {'question': 'What is 2+2?'}\n\n    Use an example in a metric:\n\n    >>> def exact_match_metric(example, pred, trace=None):\n    ...     return example.answer.lower() == pred.answer.lower()\n    >>> gold = dspy.Example(question=\"What is 1+1?\", answer=\"2\").with_inputs(\"question\")\n    >>> pred = dspy.Prediction(answer=\"2\")\n    >>> exact_match_metric(gold, pred)\n    True\n\n    Use it like a dictionary:\n\n    >>> example = dspy.Example(name=\"Alice\", age=30).with_inputs(\"name\")\n    >>> \"name\" in example\n    True\n    >>> example.get(\"city\", \"Unknown\")\n    'Unknown'\n\nSee Also:\n    [`dspy.Evaluate`][dspy.Evaluate]: Evaluate a program on a list of\n        `Example`s.\n    [`Metrics`](../../learn/evaluation/metrics.md): Write metric functions\n        that compare an `Example` with a prediction."},
      {Dspy.Lm15.BillingError, "402 — billing or payment issue."},
      {Dspy.Lm15.TokenLogprob,
       "The chosen token at one decoding step, with ranked alternatives.\n\n``top`` is the provider-reported ranked candidate list (descending\nlogprob).  Providers differ on whether the chosen token appears in\n``top`` — no guarantee either way, and providers also differ on\nwhether the requested alternative count includes the chosen token\n(Gemini documents that it does; OpenAI counts alternatives only).\nlm15 preserves the provider-reported list as-is."},
      {Dspy.Audio,
       "An audio input type for DSPy.\n\nConstruct from in-memory values only: a data URI string, raw ``bytes`` (with\n``audio_format=``), a numpy-like array, a ``{\"data\", \"audio_format\"}`` dict, or another\n``Audio``. Raw base64 is passed as ``Audio(data=..., audio_format=...)``. Construction and\nadapter parsing never access the filesystem or network; use :meth:`from_path` to read a local\nfile, :meth:`from_url` to download a remote resource, or :meth:`from_array` for array data."},
      {Dspy.Primitives.SandboxSerializableClass,
       "Abstract base for types that support RLM sandbox injection.\n\nSubclasses implement four methods:\n\n- ``sandbox_setup``: Python statements (usually imports) executed once\n  in the sandbox. The returned text is also surfaced to the LLM in the\n  variable description, so the model knows which names are in scope\n  (e.g. ``pd`` when pandas is imported).\n- ``to_sandbox``: serialize the value to text bytes or binary bytes.\n- ``sandbox_assignment``: Python code that reconstructs the value from\n  a data expression.\n- ``rlm_preview``: a short, LLM-friendly description of the value.\n\nExample::\n\n    class DataFrame(SandboxSerializable):\n        def __init__(self, df):\n            self.data = df\n\n        def sandbox_setup(self) -> str:\n            return \"import pandas as pd\\nimport base64\\nimport io\"\n\n        def to_sandbox(self) -> bytes:\n            return base64.b64encode(self.data.to_parquet(index=False))\n\n        def sandbox_assignment(self, var_name: str, data_expr: str) -> str:\n            return f\"{var_name} = pd.read_parquet(io.BytesIO(base64.b64decode({data_expr})))\"\n\n        def rlm_preview(self, max_chars: int = 500) -> str:\n            return f\"DataFrame: {self.data.shape[0]} rows x {self.data.shape[1]} columns\"\n\nSubclasses can be used directly as :class:`dspy.Signature` field\nannotations because of the inherited ``__get_pydantic_core_schema__``\nhook (see the module docstring for what that hook does and why it is\nneeded)."},
      {Dspy.AdapterParseError,
       "Raised when an adapter cannot parse an LM response into signature outputs.\n\nArgs:\n    adapter_name: Name of the adapter that failed to parse the response.\n    signature: DSPy signature whose output fields were expected.\n    lm_response: Raw LM response text or representation being parsed.\n    message: Optional additional context about the parse failure.\n    parsed_result: Partial parsed result, if any."},
      {Dspy.Lm15.RefusalPart,
       "Model explicitly refused to respond.\n\nRefusals require non-empty text because they are final semantic content;\nempty ``TextPart``/``ThinkingPart`` values remain allowed for streaming\nreassembly and provider redaction edge cases."},
      {Dspy.Lm15.RegisteredProvider,
       "A declared provider as DSPy binds it: the lm15 definition (routing,\ncredentials, wire) plus what DSPy alone needs — which metadata-snapshot\nnamespaces may describe and price its models, and the caller's own\nstatements of model support. Aliases on the definition are routing\nspellings only; they grant no metadata."},
      {Dspy.Primitives.FinalOutput,
       "Returned by interpreter.execute() when SUBMIT() is called.\n\nThis signals that the code execution loop should terminate and return\nthe contained output to the caller."},
      {Dspy.Lm15.AsyncOpenAICodexLM,
       "AsyncOpenAICodexLM(api_key: 'Credential | None' = None, account_id: 'str | None' = None, auth_path: \"'str | os.PathLike[str] | None'\" = None, transport: 'AsyncTransport' = <factory>, base_url: 'str' = 'https://chatgpt.com/backend-api/codex', originator: 'str' = 'lm15', client_version: 'str' = '0.147.0', *, adaptations: 'AdaptationPolicy' = 'note')"},
      {Dspy.Lm15.StreamStartEvent, "The response stream has started."},
      {Dspy.Primitives.ReplTypes.REPLHistory,
       "Container for REPL interaction history.\n\nImmutable: append() returns a new instance with the entry added."},
      {Dspy.Utils.StatusMessageProvider,
       "Provides customizable status message streaming for DSPy programs.\n\nThis class serves as a base for creating custom status message providers. Users can subclass\nand override its methods to define specific status messages for different stages of program execution,\neach method must return a string.\n\nExamples:\n```python\nclass MyStatusMessageProvider(StatusMessageProvider):\n    def lm_start_status_message(self, instance, inputs):\n        return f\"Calling LM with inputs {inputs}...\"\n\n    def module_end_status_message(self, outputs):\n        return f\"Module finished with output: {outputs}!\"\n\nprogram = dspy.streamify(dspy.Predict(\"q->a\"), status_message_provider=MyStatusMessageProvider())\n```"},
      {Dspy.Primitives.PythonInterpreterClass,
       "Local interpreter for secure Python execution using Deno and Pyodide.\n\nImplements the Interpreter protocol for secure code execution in a\nWASM-based sandbox. Code runs in an isolated Pyodide environment with\nno access to the host filesystem, network, or environment by default.\n\nPrerequisites:\n    Install the managed Deno runtime with ``pip install \"dspy[deno]\"``, or\n    install a compatible Deno 2.x release system-wide.\n\nExamples:\n    ```python\n    # Basic execution\n    with PythonInterpreter() as interp:\n        result = interp(\"print(1 + 2)\")  # Returns \"3\"\n\n    # With host-side tools\n    def my_tool(question: str) -> str:\n        return \"answer\"\n\n    with PythonInterpreter(tools={\"my_tool\": my_tool}) as interp:\n        result = interp(\"print(my_tool(question='test'))\")\n    ```"},
      {Dspy.Experimental.Document,
       "Experimental: This class may change or be removed in a future release without warning (introduced in v3.0.4).\n\nA document type for providing content that can be cited by language models.\n\nThis type represents documents that can be passed to language models for citation-enabled\nresponses, particularly useful with Anthropic's Citations API. Documents include the content\nand metadata that helps the LM understand and reference the source material.\n\nAttributes:\n    data: The text content of the document\n    title: Optional title for the document (used in citations)\n    media_type: MIME type of the document content (defaults to \"text/plain\")\n    context: Optional context information about the document\n\nExamples:\n    ```python\n    import dspy\n    from dspy.signatures import Signature\n    from dspy.experimental import Document, Citations\n\n    class AnswerWithSources(Signature):\n        '''Answer questions using provided documents with citations.'''\n        documents: list[Document] = dspy.InputField()\n        question: str = dspy.InputField()\n        answer: str = dspy.OutputField()\n        citations: Citations = dspy.OutputField()\n\n    # Create documents\n    docs = [\n        Document(\n            data=\"The Earth orbits the Sun in an elliptical path.\",\n            title=\"Basic Astronomy Facts\"\n        ),\n        Document(\n            data=\"Water boils at 100°C at standard atmospheric pressure.\",\n            title=\"Physics Fundamentals\",\n        )\n    ]\n\n    # Use with a citation-supporting model\n    lm = dspy.LM(\"anthropic/claude-opus-4-1-20250805\")\n    predictor = dspy.Predict(AnswerWithSources)\n    result = predictor(documents=docs, question=\"What temperature does water boil?\", lm=lm)\n    print(result.citations)\n    ```"},
      {Dspy.Lm15.AsyncXaiLM,
       "Async mirror of :class:`XaiLM` (subscription OAuth or bearer key)."},
      {Dspy.Adapter,
       "Base Adapter class.\n\nThe Adapter serves as the interface layer between DSPy module/signature and Language Models (LMs). It handles the\ncomplete transformation pipeline from DSPy inputs to LM calls and back to structured outputs.\n\nKey responsibilities:\n    - Transform user inputs and signatures into properly formatted LM prompts, which also instructs the LM to format\n        the response in a specific format.\n    - Parse LM outputs into dictionaries matching the signature's output fields.\n    - Enable/disable native LM features (function calling, citations, etc.) based on configuration.\n    - Handle conversation history, few-shot examples, and custom type processing.\n\nThe adapter pattern allows DSPy to work with different LM interfaces while maintaining a consistent programming\nmodel for users."},
      {Dspy.Experimental.TypeSafe,
       "Experimental: This class may change or be removed in a future release without warning.\n\nCall TypeSafe's System One API with DSPy's shared request cache.\n\nInstall ``dspy[typesafe]`` to use this client. Model, API key, and endpoint\ndefault to TYPESAFE_DEFAULT_MODEL, TYPESAFE_API_KEY, and TYPESAFE_BASE_URL.\nConfigure with ``dspy.configure(lm=dspy.experimental.TypeSafe(...))`` or pass\n``lm=`` when calling a predictor. Predict selects System One translation automatically.\n\nArgs:\n    model: Model name (defaults to ``jev-latest``).\n    api_key: Credential; omitted from saved state and history.\n    base_url: Endpoint (defaults to ``https://api.typesafe.ai``).\n    cache: Whether to use DSPy's cache.\n    timeout: Per-operation timeout in seconds."},
      {Dspy.Primitives.Module,
       "Base class for all DSPy modules (programs).\n\nA Module is a building block for DSPy programs that can contain predictors,\nsub-modules, and custom logic. Modules can be composed together to create\ncomplex pipelines and can be optimized using DSPy's teleprompters.\n\nAll DSPy programs should inherit from this class and implement a ``forward``\nmethod that defines the program's logic.\n\nArgs:\n    callbacks: Optional list of callback handlers for instrumentation\n        and monitoring.\n\nAttributes:\n    callbacks: List of registered callback handlers.\n    history: List of LM call history for this module.\n\nExamples:\n    >>> import dspy\n    >>> class MyProgram(dspy.Module):\n    ...     def __init__(self):\n    ...         super().__init__()\n    ...         self.predictor = dspy.Predict(\"question -> answer\")\n    ...\n    ...     def forward(self, question):\n    ...         return self.predictor(question=question)"},
      {Dspy.Lm15.AsyncClaudeCodeLM,
       "AsyncClaudeCodeLM(api_key: 'Credential | None' = None, credentials_path: \"'str | os.PathLike[str] | None'\" = None, transport: 'AsyncTransport' = <factory>, base_url: 'str' = 'https://api.anthropic.com/v1', api_version: 'str' = '2023-06-01', claude_code_version: 'str' = '2.1.170', *, adaptations: 'AdaptationPolicy' = 'note')"},
      {Dspy.Evaluate.SemanticF1,
       "Computes semantic F1 between a prediction and ground truth via LLM-based precision/recall.\n\nArgs:\n    threshold: Minimum F1 score to accept during optimization. Defaults to 0.66.\n    decompositional: If True, uses DecompositionalSemanticRecallPrecision. Defaults to False."},
      {Dspy.Adapters.Type,
       "Base class to support creating custom types for DSPy signatures.\n\nThis is the parent class of DSPy custom types, e.g, dspy.Image. Subclasses must implement the `format` method to\nreturn a list of dictionaries (same as the Array of content parts in the OpenAI API user message's content field).\n\nExamples:\n\n    ```python\n    class Image(Type):\n        url: str\n\n        def format(self) -> list[dict[str, Any]]:\n            return [{\"type\": \"image_url\", \"image_url\": {\"url\": self.url}}]\n    ```"},
      {Dspy.Lm15.ToolDerivation,
       "Full, typed account of a derivation — :func:`tool`'s explain() analogue.\n\nPython-only diagnostic surface (other lm15 languages derive from\nidiomatic sources — struct tags, macros, schema literals — and do not\nmirror this type)."},
      {Dspy.Lm15.RouterConfig,
       "Everything the router consults.  All explicit, nothing discovered\nbehind your back.  Catalog use is opt-in: pass\n``registry=ModelRegistry.discover()``.\n\n``api_keys`` maps provider string -> credential and beats env (repr-\nsuppressed; lets hermetic tests pass ``env={}``).  A credential is a\nstatic key string or a zero-argument provider callable, resolved per\nrequest by the adapter.  ``env`` defaults to ``os.environ`` at\nlookup time. An exact provider entry wins; otherwise one entry for a\nprovider with the identical non-empty declared env-key tuple supplies\nthe credential (e.g. ``openai`` also covers ``openai-chat``). Multiple\nshared candidates raise rather than guess an account. URLs and host\nsettings are never shared this way.\n\n``base_urls`` maps provider string -> the URL the provider's LM is\nbuilt with, replacing the adapter's (or the preset's) default: a\nproxy in front of OpenAI, a vLLM server on another port.  On a cloud\ndoor (azure, bedrock, vertex) the entry is the endpoint root — what\nthe console shows (``https://acct.services.ai.azure.com``), a private\nendpoint, a gateway, a sovereign cloud — and the door appends its own\npath (``/openai/v1``, ``/anthropic/v1``) unless the URL already ends\nwith it; the door's auth scheme, error mapping and doctor stay\nattached.  Without an entry the vendor's own variable is read\n(``AZURE_OPENAI_ENDPOINT``, ``ANTHROPIC_FOUNDRY_BASE_URL``,\n``AWS_ENDPOINT_URL_BEDROCK_RUNTIME`` / ``AWS_ENDPOINT_URL``), then the\nURL is built from ``settings`` (resource, region).  With an endpoint,\n``resource`` is not needed; ``region`` still is on AWS (it signs).\n\n``credentials`` maps a cloud provider string -> one named identity:\n``\"platform\"`` (the machine's own: managed identity, instance or task\nrole, attached service account), ``\"workload\"`` (the federated\nKubernetes kind), ``\"environment\"`` (a service principal or static\nkeys from env variables), ``\"cli\"`` (``az`` / ``aws`` / ``gcloud``\nsign-in).  That rung only is used and the cloud's chain is not walked:\nan absent named identity is a ``NotConfiguredError`` at construction,\nnever a fall-through to a developer login (spec/auth.md AUTH-1, amended\n2026-09-19).  An ``api_keys`` entry and a ``credentials`` entry for\nthe same provider is refused: two answers to \"who am I\".\n\n``providers`` declares providers the registry does not list — a\ngateway, a service lm15 has not receipted — as\n:class:`~lm15.registry.ProviderDefinition` values\n(``ProviderDefinition.chat(access, compat=OpenAIChatCompat(...))``).\nThey route like registry entries in every router built with this\nconfig (``id:model``, ``id/model``, each alias likewise), take\n``api_keys``/``base_urls`` entries, and answer\n``Resolution.declared``: no live receipt backs them, and lm15 says so.\nAn id or alias that a registry entry or litellm prefix already spells\nis refused: one string, one door.\n\n``timeouts`` (:class:`lm15.Timeouts`) and ``max_connections`` shape the\none transport the router builds and shares across its LMs; defaults\nare the provider SDKs' (connect 10 s, read/write/pool 600 s, 100\nconnections).  ``transport`` replaces that transport with one you\nbuilt; it cannot be combined with the two knobs, which would then\nsilently not apply."},
      {Dspy.Lm15.LMRouter,
       "Routes model strings to provider LMs.\n\nFour methods, no state you can't see: config is frozen; the only\nmutation is an LM cache keyed by provider (one LM per provider,\nbuilt lazily, reused) and the one transport those LMs share."},
      {Dspy.Primitives.CodeInterpreterError,
       "Base class for errors reported by a code interpreter.\n\nA bare instance indicates a failure that submitted code cannot repair, such\nas invalid host-side setup or a process/protocol failure. Recoverable\nsubmitted-code failures use :class:`CodeExecutionError`. Implementations\nshould make process/protocol failures terminal for that interpreter session."},
      {Dspy.Lm15.FilePage,
       "One page of stored files plus an opaque continuation cursor.\n\n``next_cursor`` is provider-issued and opaque: pass it back to\n``file_list`` to fetch the next page; ``None`` means the listing is\ncomplete."},
      {Dspy.PythonInterpreter,
       "Local interpreter for secure Python execution using Deno and Pyodide.\n\nImplements the Interpreter protocol for secure code execution in a\nWASM-based sandbox. Code runs in an isolated Pyodide environment with\nno access to the host filesystem, network, or environment by default.\n\nPrerequisites:\n    Install the managed Deno runtime with ``pip install \"dspy[deno]\"``, or\n    install a compatible Deno 2.x release system-wide.\n\nExamples:\n    ```python\n    # Basic execution\n    with PythonInterpreter() as interp:\n        result = interp(\"print(1 + 2)\")  # Returns \"3\"\n\n    # With host-side tools\n    def my_tool(question: str) -> str:\n        return \"answer\"\n\n    with PythonInterpreter(tools={\"my_tool\": my_tool}) as interp:\n        result = interp(\"print(my_tool(question='test'))\")\n    ```"},
      {Dspy.DSPyError,
       "Base class for DSPy errors with structured metadata.\n\nArgs:\n    message: Human-readable error message.\n    code: Stable DSPy error code. Defaults to the class code.\n    model: Model identifier involved in the failure.\n    provider: Provider or backend that returned the error.\n    provider_code: Provider-specific error code, when available.\n    status: HTTP status code, when the error came from an HTTP response.\n    request_id: Provider request ID, when available.\n    retry_after: Suggested retry delay in seconds, when available."},
      {Dspy.Adapters.Types.Type,
       "Base class to support creating custom types for DSPy signatures.\n\nThis is the parent class of DSPy custom types, e.g, dspy.Image. Subclasses must implement the `format` method to\nreturn a list of dictionaries (same as the Array of content parts in the OpenAI API user message's content field).\n\nExamples:\n\n    ```python\n    class Image(Type):\n        url: str\n\n        def format(self) -> list[dict[str, Any]]:\n            return [{\"type\": \"image_url\", \"image_url\": {\"url\": self.url}}]\n    ```"},
      {Dspy.KNN, ""},
      {Dspy.FinalOutput,
       "Returned by interpreter.execute() when SUBMIT() is called.\n\nThis signals that the code execution loop should terminate and return\nthe contained output to the caller."},
      {Dspy.Predict.MultiChainComparison,
       "Base class for all DSPy modules (programs).\n\nA Module is a building block for DSPy programs that can contain predictors,\nsub-modules, and custom logic. Modules can be composed together to create\ncomplex pipelines and can be optimized using DSPy's teleprompters.\n\nAll DSPy programs should inherit from this class and implement a ``forward``\nmethod that defines the program's logic.\n\nArgs:\n    callbacks: Optional list of callback handlers for instrumentation\n        and monitoring.\n\nAttributes:\n    callbacks: List of registered callback handlers.\n    history: List of LM call history for this module.\n\nExamples:\n    >>> import dspy\n    >>> class MyProgram(dspy.Module):\n    ...     def __init__(self):\n    ...         super().__init__()\n    ...         self.predictor = dspy.Predict(\"question -> answer\")\n    ...\n    ...     def forward(self, question):\n    ...         return self.predictor(question=question)"},
      {Dspy.Lm15.Response,
       "The composed artifact returned by a foundation model.\n\n``Response`` keeps only minimal convenience properties.  Use\n``response.message.first(...)`` and ``response.message.parts_of(...)``\nfor variant-specific content access.\n\n``logprobs_complete=False`` means local text editing could not preserve\nscores for all retained text. Remaining scores describe whole original\ntokens only; never infer a probability for the unscored text. True is\nthe default and does not promise the provider supplied scores at all."},
      {Dspy.Experimental.Score,
       "Experimental: This class may change or be removed in a future release without warning.\n\nA continuous score with a declared rubric and confidence.\n\nDeclare ``Score[\"poor\", \"fair\", \"excellent\"]`` in increasing order.\nPredict obtains a distribution and confidence from the backend and\naverages the level indices using those probabilities.\n``probabilities`` retains the raw distribution, keyed by rubric index.\n``level`` is the zero-based ordinal selected by the predictor's cuts on the mean\nlevel index. It does not change the continuous value or provider confidence.\nThe backend does not generate the derived value or level independently.\n\nUse the configured Score as the field annotation. Access ``.value`` or\ncall ``float(result)`` to obtain the continuous numeric value."},
      {Dspy.Lm15.ImageDelta, "An image fragment, addressed by exactly one of data/url/file_id."},
      {Dspy.Adapters.XMLAdapter,
       "Default Adapter for most language models.\n\nThe ChatAdapter formats DSPy signatures into a format compatible with most language models.\nIt uses delimiter patterns like `[[ ## field_name ## ]]` to clearly separate input and output fields in\nthe message content.\n\nKey features:\n    - Structures inputs and outputs using field header markers for clear field delineation.\n    - Provides automatic fallback to JSONAdapter if the chat format fails."},
      {Dspy.Teleprompt.Reanchor.ReAnchor,
       "Experimental: This class may change or be removed in a future release without warning.\n\nCalibrate a program's decisions: fit every threshold, Score cut, and Choice weight against the metric.\n\nThe program can be a single `Predict` or any `dspy.Module` holding predictors with decision\noutputs, and the metric is the only supervision. Each parameter changes how `Predict` reads the\nbackend's probabilities without adding request parameters. Identical requests reuse cached\nanswers, but changed upstream decisions can produce new downstream requests.\n\nReAnchor enables probability-based execution for every compatible output through the predictor's\n`fields`. It keeps each fitted configuration only when it beats the original behavior and passes\na fold check; otherwise it restores the original configuration, including absent field entries.\n\nArgs:\n    metric: Per-example metric to maximize, as in `dspy.Evaluate`. It may return a number, or a\n        `dspy.Prediction` with a `score`.\n    num_threads: Evaluation concurrency, as in `dspy.Evaluate`.\n    log_dir: When set, report.json is written here.\n    require_cache: When True, check caching on statically bound or globally configured clients.\n        Set it to False for runtime client selection or to allow uncached requests.\n\nAfter `compile`, `report` holds the fitted parameters and the metric's mean before and after\ncalibration, on the training set and on the validation set when one is given."},
      {Dspy.History,
       "Class representing the conversation history.\n\nThe conversation history is a list of messages, each message entity should have keys from the associated signature.\nFor example, if you have the following signature:\n\n```\nclass MySignature(dspy.Signature):\n    question: str = dspy.InputField()\n    history: dspy.History = dspy.InputField()\n    answer: str = dspy.OutputField()\n```\n\nThen the history should be a list of dictionaries with keys \"question\" and \"answer\".\n\nExamples:\n    ```\n    import dspy\n\n    dspy.configure(lm=dspy.LM(\"openai/gpt-4o-mini\"))\n\n    class MySignature(dspy.Signature):\n        question: str = dspy.InputField()\n        history: dspy.History = dspy.InputField()\n        answer: str = dspy.OutputField()\n\n    history = dspy.History(\n        messages=[\n            {\"question\": \"What is the capital of France?\", \"answer\": \"Paris\"},\n            {\"question\": \"What is the capital of Germany?\", \"answer\": \"Berlin\"},\n        ]\n    )\n\n    predict = dspy.Predict(MySignature)\n    outputs = predict(question=\"What is the capital of France?\", history=history)\n    ```\n\nExample of capturing the conversation history:\n    ```\n    import dspy\n\n    dspy.configure(lm=dspy.LM(\"openai/gpt-4o-mini\"))\n\n    class MySignature(dspy.Signature):\n        question: str = dspy.InputField()\n        history: dspy.History = dspy.InputField()\n        answer: str = dspy.OutputField()\n\n    predict = dspy.Predict(MySignature)\n    outputs = predict(question=\"What is the capital of France?\")\n    history = dspy.History(messages=[{\"question\": \"What is the capital of France?\", **outputs}])\n    outputs_with_history = predict(question=\"Are you sure?\", history=history)\n    ```"},
      {Dspy.ColBERTv2, "Wrapper for the ColBERTv2 Retrieval."},
      {Dspy.Clients.TrainingJob,
       "A future representing an asynchronous model fine-tuning job.\n\nThis class extends `concurrent.futures.Future` to represent a fine-tuning job\nthat can be monitored and cancelled. Subclasses should implement the `status`\nmethod to provide job status information specific to their provider.\n\nAttributes:\n    thread: The thread running the training job, if applicable.\n    model: The model identifier being fine-tuned.\n    train_data: The training data used for fine-tuning.\n    train_data_format: The format of the training data.\n    train_kwargs: Additional keyword arguments for training configuration.\n\nExamples:\n    ```python\n    from dspy.clients.provider import TrainingJob\n\n    job = TrainingJob(\n        model=\"gpt-3.5-turbo\",\n        train_data=[{\"messages\": [...]}],\n        train_data_format=TrainDataFormat.CHAT\n    )\n    # Check if job is done\n    if job.done():\n        result = job.result()\n    ```"},
      {Dspy.Completions, ""},
      {Dspy.Teleprompt.MIPROv2, ""},
      {Dspy.Lm15.CapabilityError,
       "Requested capability is not supported by this provider adapter.\n\n``feature`` (MAP-13, 2026-09-14) is the config path of what was\nrefused — ``config.top_k``, ``config.reasoning.thinking_budget``,\n``messages[0].parts[1]``, ``tools[2]`` — so the caller's own policy\nlayer can drop it and retry without parsing the message.  Absent\nwhen the refusal is not about one addressable field."},
      {Dspy.Adapters.ChatAdapter,
       "Default Adapter for most language models.\n\nThe ChatAdapter formats DSPy signatures into a format compatible with most language models.\nIt uses delimiter patterns like `[[ ## field_name ## ]]` to clearly separate input and output fields in\nthe message content.\n\nKey features:\n    - Structures inputs and outputs using field header markers for clear field delineation.\n    - Provides automatic fallback to JSONAdapter if the chat format fails."},
      {Dspy.Teleprompt.BootstrapFewShot, ""},
      {Dspy.EvaluateClass,
       "DSPy Evaluate class.\n\nThis class is used to evaluate the performance of a DSPy program. Users need to provide a evaluation dataset and\na metric function in order to use this class. This class supports parallel evaluation on the provided dataset."},
      {Dspy.Lm15.DataPart,
       "Structured data as content (changes/2026-09-17-judgments.md, D2).\n\nIn a user/system message: structured input (a provider that reads\nJSON state takes ``value`` as such; a text-only wire gets it as JSON\ntext).  In an assistant message: the answer to a ``json_schema``\nrequest that declares judgments (MAP-14) — ``value`` is the model's\nJSON object, ``probabilities`` a distribution per judgment over its\ndeclared keys when one was measured, ``method`` how (JudgmentMethod).\n``value`` is an opaque payload: any JSON value, verbatim (INV-002)."},
      {Dspy.Adapters.File,
       "A file input type for DSPy.\nSee https://platform.openai.com/docs/api-reference/chat/create#chat_create-messages-user_message-content-array_of_content_parts-file_content_part-file for specification.\n\nThe file_data field should be a data URI with the format:\n    data:<mime_type>;base64,<base64_encoded_data>\n\nConstruct from in-memory values only: raw ``bytes``, a data URI string, a\n``{\"file_data\"|\"file_id\"|\"filename\"}`` dict, or another ``File``. Construction and adapter\nparsing never access the filesystem; use :meth:`from_path` to read a local file,\n:meth:`from_bytes` for raw bytes, or :meth:`from_file_id` to reference a preuploaded file.\n\nExamples:\n    ```python\n    import dspy\n\n    class QA(dspy.Signature):\n        file: dspy.File = dspy.InputField()\n        summary = dspy.OutputField()\n    program = dspy.Predict(QA)\n    result = program(file=dspy.File.from_path(\"./research.pdf\"))\n    print(result.summary)\n    ```"},
      {Dspy.Propose.GroundedProposer, ""},
      {Dspy.LMError,
       "Base class for language model errors.\n\nCatch this class for failures at DSPy's engine and capability boundaries.\nConcrete subclasses identify local configuration, transport, authentication,\nrate limits, invalid requests, unsupported features, and provider failures.\nInvalid Python API arguments still raise TypeError/ValueError; missing\ndependencies raise ImportError. Cancellation and warning policies propagate."},
      {Dspy.Lm15.ModelSupport,
       "What a model behind a declared provider supports, stated by the caller.\n\nA compat policy says what the wire accepts; whether a model honours it is\nmodel knowledge. None means \"not stated\": DSPy then reads the metadata\nsnapshot through the provider's ``metadata_namespaces``, and treats what\nis still unknown as unsupported (the rule built-in providers live with)."},
      {Dspy.LabeledFewShot, ""},
      {Dspy.Primitives.SandboxSerializable,
       "Abstract base for types that support RLM sandbox injection.\n\nSubclasses implement four methods:\n\n- ``sandbox_setup``: Python statements (usually imports) executed once\n  in the sandbox. The returned text is also surfaced to the LLM in the\n  variable description, so the model knows which names are in scope\n  (e.g. ``pd`` when pandas is imported).\n- ``to_sandbox``: serialize the value to text bytes or binary bytes.\n- ``sandbox_assignment``: Python code that reconstructs the value from\n  a data expression.\n- ``rlm_preview``: a short, LLM-friendly description of the value.\n\nExample::\n\n    class DataFrame(SandboxSerializable):\n        def __init__(self, df):\n            self.data = df\n\n        def sandbox_setup(self) -> str:\n            return \"import pandas as pd\\nimport base64\\nimport io\"\n\n        def to_sandbox(self) -> bytes:\n            return base64.b64encode(self.data.to_parquet(index=False))\n\n        def sandbox_assignment(self, var_name: str, data_expr: str) -> str:\n            return f\"{var_name} = pd.read_parquet(io.BytesIO(base64.b64decode({data_expr})))\"\n\n        def rlm_preview(self, max_chars: int = 500) -> str:\n            return f\"DataFrame: {self.data.shape[0]} rows x {self.data.shape[1]} columns\"\n\nSubclasses can be used directly as :class:`dspy.Signature` field\nannotations because of the inherited ``__get_pydantic_core_schema__``\nhook (see the module docstring for what that hook does and why it is\nneeded)."},
      {Dspy.BootstrapRS, ""},
      {Dspy.Lm15.OpenAIChatLM,
       "Adapter for the OpenAI Chat Completions wire dialect.\n\n``compat`` may be an :class:`OpenAIChatCompat`, a preset name\n(``\"ollama\"``, ``\"groq\"``, ``\"openrouter\"``, ``\"vllm\"``, ``\"sglang\"``,\n``\"openai\"``, …), or None (plain OpenAI policy).  A preset name also\nsupplies that server's default ``base_url``; an explicit non-default\n``base_url`` argument always wins."},
      {Dspy.Lm15.SpeechGenerationRequest,
       "Text-to-speech.  Named for what every wire actually sells: speech.\n\n``voice`` and ``format`` take the provider's own vocabularies.  An\nomitted field means the SERVER decides (OpenAI's real default format\nis MP3); lm15 injects no defaults of its own.  ``format`` raises on\nproviders whose wire has no slot for it (Gemini: always PCM)."},
      {Dspy.Lm15.Turn,
       "One materialized turn.\n\n``ended_by`` is one of ``turn_end`` / ``interrupted`` / ``error`` /\n``tool_call``. ``usage`` is the field-wise sum of every ``usage`` and\n``turn_end`` event the turn saw: a tool-call response's tokens arrive\nas a ``usage`` event at the start of the continuation turn, and a\ncancelled response's tokens precede its ``interrupted``. A ``tool_call`` ending mirrors the non-live\n``finish_reason=\"tool_call\"`` contract: the model is waiting for\nYOUR result — answer with ``send_tool_result()`` and materialize the\nnext turn. turn() collects within a configurable byte/event budget.\nMaterializing adds combined text/audio copies; use raw session events\nwhen retaining a whole turn is not wanted."},
      {Dspy.LMInvalidRequestError, "The provider rejected the request shape or resource."},
      {Dspy.Lm15.StreamCleanupWarning,
       "A stream's source failed after the Response was already complete.\n\nEmitted (``warnings.warn``) when, after the end event has been yielded,\nthe source raises while draining or its ``close()``/``aclose()`` raises.\nThe provider finished the turn and billed it; the Response is returned\nunchanged.  The failure is about the connection's afterlife, not the\nanswer, so it is never raised from ``response`` — a caller who wants it\nprogrammatically reads ``ResponseStream.cleanup_errors``, and a caller\nwho wants it fatal runs with ``-W error::lm15.errors.StreamCleanupWarning``."},
      {Dspy.Adapters.Types.History,
       "Class representing the conversation history.\n\nThe conversation history is a list of messages, each message entity should have keys from the associated signature.\nFor example, if you have the following signature:\n\n```\nclass MySignature(dspy.Signature):\n    question: str = dspy.InputField()\n    history: dspy.History = dspy.InputField()\n    answer: str = dspy.OutputField()\n```\n\nThen the history should be a list of dictionaries with keys \"question\" and \"answer\".\n\nExamples:\n    ```\n    import dspy\n\n    dspy.configure(lm=dspy.LM(\"openai/gpt-4o-mini\"))\n\n    class MySignature(dspy.Signature):\n        question: str = dspy.InputField()\n        history: dspy.History = dspy.InputField()\n        answer: str = dspy.OutputField()\n\n    history = dspy.History(\n        messages=[\n            {\"question\": \"What is the capital of France?\", \"answer\": \"Paris\"},\n            {\"question\": \"What is the capital of Germany?\", \"answer\": \"Berlin\"},\n        ]\n    )\n\n    predict = dspy.Predict(MySignature)\n    outputs = predict(question=\"What is the capital of France?\", history=history)\n    ```\n\nExample of capturing the conversation history:\n    ```\n    import dspy\n\n    dspy.configure(lm=dspy.LM(\"openai/gpt-4o-mini\"))\n\n    class MySignature(dspy.Signature):\n        question: str = dspy.InputField()\n        history: dspy.History = dspy.InputField()\n        answer: str = dspy.OutputField()\n\n    predict = dspy.Predict(MySignature)\n    outputs = predict(question=\"What is the capital of France?\")\n    history = dspy.History(messages=[{\"question\": \"What is the capital of France?\", **outputs}])\n    outputs_with_history = predict(question=\"Are you sure?\", history=history)\n    ```"},
      {Dspy.Streaming.StreamListener,
       "Class that listens to the stream to capture the streeaming of a specific output field of a predictor."},
      {Dspy.Lm15.ClaudeCodeLM,
       "Anthropic Messages adapter authenticated with local Claude Code OAuth."},
      {Dspy.Clients.Engines.Engine,
       "Execution backends for DSPy's LM layer.\n\nThe outer LM layer selects these engines and owns caching, retries, fan-out\nand bookkeeping. No engine silently delegates a failed request to another\nbackend."},
      {Dspy.Lm15.TopLogprob,
       "One scored alternative token at a decoding step.\n\n``bytes`` is the token's UTF-8 byte sequence when the provider reports\nit (OpenAI); ``token_id`` is the vocabulary id when the provider\nreports it (Gemini).  Absence means \"not reported\", never zero."},
      {Dspy.Experimental.Choice,
       "Experimental: This class may change or be removed in a future release without warning.\n\nA typed option value with confidence and optional provider probabilities.\n\nDeclare ``Choice[(\"billing\", \"Payment issue\"), (\"technical\", \"Product bug\")]``.\nOption values may be strings, integers, booleans, or None. Values retain\ntheir Python types; provider probability keys are their string labels.\n``Annotated[Literal[...], Choice[...]]`` supplies criteria and enables\nevidence decoding while returning a native member. Both declarations must\ncontain the same typed values. Bare ``Choice`` metadata uses the Literal's values."},
      {Dspy.Adapters.Types.Image,
       "Base class to support creating custom types for DSPy signatures.\n\nThis is the parent class of DSPy custom types, e.g, dspy.Image. Subclasses must implement the `format` method to\nreturn a list of dictionaries (same as the Array of content parts in the OpenAI API user message's content field).\n\nExamples:\n\n    ```python\n    class Image(Type):\n        url: str\n\n        def format(self) -> list[dict[str, Any]]:\n            return [{\"type\": \"image_url\", \"image_url\": {\"url\": self.url}}]\n    ```"},
      {Dspy.Lm15.ContinuationState,
       "Opaque provider-owned state needed to continue/replay a transcript.\n\nContinuation state is not visible model content.  It travels with the\nMessage or Part it describes so provider adapters can reconstruct future\nprovider requests without relying on detached response metadata."},
      {Dspy.Lm15.SpeechGenerationResponse,
       "SpeechGenerationResponse(audio: 'AudioPart', id: 'str | None' = None, model: 'str | None' = None, usage: 'Usage' = <factory>, provider_data: 'ProviderData | None' = None)"},
      {Dspy.Embedder,
       "DSPy embedding class.\n\nThe class for computing embeddings for text inputs. This class provides a unified interface for both:\n\n1. Hosted embedding models (e.g. OpenAI's text-embedding-3-small) via litellm integration\n2. Custom embedding functions that you provide\n\nFor hosted models, simply pass the model name as a string (e.g., \"openai/text-embedding-3-small\"). The class will use\nlitellm to handle the API calls and caching.\n\nFor custom embedding models, pass a callable function that:\n- Takes a list of strings as input.\n- Returns embeddings as either:\n    - A 2D numpy array of float32 values\n    - A 2D list of float32 values\n- Each row should represent one embedding vector\n\nArgs:\n    model: The embedding model to use. This can be either a string (representing the name of the hosted embedding\n        model, must be an embedding model supported by litellm) or a callable that represents a custom embedding\n        model.\n    batch_size (int, optional): The default batch size for processing inputs in batches. Defaults to 200.\n    caching (bool, optional): Whether to cache the embedding response when using a hosted model. Defaults to True.\n    **kwargs: Additional default keyword arguments to pass to the embedding model.\n\nExamples:\n    Example 1: Using a hosted model.\n\n    ```python\n    import dspy\n\n    embedder = dspy.Embedder(\"openai/text-embedding-3-small\", batch_size=100)\n    embeddings = embedder([\"hello\", \"world\"])\n\n    assert embeddings.shape == (2, 1536)\n    ```\n\n    Example 2: Using any local embedding model, e.g. from https://huggingface.co/models?library=sentence-transformers.\n\n    ```python\n    # pip install sentence_transformers\n    import dspy\n    from sentence_transformers import SentenceTransformer\n\n    # Load an extremely efficient local model for retrieval\n    model = SentenceTransformer(\"sentence-transformers/static-retrieval-mrl-en-v1\", device=\"cpu\")\n\n    embedder = dspy.Embedder(model.encode)\n    embeddings = embedder([\"hello\", \"world\"], batch_size=1)\n\n    assert embeddings.shape == (2, 1024)\n    ```\n\n    Example 3: Using a custom function.\n\n    ```python\n    import dspy\n    import numpy as np\n\n    def my_embedder(texts):\n        return np.random.rand(len(texts), 10)\n\n    embedder = dspy.Embedder(my_embedder)\n    embeddings = embedder([\"hello\", \"world\"], batch_size=1)\n\n    assert embeddings.shape == (2, 10)\n    ```"},
      {Dspy.Clients.LM,
       "A language model supporting chat or text completion requests for use with DSPy modules.\n\nUse lm(\"hello\") for a list-returning convenience call, or pass an explicit\ndspy.lm15.Request to receive a dspy.lm15.Response. OpenAI-style messages=\ndictionaries are deprecated and scheduled for removal in DSPy 3.5. Adapters\nand custom engines must migrate to the canonical request/response contract.\nSee https://dspy.ai/community/normalized-lm-api-migration/."},
      {Dspy.Lm15.Request,
       "A complete request to a foundation model.\n\nThe composed artifact sent to the model — conversation history,\nsystem instructions, available tools, and generation config."},
      {Dspy.Lm15.ProviderLM,
       "Use the lm15 types and providers bundled with DSPy.\n\nImport public names here rather than from DSPy's private vendor directory.\nThese are the original lm15 objects, not copies or subclasses. `dspy.LM`\naccepts explicit Request objects and uses them in its native engine path.\n\nExamples:\n    >>> from dspy.lm15 import Message, Request\n    >>> request = Request(model=\"example\", messages=(Message.user(\"Hello\"),))\n    >>> request.model\n    'example'\n\nThis module also holds the providers declared to DSPy's native engine for\nthe process (`register_provider`). lm15 routes model strings through a\nregistry of receipted providers and lets a caller declare more with\n`RouterConfig(providers=...)`. A registration is an environment fact, like\nan API-key variable: a saved program records only the model string, and\nloading it needs the same registration in place. Each `dspy.LM` binds the\nregistrations present when it is constructed and keeps them for its whole\nlife, so one LM never sees two definitions of a provider."},
      {Dspy.LMTransportError,
       "The LM request failed before the provider returned a response.\n\nThis commonly represents network, DNS, TLS, connection-reset, or similar\nclient-side transport failures."},
      {Dspy.Teleprompt.KNNFewShot, ""},
      {Dspy.LMBillingError, "The provider rejected the request because billing or quota failed."},
      {Dspy.Clients.Engines.AsyncEngine,
       "Execution backends for DSPy's LM layer.\n\nThe outer LM layer selects these engines and owns caching, retries, fan-out\nand bookkeeping. No engine silently delegates a failed request to another\nbackend."},
      {Dspy.BetterTogether,
       "A meta-optimizer that combines prompt and weight optimization in configurable sequences.\n\nBetterTogether is a meta-optimizer proposed in the paper\n[Fine-Tuning and Prompt Optimization: Two Great Steps that Work Better Together](https://arxiv.org/abs/2407.10930).\nIt combines prompt optimization and weight optimization (fine-tuning) by applying them in a\nconfigurable sequence, allowing a student program to iteratively improve both its prompts and\nmodel parameters.\n\nThe core insight is that prompt and weight optimization can complement each other: prompt\noptimization can potentially discover effective task decompositions and reasoning strategies,\nwhile weight optimization can specialize the model to execute these patterns more efficiently.\nUsing these approaches together in sequences (e.g., prompt optimization then weight optimization)\nmay allow each to build on the improvements made by the other. Empirically, this approach often\noutperforms either strategy alone, even with state-of-the-art optimizers. For example, a\n[Databricks case study](https://www.databricks.com/blog/building-state-art-enterprise-agents-90x-cheaper-automated-prompt-optimization)\nshows that combining BetterTogether with GEPA and fine-tuning outperforms either approach\nalone.\n\nThe optimizer is initialized with a metric and custom optimizers. For example, you can combine\nGEPA for prompt optimization with BootstrapFinetune for weight optimization:\n``BetterTogether(metric=metric, p=GEPA(...), w=BootstrapFinetune(...))``. The ``compile()``\nmethod takes a student program, trainset, and strategy string where strategy keys correspond\nto the optimizer names from initialization. It executes each optimizer in the specified sequence.\nWhen a validation set is provided, the best performing program is returned; otherwise, the\nlatest program is returned. Note: Weight optimizers like BootstrapFinetune require student\nprograms to have LMs explicitly set (not relying on global dspy.settings.lm), and BetterTogether\nmirrors this requirement for simplicity. Therefore we call ``set_lm`` before compiling.\n\n    >>> from dspy.teleprompt import GEPA, BootstrapFinetune\n    >>>\n    >>> # Combine GEPA for prompt optimization with BootstrapFinetune for weight optimization\n    >>> optimizer = BetterTogether(\n    ...     metric=metric,\n    ...     p=GEPA(metric=metric, auto=\"medium\"),\n    ...     w=BootstrapFinetune(metric=metric)\n    ... )\n    >>>\n    >>> student.set_lm(lm)\n    >>> compiled = optimizer.compile(\n    ...     student,\n    ...     trainset=trainset,\n    ...     valset=valset,\n    ...     strategy=\"p -> w\"\n    ... )\n\nYou can pass optimizer-specific arguments to each optimizer's ``compile()`` method using\n``optimizer_compile_args``. This allows you to customize each optimizer's behavior:\n\n    >>> from dspy.teleprompt import MIPROv2\n    >>>\n    >>> # Use MIPROv2 for prompt optimization with custom parameters\n    >>> optimizer = BetterTogether(\n    ...     metric=metric,\n    ...     p=MIPROv2(metric=metric),\n    ...     w=BootstrapFinetune(metric=metric)\n    ... )\n    >>>\n    >>> student.set_lm(lm)\n    >>> compiled = optimizer.compile(\n    ...     student,\n    ...     trainset=trainset,\n    ...     valset=valset,\n    ...     strategy=\"p -> w\",\n    ...     optimizer_compile_args={\n    ...         \"p\": {\"num_trials\": 10, \"max_bootstrapped_demos\": 8},  # Configure MIPROv2's compile arguments\n    ...     }\n    ... )\n\nSince BetterTogether is a meta-optimizer that can run arbitrary optimizers in sequence, any\nsequence of optimizers can be combined together. The optimizer names used in the strategy string\ncorrespond to the keyword arguments specified in the constructor. For example, different prompt\noptimizers can be alternated multiple times (though note this is just an illustration of\nBetterTogether's flexibility, not a recommended configuration):\n\n    >>> from dspy.teleprompt import MIPROv2, GEPA\n    >>>\n    >>> # Chain two optimizers three times: MIPROv2 -> GEPA -> MIPROv2\n    >>> optimizer = BetterTogether(\n    ...     metric=metric,\n    ...     mipro=MIPROv2(metric=metric, auto=\"light\"),\n    ...     gepa=GEPA(metric=metric, auto=\"light\")\n    ... )\n    >>>\n    >>> student.set_lm(lm)\n    >>> compiled = optimizer.compile(\n    ...     student,\n    ...     trainset=trainset,\n    ...     valset=valset,\n    ...     strategy=\"mipro -> gepa -> mipro\"\n    ... )\n\nNote:\n    Output Attributes: The returned program includes two additional attributes:\n    ``candidate_programs`` and ``flag_compilation_error_occurred``. The ``candidate_programs``\n    attribute is a list of dicts, each containing 'program', 'score', and 'strategy' (e.g.,\n    '', 'p', 'p -> w', 'p -> w -> p'), sorted by descending score (similar to\n    ``dspy.MIPROv2.candidate_programs``). If any optimizer step fails,\n    ``flag_compilation_error_occurred`` is set to True and the best program found so far is\n    returned.\n\n    Model Lifecycle Management: BetterTogether automatically manages language model lifecycle\n    (launching, killing, and relaunching after fine-tuning), which are no-ops for most\n    API-based LMs. This is particularly important when using weight optimizers like\n    BootstrapFinetune with local providers (e.g., dspy.LocalProvider), as it handles model\n    initialization and cleanup between optimization steps."},
      {Dspy.Predict.BestOfN,
       "Runs a module up to N times and returns the highest-scoring prediction.\n\nAt each attempt, the module is called with a different rollout ID at\n``temperature=1.0`` to encourage output diversity. The attempt with the\nhighest reward is returned, or the first attempt whose reward meets or\nexceeds ``threshold`` (whichever comes first).\n\nArgs:\n    module: The DSPy module to run repeatedly.\n    N: Maximum number of attempts.\n    reward_fn: A callable that takes the input kwargs dict and a\n        ``Prediction``, and returns a scalar float reward score.\n    threshold: If an attempt's reward is at or above this value, that\n        prediction is returned immediately without further attempts.\n    fail_count: Number of allowed failures before raising an exception.\n        Defaults to ``N`` if not provided.\n\nExample:\n    >>> import dspy\n    >>> dspy.configure(lm=dspy.LM(\"openai/gpt-4o-mini\"))\n    >>> qa = dspy.ChainOfThought(\"question -> answer\")\n    >>> def one_word_answer(args, pred):\n    ...     return 1.0 if len(pred.answer.split()) == 1 else 0.0\n    >>> best_of_3 = dspy.BestOfN(module=qa, N=3, reward_fn=one_word_answer, threshold=1.0)\n    >>> result = best_of_3(question=\"What is the capital of Belgium?\")\n    >>> print(result.answer)  # Brussels"},
      {Dspy.Lm15.AsyncGeminiLM,
       "AsyncGeminiLM(api_key: 'Credential | None' = None, transport: 'AsyncTransport' = <factory>, base_url: 'str' = 'https://generativelanguage.googleapis.com/v1beta', upload_base_url: 'str' = 'https://generativelanguage.googleapis.com/upload/v1beta', access: 'ProviderManifest | None' = None, credentials_path: \"'str | os.PathLike[str] | None'\" = None, settings: \"'Mapping[str, str] | None'\" = None, clock: \"'Callable[[], datetime] | None'\" = None, *, credential: 'str | None' = None, adaptations: 'AdaptationPolicy' = 'note')"},
      {Dspy.Primitives.LocalInterpreter,
       "Execute Python in a persistent local subprocess.\n\nThis separates generated code from DSPy's memory, stdout, and lifecycle,\nbut it is not a security sandbox. The worker retains the host user's files,\nenvironment, credentials, subprocess, and network authority.\n\nArgs:\n    tools: Host functions exposed to executed code by name. Arguments and\n        return values must be JSON-compatible.\n    output_fields: Optional field definitions for typed ``SUBMIT`` calls.\n    execution_timeout: Maximum seconds for one execution, including host\n        tool calls. A timeout terminates the worker and its session state.\n        Python cannot forcibly stop a running host callable, so a timed-out\n        callable may finish in a detached daemon thread; its result is discarded.\n    callbacks: Optional instance-level callback handlers."},
      {Dspy.BootstrapFewShot, ""},
      {Dspy.Datasets.MATH, ""},
      {Dspy.Primitives.Completions, ""},
      {Dspy.Predict.CodeAct,
       ".. deprecated:: 3.4\n    CodeAct is deprecated. RLM is the preferred replacement.\n\nCodeAct is a module that utilizes the Code Interpreter and predefined tools to solve the problem."},
      {Dspy.AvatarOptimizer, ""},
      {Dspy.Lm15.StreamDeltaEvent, "A typed content delta arrived."},
      {Dspy.BaseModule, ""},
      {Dspy.BaseLM,
       "Base class for DSPy language models.\n\nLegacy subclasses implement forward(prompt=None, messages=None, **kwargs)\nand optionally aforward with the same arguments. Ordinary calls return\nlists of strings or dictionaries. The built-in LM also accepts explicit\ndspy.lm15.Request calls. The experimental setting does not change outputs.\n\nImplementing custom LMs through forward()/aforward() is deprecated. The old\nsubclass interface remains supported throughout DSPy 3.4 and is scheduled\nfor removal in 3.5. Implement an engine with complete(Request) -> Response\nand pass it to dspy.LM(engine=...) instead. LegacyEngine and AsyncLegacyEngine\nare transition wrappers for 3.4 only and are also scheduled for removal in 3.5.\nOpenAI-style messages= dictionaries are deprecated too: use lm15.Request\nand Message objects instead. lm(\"hello\") remains a list-returning convenience.\nSee the [migration guide](https://dspy.ai/community/normalized-lm-api-migration/#custom-engines-and-legacy-plugins).\n\nPersistent custom state belongs in dump_state/load_state. Runtime clients\nare shared by copy(), while DSPy history, callbacks and kwargs are isolated."},
      {Dspy.Lm15.UnknownModelError,
       "The router found no provider for a model string.\n\nNo routable provider prefix, no catalog match, and no rule matched\n(spec/vocabularies.md ``unknown_model``, 2026-09-08).  Local and\npre-network: no provider was asked — a provider's own \"no such model\"\nreply is :class:`UnsupportedModelError`.  Carries ``model`` (the string\nas requested); ``rules_tried`` and ``catalog_searched`` are the\nreference's diagnostics, not contract payload."},
      {Dspy.Predict.Parallel, ""},
      {Dspy.Primitives.CodeInterpreter,
       "Protocol for code execution environments (interpreters).\n\nImplementations must provide:\n- start(): Initialize the interpreter (optional, can be lazy)\n- execute(): Run code and return results\n- shutdown(): Clean up resources\n\nThe interpreter maintains state across execute() calls within a session,\nallowing variables defined in one call to be used in subsequent calls.\n\nLifecycle:\n    1. Create instance (config only, no resources allocated)\n    2. start() - Initialize interpreter (explicit) or let execute() do it (lazy)\n    3. execute() - Run code (can be called many times)\n    4. shutdown() - Release resources\n\nExample implementations:\n    - PythonInterpreter: Deno/Pyodide WASM interpreter (local sandbox)\n    - LocalInterpreter: Persistent local CPython worker (not a sandbox)\n    - MockInterpreter: Scriptable responses for testing\n\nPooling:\n    For interpreter pooling, call start() to pre-warm instances, then\n    distribute execute() calls across the pool."},
      {Dspy.Lm15.ThinkingPart,
       "Model reasoning trace.\n\nHidden thinking (Anthropic ``redacted_thinking``, an OpenAI reasoning\nitem with no summary) is a ThinkingPart with empty ``text`` and its\nreplay state in ``continuation``.  There is no flag and no placeholder\ntext (MAP-7 rule 11, ratified 2026-09-06)."},
      {Dspy.Lm15.AsyncOpenAIChatLM,
       "AsyncOpenAIChatLM(api_key: 'Credential | None' = None, transport: 'AsyncTransport' = <factory>, base_url: 'str' = 'https://api.openai.com/v1', compat: 'Any | None' = None, access: 'ProviderManifest | None' = None, credentials_path: \"'str | os.PathLike[str] | None'\" = None, settings: \"'Mapping[str, str] | None'\" = None, clock: \"'Callable[[], datetime] | None'\" = None, *, credential: 'str | None' = None, adaptations: 'AdaptationPolicy' = 'note')"},
      {Dspy.Primitives.ReplTypes.REPLEntry,
       "A single REPL interaction entry containing reasoning, code, and output."},
      {Dspy.LMAuthError, "The provider rejected the request because authentication failed."},
      {Dspy.Teleprompt.GEPA,
       "Experimental: This class may change or be removed in a future release without warning (introduced in v3.0.0).\n\n\nGEPA is an evolutionary optimizer, which uses reflection to evolve text components\nof complex systems. GEPA is proposed in the paper [GEPA: Reflective Prompt Evolution Can Outperform Reinforcement Learning](https://arxiv.org/abs/2507.19457).\nThe GEPA optimization engine is provided by the `gepa` package, available from [https://github.com/gepa-ai/gepa](https://github.com/gepa-ai/gepa).\n\nGEPA captures full traces of the DSPy module's execution, identifies the parts of the trace\ncorresponding to a specific predictor, and reflects on the behaviour of the predictor to\npropose a new instruction for the predictor. GEPA allows users to provide textual feedback\nto the optimizer, which is used to guide the evolution of the predictor. The textual feedback\ncan be provided at the granularity of individual predictors, or at the level of the entire system's\nexecution.\n\nTo provide feedback to the GEPA optimizer, implement a metric as follows:\n```\ndef metric(\n    gold: Example,\n    pred: Prediction,\n    trace: Optional[DSPyTrace] = None,\n    pred_name: Optional[str] = None,\n    pred_trace: Optional[DSPyTrace] = None,\n    program_trace: Optional[DSPyTrace] = None,\n) -> float | ScoreWithFeedback:\n    \"\"\"\n    This function is called with the following arguments:\n    - gold: The gold example.\n    - pred: The predicted output.\n    - trace: Optional. The trace of the program's execution.\n    - pred_name: Optional. The name of the target predictor currently being optimized by GEPA, for which\n        the feedback is being requested.\n    - pred_trace: Optional. The trace of the target predictor's execution GEPA is seeking feedback for.\n    - program_trace: Optional. The program's execution trace, supplied at scoring time when a `dspy.Flex`\n        submodule is being optimized. Declare it to score against how the answer was produced (e.g. an\n        LM-call penalty). Defaults to None.\n\n    Note the `pred_name` and `pred_trace` arguments. During optimization, GEPA will call the metric to obtain\n    feedback for individual predictors being optimized. GEPA provides the name of the predictor in `pred_name`\n    and the sub-trace (of the trace) corresponding to the predictor in `pred_trace`.\n    If available at the predictor level, the metric should return {'score': float, 'feedback': str} corresponding\n    to the predictor.\n    If not available at the predictor level, the metric can also return a text feedback at the program level\n    (using just the gold, pred and trace).\n    If no feedback is returned, GEPA will use a simple text feedback consisting of just the score:\n    f\"This trajectory got a score of {score}.\"\n    \"\"\"\n    ...\n```\n\nGEPA can also be used as a batch inference-time search strategy, by passing `valset=trainset, track_stats=True, track_best_outputs=True`, and using the\n`detailed_results` attribute of the optimized program (returned by `compile`) to get the Pareto frontier of the batch. `optimized_program.detailed_results.best_outputs_valset` will contain the best outputs for each task in the batch.\n\nExamples:\n```\ngepa = GEPA(metric=metric, track_stats=True)\nbatch_of_tasks = [dspy.Example(...) for task in tasks]\nnew_prog = gepa.compile(student, trainset=trainset, valset=batch_of_tasks)\npareto_frontier = new_prog.detailed_results.val_aggregate_scores\n# pareto_frontier is a list of scores, one for each task in the batch.\n```\n\nArgs:\n    metric: The metric function to use for feedback and evaluation.\n    auto: The auto budget to use for the run. Options: \"light\", \"medium\", \"heavy\".\n    max_full_evals: The maximum number of full evaluations to perform.\n    max_metric_calls: The maximum number of metric calls to perform.\n    reflection_minibatch_size: The number of examples to use for reflection in a single GEPA step. Default is 3.\n    candidate_selection_strategy: The strategy to use for candidate selection. Default is \"pareto\",\n        which stochastically selects candidates from the Pareto frontier of all validation scores.\n        Options: \"pareto\", \"current_best\".\n    reflection_lm: The language model to use for reflection. Required parameter. GEPA benefits from\n        a strong reflection model. Consider using `dspy.LM(model='gpt-5', temperature=1.0, max_tokens=32000)`\n        for optimal performance.\n    skip_perfect_score: Whether to skip examples with perfect scores during reflection. Default is True.\n    instruction_proposer: Optional custom instruction proposer implementing GEPA's ProposalFn protocol.\n        **Default: None (recommended for most users)** - Uses GEPA's proven instruction proposer from\n        the [GEPA library](https://github.com/gepa-ai/gepa), which implements the\n        [`ProposalFn`](https://github.com/gepa-ai/gepa/blob/main/src/gepa/core/adapter.py). This default\n        proposer is highly capable and was validated across diverse experiments reported in the GEPA\n        paper and tutorials.\n\n        See documentation on custom instruction proposers\n        [here](https://dspy.ai/api/optimizers/GEPA/GEPA_Advanced/#custom-instruction-proposers).\n\n        **Advanced Feature**: Only needed for specialized scenarios:\n        - **Multi-modal handling**: Processing dspy.Image inputs alongside textual information\n        - **Nuanced control over constraints**: Fine-grained control over instruction length, format,\n          and structural requirements beyond standard feedback mechanisms\n        - **Domain-specific knowledge injection**: Specialized terminology or context that cannot be\n          provided through feedback_func alone\n        - **Provider-specific prompting**: Optimizations for specific LLM providers (OpenAI, Anthropic)\n          with unique formatting preferences\n        - **Coupled component updates**: Coordinated updates of multiple components together rather\n          than independent optimization\n        - **External knowledge integration**: Runtime access to databases, APIs, or knowledge bases\n\n        The default proposer handles the vast majority of use cases effectively. Use\n        MultiModalInstructionProposer() from dspy.teleprompt.gepa.instruction_proposal for visual\n        content or implement custom ProposalFn for highly specialized requirements.\n\n        Note: When both instruction_proposer and reflection_lm are set, the instruction_proposer is called\n        in the reflection_lm context. However, reflection_lm is optional when using a custom instruction_proposer.\n        Custom instruction proposers can invoke their own LLMs if needed.\n    code_proposer: Optional custom proposer for `dspy.Flex` code components, implementing the\n        `CodeProposalFn` protocol from `dspy.teleprompt.gepa.gepa_utils`.\n        **Default: None** - Uses the built-in code proposer, which rewrites each Flex\n        submodule's full `module_src` from the failing examples and feedback.\n\n        A custom code proposer is called once per reflection round with the code components to\n        update, the current candidate, the reflective dataset, and per-component task\n        descriptions (the rendered Flex signature) and context blurbs (available tools and\n        style notes). It must return a complete replacement `dspy.Module` subclass source per\n        component. It is invoked inside the reflection_lm context, so predictors it creates\n        use the reflection LM unless it selects a different one with `dspy.context(lm=...)`.\n        Unlike instruction_proposer, it does not satisfy the requirement for a reflection\n        provider: GEPA still requires reflection_lm or instruction_proposer to be set.\n\n        Only Flex components are routed to it; regular predictors keep going to\n        instruction_proposer (or the default instruction proposer). This parameter has no\n        effect on programs without `dspy.Flex` submodules.\n    component_selector: Custom component selector implementing the [ReflectionComponentSelector](https:/"},
      {Dspy.Signature, ""},
      {Dspy.Lm15.ProviderDefinition,
       "Everything lm15 knows about one named provider, as a value.\n\n``id``              canonical provider string (hyphenated).\n``dialect``         the wire format; names which adapter class speaks it.\n``adapter``         sync LM class; ``async_adapter`` its async mirror.\n``access``          the credential chain, headers, surfaces and default\n                    base URL (``lm15.access``); for a bound entry the\n                    router passes this to the dialect constructor.\n``compat``          compat preset name (bound registry entries), or the\n                    compat object itself (a declared entry, whose\n                    preset lives in the caller's code, not lm15's\n                    tables).\n``aliases``         extra input spellings of the provider, canonical\n                    form (``fireworks-ai`` for litellm's\n                    ``fireworks_ai/``); accepted as ``alias:model``\n                    and ``alias/model``, never emitted.\n``placeholder_key`` the key a keyless local server accepts when nothing\n                    is configured (AUTH-1 last rung); None otherwise.\n``console_url``     where a human gets a key (docs, doctor hints).\n``note``            one-line human rationale, surfaced in docs and\n                    ``Resolution.describe``."},
      {Dspy.ReAct,
       "Base class for all DSPy modules (programs).\n\nA Module is a building block for DSPy programs that can contain predictors,\nsub-modules, and custom logic. Modules can be composed together to create\ncomplex pipelines and can be optimized using DSPy's teleprompters.\n\nAll DSPy programs should inherit from this class and implement a ``forward``\nmethod that defines the program's logic.\n\nArgs:\n    callbacks: Optional list of callback handlers for instrumentation\n        and monitoring.\n\nAttributes:\n    callbacks: List of registered callback handlers.\n    history: List of LM call history for this module.\n\nExamples:\n    >>> import dspy\n    >>> class MyProgram(dspy.Module):\n    ...     def __init__(self):\n    ...         super().__init__()\n    ...         self.predictor = dspy.Predict(\"question -> answer\")\n    ...\n    ...     def forward(self, question):\n    ...         return self.predictor(question=question)"},
      {Dspy.Experimental.Citations,
       "Experimental: This class may change or be removed in a future release without warning (introduced in v3.0.4).\n\nCitations extracted from an LM response with source references.\n\nThis type represents citations returned by language models that support\ncitation extraction, particularly Anthropic's Citations API through LiteLLM.\nCitations include the quoted text and source information.\n\nExamples:\n    ```python\n    import os\n    import dspy\n    from dspy.signatures import Signature\n    from dspy.experimental import Citations, Document\n    os.environ[\"ANTHROPIC_API_KEY\"] = \"YOUR_ANTHROPIC_API_KEY\"\n\n    class AnswerWithSources(Signature):\n        '''Answer questions using provided documents with citations.'''\n        documents: list[Document] = dspy.InputField()\n        question: str = dspy.InputField()\n        answer: str = dspy.OutputField()\n        citations: Citations = dspy.OutputField()\n\n    # Create documents to provide as sources\n    docs = [\n        Document(\n            data=\"The Earth orbits the Sun in an elliptical path.\",\n            title=\"Basic Astronomy Facts\"\n        ),\n        Document(\n            data=\"Water boils at 100°C at standard atmospheric pressure.\",\n            title=\"Physics Fundamentals\",\n            metadata={\"author\": \"Dr. Smith\", \"year\": 2023}\n        )\n    ]\n\n    # Use with a model that supports citations like Claude\n    lm = dspy.LM(\"anthropic/claude-opus-4-1-20250805\")\n    predictor = dspy.Predict(AnswerWithSources)\n    result = predictor(documents=docs, question=\"What temperature does water boil?\", lm=lm)\n\n    for citation in result.citations.citations:\n        print(citation.format())\n    ```"},
      {Dspy.Lm15.FileInfo,
       "A snapshot of one provider-side stored file.\n\n``id`` is the canonical reference for this provider: paste it into a\nmedia Part's ``file_id`` and the chat mapping places it on the wire\n(OpenAI/Anthropic file ids verbatim; Gemini the file URI, because\nGemini requests address files by URI, not resource name).\n\nOptional fields stay ``None`` when the provider does not report the\nvalue (OpenAI reports no MIME type; only Gemini reports expiry).\n``downloadable`` is tri-state: ``True``/``False`` when the provider\nstates the capability, ``None`` when it does not say."},
      {Dspy.Signatures.SignatureMeta, ""},
      {Dspy.Lm15.OpenAILM,
       "OpenAI Responses dialect, bound to an access policy.\n\n``access`` defaults to the API-key policy (``lm15.access.OPENAI_API``);\n``lm15.access.OPENAI_CODEX`` binds the same dialect to the ChatGPT Codex\nbackend on a local Codex CLI login (``OpenAICodexLM`` is that binding\nunder a name). Policy consult points: the auth header and static\nheaders, the ``chatgpt-account-id`` header when the credential carries\nan account, the login hint on errors, the endpoint surfaces, and the\n``backend`` switch at four stated places — payload defaults\n(instructions prefix, ``store: false``, streaming-only, no max-token\nknob), streaming-first ``complete``, the ``{\"detail\": ...}`` error\nenvelope, and the ``/models`` endpoint shape."},
      {Dspy.Adapters.Reasoning,
       "Reasoning type in DSPy.\n\nThis type is useful when you want the DSPy output to include the reasoning of the LM. We build this type so that\nDSPy can support the reasoning model and non-reasoning model with the same code.\n\nThis is a str-like type, you can convert a string directly to a Reasoning object, and from DSPy adapters'\nperspective, `Reasoning` is treated as a string."},
      {Dspy.Lm15.TransportError,
       "High-level LM transport failure.\n\nProvider LMs wrap lower-level ``lm15.transports.TransportError`` exceptions\ninto this class."},
      {Dspy.Adapters.History,
       "Class representing the conversation history.\n\nThe conversation history is a list of messages, each message entity should have keys from the associated signature.\nFor example, if you have the following signature:\n\n```\nclass MySignature(dspy.Signature):\n    question: str = dspy.InputField()\n    history: dspy.History = dspy.InputField()\n    answer: str = dspy.OutputField()\n```\n\nThen the history should be a list of dictionaries with keys \"question\" and \"answer\".\n\nExamples:\n    ```\n    import dspy\n\n    dspy.configure(lm=dspy.LM(\"openai/gpt-4o-mini\"))\n\n    class MySignature(dspy.Signature):\n        question: str = dspy.InputField()\n        history: dspy.History = dspy.InputField()\n        answer: str = dspy.OutputField()\n\n    history = dspy.History(\n        messages=[\n            {\"question\": \"What is the capital of France?\", \"answer\": \"Paris\"},\n            {\"question\": \"What is the capital of Germany?\", \"answer\": \"Berlin\"},\n        ]\n    )\n\n    predict = dspy.Predict(MySignature)\n    outputs = predict(question=\"What is the capital of France?\", history=history)\n    ```\n\nExample of capturing the conversation history:\n    ```\n    import dspy\n\n    dspy.configure(lm=dspy.LM(\"openai/gpt-4o-mini\"))\n\n    class MySignature(dspy.Signature):\n        question: str = dspy.InputField()\n        history: dspy.History = dspy.InputField()\n        answer: str = dspy.OutputField()\n\n    predict = dspy.Predict(MySignature)\n    outputs = predict(question=\"What is the capital of France?\")\n    history = dspy.History(messages=[{\"question\": \"What is the capital of France?\", **outputs}])\n    outputs_with_history = predict(question=\"Are you sure?\", history=history)\n    ```"},
      {Dspy.Utils.DummyVectorizer, "Simple vectorizer based on n-grams."},
      {Dspy.Lm15.AsyncAnthropicLM,
       "AsyncAnthropicLM(api_key: 'Credential | None' = None, transport: 'AsyncTransport' = <factory>, base_url: 'str' = 'https://api.anthropic.com/v1', api_version: 'str' = '2023-06-01', compat: 'Any | None' = None, access: 'ProviderManifest | None' = None, credentials_path: \"'str | os.PathLike[str] | None'\" = None, settings: \"'Mapping[str, str] | None'\" = None, clock: \"'Callable[[], datetime] | None'\" = None, *, credential: 'str | None' = None, adaptations: 'AdaptationPolicy' = 'note')"},
      {Dspy.LMUnsupportedModelError,
       "The requested model is unavailable or unsupported by the provider."},
      {Dspy.Lm15.ToolChoice, "How the model should use tools."},
      {Dspy.Lm15.TypeSafeLM, "TypeSafe System One dialect (``POST /v1/systemone``)."},
      {Dspy.Lm15.AsyncTypeSafeLM,
       "Async mirror of :class:`TypeSafeLM` (composition; the inner adapter\ncannot reach the network)."},
      {Dspy.Lm15.LM15Error,
       "Base for all lm15 errors.\n\nErrors keep the human-readable exception message in ``str(error)`` while\nalso exposing structured metadata for logging, telemetry, retries, and\nprogrammatic handling."},
      {Dspy.COPRO, ""},
      {Dspy.CodeExecutionError,
       "Recoverable error raised by code running in a healthy interpreter."},
      {Dspy.Reasoning,
       "Reasoning type in DSPy.\n\nThis type is useful when you want the DSPy output to include the reasoning of the LM. We build this type so that\nDSPy can support the reasoning model and non-reasoning model with the same code.\n\nThis is a str-like type, you can convert a string directly to a Reasoning object, and from DSPy adapters'\nperspective, `Reasoning` is treated as a string."},
      {Dspy.Lm15.GeminiLM,
       "GeminiLM(api_key: 'Credential | None' = None, transport: 'SyncTransport' = <factory>, base_url: 'str' = 'https://generativelanguage.googleapis.com/v1beta', upload_base_url: 'str' = 'https://generativelanguage.googleapis.com/upload/v1beta', access: 'ProviderManifest | None' = None, credentials_path: \"'str | os.PathLike[str] | None'\" = None, settings: \"'Mapping[str, str] | None'\" = None, clock: \"'Callable[[], datetime] | None'\" = None, *, credential: 'str | None' = None, adaptations: 'AdaptationPolicy' = 'note')"},
      {Dspy.LM,
       "A language model supporting chat or text completion requests for use with DSPy modules.\n\nUse lm(\"hello\") for a list-returning convenience call, or pass an explicit\ndspy.lm15.Request to receive a dspy.lm15.Response. OpenAI-style messages=\ndictionaries are deprecated and scheduled for removal in DSPy 3.5. Adapters\nand custom engines must migrate to the canonical request/response contract.\nSee https://dspy.ai/community/normalized-lm-api-migration/."},
      {Dspy.Ensemble, ""},
      {Dspy.Predict.ChainOfThought,
       "A module that reasons step by step before producing the output.\n\nExtends a given signature with an additional ``reasoning`` output field that\nthe language model fills in before answering. This chain-of-thought process\nimproves accuracy on tasks that require multi-step logic or explanation.\n\nArgs:\n    signature: The DSPy signature defining the module's inputs and outputs.\n    rationale_field: An optional custom ``FieldInfo`` for the reasoning field.\n        If not provided, a default field with description ``${reasoning}`` is used.\n    rationale_field_type: The type annotation for the reasoning field. Defaults\n        to ``str``. Ignored if ``rationale_field`` is provided.\n    **config: Additional keyword arguments forwarded to the underlying\n        ``dspy.Predict`` module.\n\nExample:\n    >>> import dspy\n    >>> cot = dspy.ChainOfThought(\"question -> answer\")\n    >>> result = cot(question=\"What is the capital of France?\")\n    >>> print(result.reasoning)\n    >>> print(result.answer)"},
      {Dspy.TwoStepAdapter,
       "A two-stage adapter that:\n    1. Uses a simpler, more natural prompt for the main LM\n    2. Uses a smaller LM with chat adapter to extract structured data from the response of main LM\nThis adapter uses a common __call__ logic defined in base Adapter class.\nThis class is particularly useful when interacting with reasoning models as the main LM since reasoning models\nare known to struggle with structured outputs.\n\nExamples:\n```\nimport dspy\nlm = dspy.LM(model=\"openai/o3-mini\", max_tokens=16000, temperature = 1.0)\nadapter = dspy.TwoStepAdapter(dspy.LM(\"openai/gpt-4o-mini\"))\ndspy.configure(lm=lm, adapter=adapter)\nprogram = dspy.ChainOfThought(\"question->answer\")\nresult = program(\"What is the capital of France?\")\nprint(result)\n```"},
      {Dspy.LMProviderError,
       "The provider returned an error response.\n\nProvider errors include structured metadata when available, such as HTTP\n`status`, provider `request_id`, provider-specific `provider_code`, and\n`retry_after` for rate limits."},
      {Dspy.Lm15.CachedPrefix,
       "A reusable prompt beginning: ``cached = lm.cache(prefix)``.\n\n``prefix`` is a Request whose model, system, tools, and messages form\nthe reusable part (its config must be default: a cached object has no\ntemperature).  ``resource`` is the stored object on providers with\nthat tier, ``None`` on providers that mark blocks or cache\nautomatically. Optional ``provider`` preserves a router destination;\nprefix/resource models remain wire names, while suffix requests use\n``provider:wiremodel``. Reuse with the same router configuration/account.\n``request(messages)`` builds a Request that appends\n``messages`` and sets the cache boundary at the seam; ``cached +\nmessages`` is Python sugar for it.  The built Request and its wire\nbytes are what the contract pins; the sugar is per-language."},
      {Dspy.SignatureMeta, ""},
      {Dspy.Retrievers.EmbeddingsWithScores,
       "DSPy EmbeddingsWithScores retriever.\n\nThis class extends the Embeddings retriever to also return similarity scores alongside passages and indices.\nSimilarity scores enable downstream such as thresholding and re-ranking."},
      {Dspy.Lm15.CacheInfo,
       "A snapshot of one provider-side stored cache object.\n\n``id`` is the provider's reference verbatim; place it in\n``CacheConfig.resource``.  ``tokens`` is the stored token count when\nreported (storage cost = tokens x hours x the provider's rate).\n``expires_at`` is ISO-8601 UTC, normalized from the provider's form."},
      {Dspy.Lm15.ContextLengthError, "The input exceeds the model's context window."},
      {Dspy.Lm15.StreamAssemblyError,
       "A stream could not be assembled into a Response without inventing a fact.\n\nThree defects raise it (MAP-9 and MAP-3, contract change\n2026-09-11-stream-completion):\n\n- a tool call's fragments never carried a name (MAP-9): an unnamed call\n  is not actionable (MAP-1), and guessing a name from the request\n  dispatches the wrong function silently;\n- the stream ended without an end event: the finish reason and usage\n  never arrived, and reporting the text as a finished turn would invent\n  both;\n- an event arrived after the end event (MAP-3): it has no place in the\n  Response, and dropping it would be silent loss.\n\nAll three are adapter or source defects, not model behaviour — every\nshipped dialect names a call on its first fragment and ends exactly\nonce — so the message points at the adapter.\n\n``partial`` is everything that did assemble (text, thinking, other\nparts, usage, finish reason) with the offending material left out, so a\ncaller that wants to salvage the turn can; ``part_index`` is the first\noffending part (MAP-9 only).\n\nWhat does NOT raise it: a failure after the end event that is not an\nevent — the source raising while it drains, or its ``close()`` raising.\nThe Response is complete; it is returned, and the failure is reported\nas a :class:`StreamCleanupWarning`."},
      {Dspy.Lm15.ErrorDetail, "Structured error information.  A dataclass, not a dict."},
      {Dspy.Lm15.ImagePart, "An image, addressed by exactly one of data/url/file_id/path."},
      {Dspy.Adapters.Types.File,
       "A file input type for DSPy.\nSee https://platform.openai.com/docs/api-reference/chat/create#chat_create-messages-user_message-content-array_of_content_parts-file_content_part-file for specification.\n\nThe file_data field should be a data URI with the format:\n    data:<mime_type>;base64,<base64_encoded_data>\n\nConstruct from in-memory values only: raw ``bytes``, a data URI string, a\n``{\"file_data\"|\"file_id\"|\"filename\"}`` dict, or another ``File``. Construction and adapter\nparsing never access the filesystem; use :meth:`from_path` to read a local file,\n:meth:`from_bytes` for raw bytes, or :meth:`from_file_id` to reference a preuploaded file.\n\nExamples:\n    ```python\n    import dspy\n\n    class QA(dspy.Signature):\n        file: dspy.File = dspy.InputField()\n        summary = dspy.OutputField()\n    program = dspy.Predict(QA)\n    result = program(file=dspy.File.from_path(\"./research.pdf\"))\n    print(result.summary)\n    ```"},
      {Dspy.Example,
       "A flexible data container for DSPy examples and training data with named fields.\n\nAn `Example` is roughly one row from a HuggingFace dataset or pandas\n`DataFrame`. It behaves a lot like a dictionary or dot-access record: you\ncan read fields with `example[\"question\"]` or `example.question`.\n\nIn DSPy, lists of `Example` objects are your trainset, devset, and testset.\nMost examples are built from keyword arguments or an existing record, then\ntagged with `with_inputs(...)` to say which fields should be fed into a\nmodule. The remaining fields are labels or metadata.\n\nWhen you write evaluation code, custom optimizers, or training loops, use\n`example.inputs()` for the fields you want to pass to a module, and use\n`example.labels()` for the fields you want to compare against the module's\noutput.\n\nExamples:\n    Build one from keyword arguments:\n\n    >>> import dspy\n    >>> example = dspy.Example(\n    ...     question=\"What is the capital of France?\",\n    ...     answer=\"Paris\",\n    ... ).with_inputs(\"question\")\n    >>> example.question\n    'What is the capital of France?'\n    >>> example.answer\n    'Paris'\n    >>> example.inputs().toDict()\n    {'question': 'What is the capital of France?'}\n\n    Build one from an existing record:\n\n    >>> record = {\"question\": \"What is 2+2?\", \"answer\": \"4\"}\n    >>> example = dspy.Example(**record).with_inputs(\"question\")\n    >>> example[\"question\"]\n    'What is 2+2?'\n    >>> example.labels().answer\n    '4'\n\n    Mark which fields are inputs:\n\n    >>> example = dspy.Example(\n    ...     question=\"What is the weather?\",\n    ...     answer=\"It's sunny\",\n    ... ).with_inputs(\"question\")\n    >>> example.inputs().question\n    'What is the weather?'\n    >>> example.labels().answer\n    \"It's sunny\"\n\n    Use examples in a trainset:\n\n    >>> trainset = [\n    ...     dspy.Example(question=\"What is 2+2?\", answer=\"4\").with_inputs(\"question\"),\n    ...     dspy.Example(question=\"What is 3+3?\", answer=\"6\").with_inputs(\"question\"),\n    ... ]\n    >>> trainset[0].inputs().toDict()\n    {'question': 'What is 2+2?'}\n\n    Use an example in a metric:\n\n    >>> def exact_match_metric(example, pred, trace=None):\n    ...     return example.answer.lower() == pred.answer.lower()\n    >>> gold = dspy.Example(question=\"What is 1+1?\", answer=\"2\").with_inputs(\"question\")\n    >>> pred = dspy.Prediction(answer=\"2\")\n    >>> exact_match_metric(gold, pred)\n    True\n\n    Use it like a dictionary:\n\n    >>> example = dspy.Example(name=\"Alice\", age=30).with_inputs(\"name\")\n    >>> \"name\" in example\n    True\n    >>> example.get(\"city\", \"Unknown\")\n    'Unknown'\n\nSee Also:\n    [`dspy.Evaluate`][dspy.Evaluate]: Evaluate a program on a list of\n        `Example`s.\n    [`Metrics`](../../learn/evaluation/metrics.md): Write metric functions\n        that compare an `Example` with a prediction."},
      {Dspy.Predict,
       "Basic DSPy module that maps inputs to outputs using a language model.\n\nArgs:\n    signature: The input/output signature describing the task.\n    callbacks: Optional list of callbacks for instrumentation.\n    **config: Default keyword arguments forwarded to the underlying\n        language model. These values can be overridden for a single\n        invocation by passing a ``config`` dictionary when calling the\n        module. For example::\n\n            predict = dspy.Predict(\"q -> a\", rollout_id=1, temperature=1.0)\n            predict(q=\"What is 1 + 52?\", config={\"rollout_id\": 2, \"temperature\": 1.0})"},
      {Dspy.Datasets.Dataset, ""},
      {Dspy.Lm15.AnthropicLM,
       "Anthropic Messages dialect, bound to an access policy.\n\n``access`` defaults to the API-key policy (``lm15.access.ANTHROPIC_API``);\n``lm15.access.CLAUDE_CODE`` binds the same dialect to a local Claude\nCode login (``ClaudeCodeLM`` is that binding under a name). The policy\nis consulted at exactly these points: the auth header, static headers\n(``anthropic-beta`` is joined with the dialect's own betas), the system\nprefix, the login hint on errors, and the endpoint surfaces."},
      {Dspy.Lm15.ToolResultPart,
       "The result of an external computation, sent back to the model."},
      {Dspy.Lm15.VideoJob,
       "Use the lm15 types and providers bundled with DSPy.\n\nImport public names here rather than from DSPy's private vendor directory.\nThese are the original lm15 objects, not copies or subclasses. `dspy.LM`\naccepts explicit Request objects and uses them in its native engine path.\n\nExamples:\n    >>> from dspy.lm15 import Message, Request\n    >>> request = Request(model=\"example\", messages=(Message.user(\"Hello\"),))\n    >>> request.model\n    'example'\n\nThis module also holds the providers declared to DSPy's native engine for\nthe process (`register_provider`). lm15 routes model strings through a\nregistry of receipted providers and lets a caller declare more with\n`RouterConfig(providers=...)`. A registration is an environment fact, like\nan API-key variable: a saved program records only the model string, and\nloading it needs the same registration in place. Each `dspy.LM` binds the\nregistrations present when it is constructed and keeps them for its whole\nlife, so one LM never sees two definitions of a provider."},
      {Dspy.OldField, "A more ergonomic datatype that infers prefix and desc if omitted."},
      {Dspy.Lm15.CitationPart, "A reference to source material."},
      {Dspy.CodeInterpreterError,
       "Base class for errors reported by a code interpreter.\n\nA bare instance indicates a failure that submitted code cannot repair, such\nas invalid host-side setup or a process/protocol failure. Recoverable\nsubmitted-code failures use :class:`CodeExecutionError`. Implementations\nshould make process/protocol failures terminal for that interpreter session."},
      {Dspy.Clients.Engines.LegacyEngine,
       "Deprecated 3.4 transition wrapper, scheduled for removal in DSPy 3.5.\n\nMigrate the underlying implementation to complete(Request) -> Response.\nWrapping it explicitly does not extend the legacy interface's lifetime."},
      {Dspy.Lm15.XaiLM,
       "Chat Completions adapter for xAI, with subscription OAuth fallback.\n\nxAI is a provider, not an access path: its image and video wire, and\nits refusals (reasoning off, logprobs, the MAP-8 cells), are provider\nfacts and live here. Only the credential path is composed: the\n``lm15.access.XAI`` policy carries the ``oauth-unless-explicit`` chain,\nthe login hint, and the endpoint surfaces."},
      {Dspy.Predict.KNN, ""},
      {Dspy.Lm15.AsyncVideoJob,
       "Use the lm15 types and providers bundled with DSPy.\n\nImport public names here rather than from DSPy's private vendor directory.\nThese are the original lm15 objects, not copies or subclasses. `dspy.LM`\naccepts explicit Request objects and uses them in its native engine path.\n\nExamples:\n    >>> from dspy.lm15 import Message, Request\n    >>> request = Request(model=\"example\", messages=(Message.user(\"Hello\"),))\n    >>> request.model\n    'example'\n\nThis module also holds the providers declared to DSPy's native engine for\nthe process (`register_provider`). lm15 routes model strings through a\nregistry of receipted providers and lets a caller declare more with\n`RouterConfig(providers=...)`. A registration is an environment fact, like\nan API-key variable: a saved program records only the model string, and\nloading it needs the same registration in place. Each `dspy.LM` binds the\nregistrations present when it is constructed and keeps them for its whole\nlife, so one LM never sees two definitions of a provider."},
      {Dspy.Lm15.ServerError, "Provider-side failure (5xx)."},
      {Dspy.Lm15.EndpointSupport,
       "EndpointSupport(complete: 'bool' = True, stream: 'bool' = True, live: 'bool' = False, files: 'bool' = False, batches: 'bool' = False, images: 'bool' = False, speech: 'bool' = False, video: 'bool' = False, responses_api: 'bool' = False, models: 'bool' = False, caches: 'bool' = False, extra: 'frozenset[str]' = <factory>)"},
      {Dspy.Lm15.StreamErrorEvent, "The stream failed."},
      {Dspy.LocalInterpreter,
       "Execute Python in a persistent local subprocess.\n\nThis separates generated code from DSPy's memory, stdout, and lifecycle,\nbut it is not a security sandbox. The worker retains the host user's files,\nenvironment, credentials, subprocess, and network authority.\n\nArgs:\n    tools: Host functions exposed to executed code by name. Arguments and\n        return values must be JSON-compatible.\n    output_fields: Optional field definitions for typed ``SUBMIT`` calls.\n    execution_timeout: Maximum seconds for one execution, including host\n        tool calls. A timeout terminates the worker and its session state.\n        Python cannot forcibly stop a running host callable, so a timed-out\n        callable may finish in a detached daemon thread; its result is discarded.\n    callbacks: Optional instance-level callback handlers."},
      {Dspy.LMServerError, "The provider failed while handling the request."},
      {Dspy.Predict.ReActV2,
       "Experimental: This class may change or be removed in a future release without warning.\n\nAn agent that interleaves reasoning and tool calls to fulfill a signature.\n\nThe model selects tools, observes their results, and calls ``submit`` with the\nfinal outputs. Supports native function calling and text-based tool calls.\nPredictions include the declared outputs, conversation history, and termination\nreason.\n\nArgs:\n    signature: The signature defining the agent's inputs and outputs.\n    tools: Functions, callable objects, or ``dspy.Tool`` instances available to the agent.\n    max_iters: Maximum number of tool-calling turns before a final submit attempt."},
      {Dspy.Lm15.ToolConfig,
       "Options for :func:`tool` / :func:`derive`.\n\nExplicit over magic: every knob is visible.\n\ndocstring_style: ``\"auto\"`` tries Google, NumPy, then Sphinx\n    (``:param x:``) markers pragmatically; or pin one style; ``\"none\"``\n    skips parameter descriptions entirely.\ninclude_defaults: emit JSON-compatible default values as ``\"default\"``\n    in the property schema. Informational only; never affects\n    required-ness. Non-JSON defaults are silently skipped (documented).\nadditional_properties_false: emit ``\"additionalProperties\": false`` on\n    the top-level object. Off by default (provider-neutral); some\n    providers' strict tool modes want this — see the cookbook caveat\n    about strict mode also requiring all properties to be required.\noverrides: per-parameter raw JSON-Schema fragments used *in place of*\n    derivation for the named parameter, e.g.\n    ``((\"when\", {\"type\": \"string\", \"format\": \"date-time\"}),)``.\n    A surgical escape hatch: one un-derivable parameter no longer forces\n    a fully hand-written FunctionTool. Stored as a tuple of\n    ``(param_name, schema)`` pairs so ToolConfig stays a plain frozen\n    dataclass; a docstring description is still merged in when the\n    override fragment has no ``\"description\"`` of its own."},
      {Dspy.Lm15.BinaryPart, "Arbitrary binary content, addressed by exactly one media source."},
      {Dspy.Clients.Engines.AsyncLegacyEngine,
       "Deprecated async transition wrapper, scheduled for removal in DSPy 3.5."},
      {Dspy.Evaluate.EvaluationResult,
       "A class that represents the result of an evaluation.\nIt is a subclass of `dspy.Prediction` that contains the following fields\n\n- score: An float value (e.g., 67.30) representing the overall performance\n- results: a list of (example, prediction, score) tuples for each example in devset"},
      {Dspy.Adapters.Audio,
       "An audio input type for DSPy.\n\nConstruct from in-memory values only: a data URI string, raw ``bytes`` (with\n``audio_format=``), a numpy-like array, a ``{\"data\", \"audio_format\"}`` dict, or another\n``Audio``. Raw base64 is passed as ``Audio(data=..., audio_format=...)``. Construction and\nadapter parsing never access the filesystem or network; use :meth:`from_path` to read a local\nfile, :meth:`from_url` to download a remote resource, or :meth:`from_array` for array data."},
      {Dspy.RLM,
       "Experimental: This class may change or be removed in a future release without warning.\n\nRecursive Language Model module.\n\nUses a sandboxed REPL to let the LLM programmatically explore large contexts\nthrough code execution. The LLM writes Python code to examine data, call\nsub-LLMs for semantic analysis, and build up answers iteratively.\n\n``interpreter_factory`` defaults to ``PythonInterpreter`` (Deno/Pyodide/WASM), and\n``dspy.configure(interpreter_factory=...)`` replaces that default. Either route\naccepts an adapter for a remote sandbox.\nRLM updates the interpreter's mutable ``tools`` dictionary with\ninvocation-scoped tools before execution. Pass a zero-argument factory via\n``interpreter_factory=`` at call time to override the runtime for one invocation.\nRLM shuts down every interpreter it creates.\n\nExamples:\n    ```python\n    # Basic usage\n    rlm = dspy.RLM(\"context, query -> output\", max_iters=10)\n    result = rlm(context=\"...very long text...\", query=\"What is the magic number?\")\n    print(result.output)\n    ```"},
      {Dspy.Lm15.BatchJob, "A live handle on one provider-side batch job."},
      {Dspy.Teleprompt.BetterTogether,
       "A meta-optimizer that combines prompt and weight optimization in configurable sequences.\n\nBetterTogether is a meta-optimizer proposed in the paper\n[Fine-Tuning and Prompt Optimization: Two Great Steps that Work Better Together](https://arxiv.org/abs/2407.10930).\nIt combines prompt optimization and weight optimization (fine-tuning) by applying them in a\nconfigurable sequence, allowing a student program to iteratively improve both its prompts and\nmodel parameters.\n\nThe core insight is that prompt and weight optimization can complement each other: prompt\noptimization can potentially discover effective task decompositions and reasoning strategies,\nwhile weight optimization can specialize the model to execute these patterns more efficiently.\nUsing these approaches together in sequences (e.g., prompt optimization then weight optimization)\nmay allow each to build on the improvements made by the other. Empirically, this approach often\noutperforms either strategy alone, even with state-of-the-art optimizers. For example, a\n[Databricks case study](https://www.databricks.com/blog/building-state-art-enterprise-agents-90x-cheaper-automated-prompt-optimization)\nshows that combining BetterTogether with GEPA and fine-tuning outperforms either approach\nalone.\n\nThe optimizer is initialized with a metric and custom optimizers. For example, you can combine\nGEPA for prompt optimization with BootstrapFinetune for weight optimization:\n``BetterTogether(metric=metric, p=GEPA(...), w=BootstrapFinetune(...))``. The ``compile()``\nmethod takes a student program, trainset, and strategy string where strategy keys correspond\nto the optimizer names from initialization. It executes each optimizer in the specified sequence.\nWhen a validation set is provided, the best performing program is returned; otherwise, the\nlatest program is returned. Note: Weight optimizers like BootstrapFinetune require student\nprograms to have LMs explicitly set (not relying on global dspy.settings.lm), and BetterTogether\nmirrors this requirement for simplicity. Therefore we call ``set_lm`` before compiling.\n\n    >>> from dspy.teleprompt import GEPA, BootstrapFinetune\n    >>>\n    >>> # Combine GEPA for prompt optimization with BootstrapFinetune for weight optimization\n    >>> optimizer = BetterTogether(\n    ...     metric=metric,\n    ...     p=GEPA(metric=metric, auto=\"medium\"),\n    ...     w=BootstrapFinetune(metric=metric)\n    ... )\n    >>>\n    >>> student.set_lm(lm)\n    >>> compiled = optimizer.compile(\n    ...     student,\n    ...     trainset=trainset,\n    ...     valset=valset,\n    ...     strategy=\"p -> w\"\n    ... )\n\nYou can pass optimizer-specific arguments to each optimizer's ``compile()`` method using\n``optimizer_compile_args``. This allows you to customize each optimizer's behavior:\n\n    >>> from dspy.teleprompt import MIPROv2\n    >>>\n    >>> # Use MIPROv2 for prompt optimization with custom parameters\n    >>> optimizer = BetterTogether(\n    ...     metric=metric,\n    ...     p=MIPROv2(metric=metric),\n    ...     w=BootstrapFinetune(metric=metric)\n    ... )\n    >>>\n    >>> student.set_lm(lm)\n    >>> compiled = optimizer.compile(\n    ...     student,\n    ...     trainset=trainset,\n    ...     valset=valset,\n    ...     strategy=\"p -> w\",\n    ...     optimizer_compile_args={\n    ...         \"p\": {\"num_trials\": 10, \"max_bootstrapped_demos\": 8},  # Configure MIPROv2's compile arguments\n    ...     }\n    ... )\n\nSince BetterTogether is a meta-optimizer that can run arbitrary optimizers in sequence, any\nsequence of optimizers can be combined together. The optimizer names used in the strategy string\ncorrespond to the keyword arguments specified in the constructor. For example, different prompt\noptimizers can be alternated multiple times (though note this is just an illustration of\nBetterTogether's flexibility, not a recommended configuration):\n\n    >>> from dspy.teleprompt import MIPROv2, GEPA\n    >>>\n    >>> # Chain two optimizers three times: MIPROv2 -> GEPA -> MIPROv2\n    >>> optimizer = BetterTogether(\n    ...     metric=metric,\n    ...     mipro=MIPROv2(metric=metric, auto=\"light\"),\n    ...     gepa=GEPA(metric=metric, auto=\"light\")\n    ... )\n    >>>\n    >>> student.set_lm(lm)\n    >>> compiled = optimizer.compile(\n    ...     student,\n    ...     trainset=trainset,\n    ...     valset=valset,\n    ...     strategy=\"mipro -> gepa -> mipro\"\n    ... )\n\nNote:\n    Output Attributes: The returned program includes two additional attributes:\n    ``candidate_programs`` and ``flag_compilation_error_occurred``. The ``candidate_programs``\n    attribute is a list of dicts, each containing 'program', 'score', and 'strategy' (e.g.,\n    '', 'p', 'p -> w', 'p -> w -> p'), sorted by descending score (similar to\n    ``dspy.MIPROv2.candidate_programs``). If any optimizer step fails,\n    ``flag_compilation_error_occurred`` is set to True and the best program found so far is\n    returned.\n\n    Model Lifecycle Management: BetterTogether automatically manages language model lifecycle\n    (launching, killing, and relaunching after fine-tuning), which are no-ops for most\n    API-based LMs. This is particularly important when using weight optimizers like\n    BootstrapFinetune with local providers (e.g., dspy.LocalProvider), as it handles model\n    initialization and cleanup between optimization steps."},
      {Dspy.Lm15.MissingCredentialError,
       "Provider resolved but no API key was found.\n\nA :class:`lm15.errors.NotConfiguredError` (code ``not_configured``) —\nsemantically the credential case IS not-configured — so\n``except NotConfiguredError`` handlers keep working.  Carries\n``provider`` and ``env_keys`` straight from the ProviderManifest."},
      {Dspy.Adapters.Tool,
       "Tool class.\n\nThis class is used to simplify the creation of tools for tool calling (function calling) in LLMs. Only supports\nfunctions for now."},
      {Dspy.Lm15.ModelInfo,
       "ModelInfo(id: 'str', provider: 'str', api_family: 'str', aliases: 'tuple[str, ...]' = (), origin: 'ModelOrigin' = <factory>, inference: 'InferenceModelInfo | None' = None, compat: 'CompatProfile | None' = None, extensions: 'JsonObject | None' = None)"},
      {Dspy.Clients.Provider,
       "Base class for language model providers.\n\nA provider is responsible for managing language model instances, including\nlaunching, killing, and fine-tuning models. Subclasses should implement\nprovider-specific logic for these operations.\n\nAttributes:\n    finetunable: Whether this provider supports fine-tuning.\n    reinforceable: Whether this provider supports reinforcement learning.\n    TrainingJob: The class to use for training jobs (subclass of `TrainingJob`).\n    ReinforceJob: The class to use for reinforcement learning jobs (subclass of `ReinforceJob`).\n\nExamples:\n    ```python\n    from dspy.clients.provider import Provider\n\n    class MyProvider(Provider):\n        def __init__(self):\n            super().__init__()\n            self.finetunable = True\n\n        @staticmethod\n        def is_provider_model(model: str) -> bool:\n            return model.startswith(\"myprovider/\")\n\n        @staticmethod\n        def finetune(job, model, train_data, train_data_format, train_kwargs=None):\n            # Implement fine-tuning logic\n            return \"fine_tuned_model_id\"\n    ```"},
      {Dspy.Lm15.ModelRegistry,
       "ModelRegistry(_models: 'dict[tuple[str, str], ModelInfo]' = <factory>, _aliases: 'dict[tuple[str, str], tuple[str, str]]' = <factory>)"},
      {Dspy.Streaming.StatusMessageProvider,
       "Provides customizable status message streaming for DSPy programs.\n\nThis class serves as a base for creating custom status message providers. Users can subclass\nand override its methods to define specific status messages for different stages of program execution,\neach method must return a string.\n\nExamples:\n```python\nclass MyStatusMessageProvider(StatusMessageProvider):\n    def lm_start_status_message(self, instance, inputs):\n        return f\"Calling LM with inputs {inputs}...\"\n\n    def module_end_status_message(self, outputs):\n        return f\"Module finished with output: {outputs}!\"\n\nprogram = dspy.streamify(dspy.Predict(\"q->a\"), status_message_provider=MyStatusMessageProvider())\n```"},
      {Dspy.Adapters.Types.ToolCalls,
       "Base class to support creating custom types for DSPy signatures.\n\nThis is the parent class of DSPy custom types, e.g, dspy.Image. Subclasses must implement the `format` method to\nreturn a list of dictionaries (same as the Array of content parts in the OpenAI API user message's content field).\n\nExamples:\n\n    ```python\n    class Image(Type):\n        url: str\n\n        def format(self) -> list[dict[str, Any]]:\n            return [{\"type\": \"image_url\", \"image_url\": {\"url\": self.url}}]\n    ```"},
      {Dspy.Adapters.Image,
       "Base class to support creating custom types for DSPy signatures.\n\nThis is the parent class of DSPy custom types, e.g, dspy.Image. Subclasses must implement the `format` method to\nreturn a list of dictionaries (same as the Array of content parts in the OpenAI API user message's content field).\n\nExamples:\n\n    ```python\n    class Image(Type):\n        url: str\n\n        def format(self) -> list[dict[str, Any]]:\n            return [{\"type\": \"image_url\", \"image_url\": {\"url\": self.url}}]\n    ```"},
      {Dspy.Retrievers.Embeddings,
       "DSPy Embeddings retriever.\n\nThis class retrieves the top-k most similar passages from a corpus using embedding-based similarity search.\nFor large corpora, a FAISS index is built for fast approximate candidate retrieval, followed by exact\nre-ranking. For small corpora, brute-force search is used."},
      {Dspy.Clients.Engines.LiteLLMEngine,
       "One synchronous attempt. LiteLLM's process-global clients are borrowed."},
      {Dspy.Clients.Engines.AsyncLiteLLMEngine,
       "Async equivalent; cancellation propagates without retrying."},
      {Dspy.Predict.ReAct,
       "Base class for all DSPy modules (programs).\n\nA Module is a building block for DSPy programs that can contain predictors,\nsub-modules, and custom logic. Modules can be composed together to create\ncomplex pipelines and can be optimized using DSPy's teleprompters.\n\nAll DSPy programs should inherit from this class and implement a ``forward``\nmethod that defines the program's logic.\n\nArgs:\n    callbacks: Optional list of callback handlers for instrumentation\n        and monitoring.\n\nAttributes:\n    callbacks: List of registered callback handlers.\n    history: List of LM call history for this module.\n\nExamples:\n    >>> import dspy\n    >>> class MyProgram(dspy.Module):\n    ...     def __init__(self):\n    ...         super().__init__()\n    ...         self.predictor = dspy.Predict(\"question -> answer\")\n    ...\n    ...     def forward(self, question):\n    ...         return self.predictor(question=question)"},
      {Dspy.Clients.BaseLM,
       "Base class for DSPy language models.\n\nLegacy subclasses implement forward(prompt=None, messages=None, **kwargs)\nand optionally aforward with the same arguments. Ordinary calls return\nlists of strings or dictionaries. The built-in LM also accepts explicit\ndspy.lm15.Request calls. The experimental setting does not change outputs.\n\nImplementing custom LMs through forward()/aforward() is deprecated. The old\nsubclass interface remains supported throughout DSPy 3.4 and is scheduled\nfor removal in 3.5. Implement an engine with complete(Request) -> Response\nand pass it to dspy.LM(engine=...) instead. LegacyEngine and AsyncLegacyEngine\nare transition wrappers for 3.4 only and are also scheduled for removal in 3.5.\nOpenAI-style messages= dictionaries are deprecated too: use lm15.Request\nand Message objects instead. lm(\"hello\") remains a list-returning convenience.\nSee the [migration guide](https://dspy.ai/community/normalized-lm-api-migration/#custom-engines-and-legacy-plugins).\n\nPersistent custom state belongs in dump_state/load_state. Runtime clients\nare shared by copy(), while DSPy history, callbacks and kwargs are isolated."},
      {Dspy.Lm15.AnthropicCompat,
       "Partial compatibility policy for Anthropic Messages-family APIs.\n\n``model_prefixes`` refuses, before the wire, any model id that does not\nstart with one of the prefixes: DeepSeek's endpoint silently serves\n``claude-opus*`` as ``deepseek-v4-pro`` and ``claude-haiku*`` /\n``claude-sonnet*`` as ``deepseek-v4-flash`` (docs and live 2026-09-03);\nlm15 does not take part in a substitution the caller cannot see.\nNone means any model id goes out as typed."},
      {Dspy.Lm15.AsyncOpenAILM,
       "AsyncOpenAILM(api_key: 'Credential | None' = None, transport: 'AsyncTransport' = <factory>, base_url: 'str' = 'https://api.openai.com/v1', profile: 'Any | None' = None, access: 'ProviderManifest | None' = None, credentials_path: \"'str | os.PathLike[str] | None'\" = None, account_id: 'str | None' = None, *, compat: 'Any | None' = None, settings: \"'Mapping[str, str] | None'\" = None, credential: 'str | None' = None, clock: \"'Callable[[], datetime] | None'\" = None, adaptations: 'AdaptationPolicy' = 'note')"},
      {Dspy.Signatures.OldInputField,
       "A more ergonomic datatype that infers prefix and desc if omitted."},
      {Dspy.Predict.Refine,
       "Base class for all DSPy modules (programs).\n\nA Module is a building block for DSPy programs that can contain predictors,\nsub-modules, and custom logic. Modules can be composed together to create\ncomplex pipelines and can be optimized using DSPy's teleprompters.\n\nAll DSPy programs should inherit from this class and implement a ``forward``\nmethod that defines the program's logic.\n\nArgs:\n    callbacks: Optional list of callback handlers for instrumentation\n        and monitoring.\n\nAttributes:\n    callbacks: List of registered callback handlers.\n    history: List of LM call history for this module.\n\nExamples:\n    >>> import dspy\n    >>> class MyProgram(dspy.Module):\n    ...     def __init__(self):\n    ...         super().__init__()\n    ...         self.predictor = dspy.Predict(\"question -> answer\")\n    ...\n    ...     def forward(self, question):\n    ...         return self.predictor(question=question)"},
      {Dspy.Lm15.AudioPart, "Audio content, addressed by exactly one of data/url/file_id/path."},
      {Dspy.ChatAdapter,
       "Default Adapter for most language models.\n\nThe ChatAdapter formats DSPy signatures into a format compatible with most language models.\nIt uses delimiter patterns like `[[ ## field_name ## ]]` to clearly separate input and output fields in\nthe message content.\n\nKey features:\n    - Structures inputs and outputs using field header markers for clear field delineation.\n    - Provides automatic fallback to JSONAdapter if the chat format fails."},
      {Dspy.SandboxSerializable,
       "Abstract base for types that support RLM sandbox injection.\n\nSubclasses implement four methods:\n\n- ``sandbox_setup``: Python statements (usually imports) executed once\n  in the sandbox. The returned text is also surfaced to the LLM in the\n  variable description, so the model knows which names are in scope\n  (e.g. ``pd`` when pandas is imported).\n- ``to_sandbox``: serialize the value to text bytes or binary bytes.\n- ``sandbox_assignment``: Python code that reconstructs the value from\n  a data expression.\n- ``rlm_preview``: a short, LLM-friendly description of the value.\n\nExample::\n\n    class DataFrame(SandboxSerializable):\n        def __init__(self, df):\n            self.data = df\n\n        def sandbox_setup(self) -> str:\n            return \"import pandas as pd\\nimport base64\\nimport io\"\n\n        def to_sandbox(self) -> bytes:\n            return base64.b64encode(self.data.to_parquet(index=False))\n\n        def sandbox_assignment(self, var_name: str, data_expr: str) -> str:\n            return f\"{var_name} = pd.read_parquet(io.BytesIO(base64.b64decode({data_expr})))\"\n\n        def rlm_preview(self, max_chars: int = 500) -> str:\n            return f\"DataFrame: {self.data.shape[0]} rows x {self.data.shape[1]} columns\"\n\nSubclasses can be used directly as :class:`dspy.Signature` field\nannotations because of the inherited ``__get_pydantic_core_schema__``\nhook (see the module docstring for what that hook does and why it is\nneeded)."},
      {Dspy.Lm15.AmbiguousModelError,
       "The catalog matched a model string under more than one provider, or\nunder more than one entry of one provider (``ambiguous_model``,\n2026-09-08).  The fix is an explicit ``provider:`` prefix.  Carries\n``model`` and ``providers`` (every candidate, catalog order,\ndeduplicated)."},
      {Dspy.Lm15.ImageGenerationRequest,
       "Text (and optionally input images, for edits) in; images out.\n\n``size`` takes the provider's own sizing vocabulary — like ``model``\nand ``voice``, the field is portable but the values are not (OpenAI:\npixels; Gemini: aspect ratios).  ``images`` are ordinary ImageParts\n(inline data / url / file_id / path); adapters route them to the\nprovider's real edit door and raise where the wire has none."},
      {Dspy.Lm15.CacheConfig,
       "Universal prompt cache configuration (MAP-6, docs/mapping-rules.md).\n\nEvery provider caches in up to three tiers: automatic (nothing to\nsend, best-effort), breakpoint (a mark on a block, guaranteed above a\nper-model minimum), resource (a stored, named, billed object).  The\nfields name INTENTS; each adapter maps an intent to the best tier it\nhas, and the result is always visible in ``Usage.cache_read_tokens``.\n\n- mode=\"auto\": the cheapest safe instruction (a mark on the stable\n  beginning where marks exist; nothing elsewhere).\n- mode=\"off\": send nothing; where a provider has a real off switch\n  for cache WRITES, send it.\n- prefix=\"stable\": mark the end of system + tools.\n  prefix=\"history\": mark the end of the last message (a growing\n  chat).  Providers without marks fall back to the automatic tier:\n  that fallback spends nothing and its outcome is observable, the\n  two conditions that permit it.\n- prefix_until_index: the same intent with a precise position (the\n  last block of that message).  Mutually exclusive with ``prefix``.\n- retention: \"long\" asks for the longer lifetime at extra cost;\n  providers without one RAISE.\n- key: a best-effort routing/affinity hint; providers without one RAISE.\n- resource: the id of a stored cache object (``CacheInfo.id``) to\n  reference; providers without the resource tier RAISE."},
      {Dspy.LMUnexpectedError,
       "An unexpected failure occurred at the LM provider boundary.\n\nDSPy raises this for an unclassified engine failure and preserves its\noriginal exception as the cause. Only the owning engine interprets SDK\nerrors: arbitrary message text or status-like attributes do not make a\ncustom engine failure retryable. This is not a provider-response error or\na model-output parsing error."},
      {Dspy.ToolCallResults, ""},
      {Dspy.Retrieve, ""},
      {Dspy.ReActV2,
       "Experimental: This class may change or be removed in a future release without warning.\n\nAn agent that interleaves reasoning and tool calls to fulfill a signature.\n\nThe model selects tools, observes their results, and calls ``submit`` with the\nfinal outputs. Supports native function calling and text-based tool calls.\nPredictions include the declared outputs, conversation history, and termination\nreason.\n\nArgs:\n    signature: The signature defining the agent's inputs and outputs.\n    tools: Functions, callable objects, or ``dspy.Tool`` instances available to the agent.\n    max_iters: Maximum number of tool-calling turns before a final submit attempt."},
      {Dspy.Clients.Engines.AsyncLM15Engine,
       "One async attempt; owned pools are confined to their event loop."},
      {Dspy.Adapters.Types.Tool,
       "Tool class.\n\nThis class is used to simplify the creation of tools for tool calling (function calling) in LLMs. Only supports\nfunctions for now."},
      {Dspy.Primitives.PythonInterpreter.CodeExecutionError,
       "Recoverable error raised by code running in a healthy interpreter."},
      {Dspy.Utils.DummyLM,
       "Dummy language model for unit testing purposes.\n\nThree modes of operation:\n\nMode 1: List of dictionaries\n\nIf a list of dictionaries is provided, the dummy model will return the next dictionary\nin the list for each request, formatted according to the `format_field_with_value` function.\n\nExamples:\n\n```\nlm = DummyLM([{\"answer\": \"red\"}, {\"answer\": \"blue\"}])\ndspy.configure(lm=lm)\npredictor(\"What color is the sky?\")\n# Output:\n# [[## answer ##]]\n# red\npredictor(\"What color is the sky?\")\n# Output:\n# [[## answer ##]]\n# blue\n```\n\nMode 2: Dictionary of dictionaries\n\nIf a dictionary of dictionaries is provided, the dummy model will return the value\ncorresponding to the key which is contained with the final message of the prompt,\nformatted according to the `format_field_with_value` function from the chat adapter.\n\n```\nlm = DummyLM({\"What color is the sky?\": {\"answer\": \"blue\"}})\ndspy.configure(lm=lm)\npredictor(\"What color is the sky?\")\n# Output:\n# [[## answer ##]]\n# blue\n```\n\nMode 3: Follow examples\n\nIf `follow_examples` is set to True, and the prompt contains an example input exactly equal to the prompt,\nthe dummy model will return the output from that example.\n\n```\nlm = DummyLM([{\"answer\": \"red\"}], follow_examples=True)\ndspy.configure(lm=lm)\npredictor(\"What color is the sky?, demos=dspy.Example(input=\"What color is the sky?\", output=\"blue\"))\n# Output:\n# [[## answer ##]]\n# blue\n```"},
      {Dspy.Teleprompt.AvatarOptimizer, ""},
      {Dspy.EmbeddingsWithScores,
       "DSPy EmbeddingsWithScores retriever.\n\nThis class extends the Embeddings retriever to also return similarity scores alongside passages and indices.\nSimilarity scores enable downstream such as thresholding and re-ranking."},
      {Dspy.Predict.Tool,
       "Tool class.\n\nThis class is used to simplify the creation of tools for tool calling (function calling) in LLMs. Only supports\nfunctions for now."},
      {Dspy.Adapters.Types.Reasoning,
       "Reasoning type in DSPy.\n\nThis type is useful when you want the DSPy output to include the reasoning of the LM. We build this type so that\nDSPy can support the reasoning model and non-reasoning model with the same code.\n\nThis is a str-like type, you can convert a string directly to a Reasoning object, and from DSPy adapters'\nperspective, `Reasoning` is treated as a string."},
      {Dspy.BootstrapFewShotWithRandomSearch, ""},
      {Dspy.LMCollectionLimitError,
       "A local collector reached its budget; never automatically retry.\n\nRetains the canonical source error so its partial result remains lazy:\nwrapping a memory-limit failure must not allocate combined text/audio.\n``partial_events`` and ``rejected_event`` are available without that work."},
      {Dspy.Lm15.Usage,
       "Token usage.\n\n``input_tokens`` and ``output_tokens`` are common dimensions.\n``total_tokens`` is the provider-reported or billed total when present;\nproviders may include reasoning, audio, cache, or future token classes in\ntotals differently, so it is not forced to equal input + output.\n\n``reasoning_tokens`` is populated only when the provider reports an exact\nseparate reasoning/thinking token count. Some providers, notably Anthropic,\ncan return ``ThinkingPart`` content while reporting only combined\n``output_tokens``; in that case ``reasoning_tokens`` remains ``None``.\n\nEvery counter is ``int | None``: ``None`` means \"the provider did not\nreport this dimension\", which is distinct from a reported ``0``.\n``Usage()`` with no arguments therefore means \"nothing reported\" and\nserializes to ``{}`` (omitted entirely by enclosing serializers, per\ndocs/serde-rules.md).\n\n``total_tokens``: an explicit value is preserved as provider telemetry.\nWhen omitted, it auto-computes as ``input_tokens + output_tokens`` only\nwhen BOTH are present; if either is ``None`` the total stays ``None``.\n\nArithmetic over usage (e.g. ``InferencePricing.estimate``) must treat\n``None`` as \"unknown\", never as zero.\n\nCounters are provider-verbatim (spec/types.md, Usage, 2026-09-02): the\nsame field means different things across providers. ``input_tokens``\nincludes cached tokens on OpenAI, Gemini, and xAI but not on Anthropic\n(whose cache counters are disjoint); ``output_tokens`` includes\nreasoning on OpenAI and Anthropic but not on Gemini and xAI. lm15 does\nnot normalise: that is what a bill reconciles against. A consumer\ncomparing across providers must apply the provider's rule from the\ntable in the spec; ``Response.provider_data`` and the router's\nresolution say which provider answered."},
      {Dspy.Adapters.Types.Code,
       "Code type in DSPy.\n\nThis type is useful for code generation and code analysis.\n\nExample 1: dspy.Code as output type in code generation:\n\n```python\nimport dspy\n\ndspy.configure(lm=dspy.LM(\"openai/gpt-4o-mini\"))\n\n\nclass CodeGeneration(dspy.Signature):\n    '''Generate python code to answer the question.'''\n\n    question: str = dspy.InputField(description=\"The question to answer\")\n    code: dspy.Code[\"java\"] = dspy.OutputField(description=\"The code to execute\")\n\n\npredict = dspy.Predict(CodeGeneration)\n\nresult = predict(question=\"Given an array, find if any of the two numbers sum up to 10\")\nprint(result.code)\n```\n\nExample 2: dspy.Code as input type in code analysis:\n\n```python\nimport dspy\nimport inspect\n\ndspy.configure(lm=dspy.LM(\"openai/gpt-4o-mini\"))\n\nclass CodeAnalysis(dspy.Signature):\n    '''Analyze the time complexity of the function.'''\n\n    code: dspy.Code[\"python\"] = dspy.InputField(description=\"The function to analyze\")\n    result: str = dspy.OutputField(description=\"The time complexity of the function\")\n\n\npredict = dspy.Predict(CodeAnalysis)\n\n\ndef sleepsort(x):\n    import time\n\n    for i in x:\n        time.sleep(i)\n        print(i)\n\nresult = predict(code=inspect.getsource(sleepsort))\nprint(result.result)\n```"},
      {Dspy.Lm15.ProviderError, "The provider returned an error response."},
      {Dspy.Lm15.Message,
       "A contribution to a conversation, attributed to a speaker.\n\nA message is a sequence of typed Parts.  Roles:\n\n- ``user``      — end-user input\n- ``assistant``  — model output\n- ``tool``      — tool execution results\n- ``developer`` — high-authority instructions from the application\n                  developer.  Can appear mid-conversation to inject\n                  new instructions without invalidating the KV-cache\n                  prefix.  On OpenAI this maps to the native\n                  ``developer`` role; on other providers the LM\n                  converts it to a user message with a clear prefix."},
      {Dspy.Datasets.HotPotQA, ""},
      {Dspy.Teleprompt.BootstrapFewShotWithOptuna, ""},
      {Dspy.Teleprompt.InferRules, ""},
      {Dspy.Datasets.Colors, ""},
      {Dspy.Primitives.CodeExecutionError,
       "Recoverable error raised by code running in a healthy interpreter."},
      {Dspy.Lm15.AuthError, "Authentication failed — invalid, expired, or missing API key."},
      {Dspy.Clients.Embedder,
       "DSPy embedding class.\n\nThe class for computing embeddings for text inputs. This class provides a unified interface for both:\n\n1. Hosted embedding models (e.g. OpenAI's text-embedding-3-small) via litellm integration\n2. Custom embedding functions that you provide\n\nFor hosted models, simply pass the model name as a string (e.g., \"openai/text-embedding-3-small\"). The class will use\nlitellm to handle the API calls and caching.\n\nFor custom embedding models, pass a callable function that:\n- Takes a list of strings as input.\n- Returns embeddings as either:\n    - A 2D numpy array of float32 values\n    - A 2D list of float32 values\n- Each row should represent one embedding vector\n\nArgs:\n    model: The embedding model to use. This can be either a string (representing the name of the hosted embedding\n        model, must be an embedding model supported by litellm) or a callable that represents a custom embedding\n        model.\n    batch_size (int, optional): The default batch size for processing inputs in batches. Defaults to 200.\n    caching (bool, optional): Whether to cache the embedding response when using a hosted model. Defaults to True.\n    **kwargs: Additional default keyword arguments to pass to the embedding model.\n\nExamples:\n    Example 1: Using a hosted model.\n\n    ```python\n    import dspy\n\n    embedder = dspy.Embedder(\"openai/text-embedding-3-small\", batch_size=100)\n    embeddings = embedder([\"hello\", \"world\"])\n\n    assert embeddings.shape == (2, 1536)\n    ```\n\n    Example 2: Using any local embedding model, e.g. from https://huggingface.co/models?library=sentence-transformers.\n\n    ```python\n    # pip install sentence_transformers\n    import dspy\n    from sentence_transformers import SentenceTransformer\n\n    # Load an extremely efficient local model for retrieval\n    model = SentenceTransformer(\"sentence-transformers/static-retrieval-mrl-en-v1\", device=\"cpu\")\n\n    embedder = dspy.Embedder(model.encode)\n    embeddings = embedder([\"hello\", \"world\"], batch_size=1)\n\n    assert embeddings.shape == (2, 1024)\n    ```\n\n    Example 3: Using a custom function.\n\n    ```python\n    import dspy\n    import numpy as np\n\n    def my_embedder(texts):\n        return np.random.rand(len(texts), 10)\n\n    embedder = dspy.Embedder(my_embedder)\n    embeddings = embedder([\"hello\", \"world\"], batch_size=1)\n\n    assert embeddings.shape == (2, 10)\n    ```"},
      {Dspy.Primitives.PythonInterpreter,
       "Local interpreter for secure Python execution using Deno and Pyodide.\n\nImplements the Interpreter protocol for secure code execution in a\nWASM-based sandbox. Code runs in an isolated Pyodide environment with\nno access to the host filesystem, network, or environment by default.\n\nPrerequisites:\n    Install the managed Deno runtime with ``pip install \"dspy[deno]\"``, or\n    install a compatible Deno 2.x release system-wide.\n\nExamples:\n    ```python\n    # Basic execution\n    with PythonInterpreter() as interp:\n        result = interp(\"print(1 + 2)\")  # Returns \"3\"\n\n    # With host-side tools\n    def my_tool(question: str) -> str:\n        return \"answer\"\n\n    with PythonInterpreter(tools={\"my_tool\": my_tool}) as interp:\n        result = interp(\"print(my_tool(question='test'))\")\n    ```"},
      {Dspy.Adapters.Adapter,
       "Base Adapter class.\n\nThe Adapter serves as the interface layer between DSPy module/signature and Language Models (LMs). It handles the\ncomplete transformation pipeline from DSPy inputs to LM calls and back to structured outputs.\n\nKey responsibilities:\n    - Transform user inputs and signatures into properly formatted LM prompts, which also instructs the LM to format\n        the response in a specific format.\n    - Parse LM outputs into dictionaries matching the signature's output fields.\n    - Enable/disable native LM features (function calling, citations, etc.) based on configuration.\n    - Handle conversation history, few-shot examples, and custom type processing.\n\nThe adapter pattern allows DSPy to work with different LM interfaces while maintaining a consistent programming\nmodel for users."},
      {Dspy.Lm15.AudioDelta,
       "An audio fragment arriving during streaming.\n\nMedia deltas are partial stream chunks, not final media parts.  They may\ncarry unaligned base64 data, a URL/file id update, or metadata only; final\nmedia validation happens when the stream is assembled into an AudioPart."},
      {Dspy.Lm15.ThinkingDelta, "A reasoning/thinking fragment arriving during streaming."},
      {Dspy.Adapters.ToolCalls,
       "Base class to support creating custom types for DSPy signatures.\n\nThis is the parent class of DSPy custom types, e.g, dspy.Image. Subclasses must implement the `format` method to\nreturn a list of dictionaries (same as the Array of content parts in the OpenAI API user message's content field).\n\nExamples:\n\n    ```python\n    class Image(Type):\n        url: str\n\n        def format(self) -> list[dict[str, Any]]:\n            return [{\"type\": \"image_url\", \"image_url\": {\"url\": self.url}}]\n    ```"},
      {Dspy.LMUnsupportedFeatureError,
       "The LM, provider, or DSPy provider wrapper does not support a requested feature.\n\nArgs:\n    message: Human-readable error message.\n    feature: One unsupported setting path reported by the engine.\n    features: Feature names that were requested but unavailable, such as\n        `\"finetuning\"`, `\"reinforce\"`, or `\"structured_outputs\"`.\n    issues: Optional detailed reasons the requested feature could not be\n        used.\n    **kwargs: Structured error metadata accepted by `DSPyError`."},
      {Dspy.Utils.StatusMessage, "Dataclass that wraps a status message for status streaming."},
      {Dspy.Lm15.AsyncBatchJob,
       "Async twin of :class:`BatchJob`; identical types, awaitable verbs."},
      {Dspy.Lm15.Timeouts,
       "How long to wait, in seconds, at each step of a request.\n\n- ``connect``: TCP + TLS (and a proxy CONNECT) to the host.\n- ``read``: the next byte of the reply — headers first, then each body\n  chunk.  A model that is still thinking sends nothing, so this is the\n  one to raise for slow local models and long non-streaming answers.\n- ``write``: sending the request bytes.\n- ``pool``: a free connection when ``max_connections`` are all busy.\n  ``None`` waits as long as it takes.\n\nEvery value must be positive (or ``None`` for ``pool``); a zero or\nnegative timeout is a configuration mistake, not \"no timeout\"."},
      {Dspy.Lm15.RateLimitError, "Rate limited by the provider (HTTP 429)."},
      {Dspy.JSONAdapter,
       "Default Adapter for most language models.\n\nThe ChatAdapter formats DSPy signatures into a format compatible with most language models.\nIt uses delimiter patterns like `[[ ## field_name ## ]]` to clearly separate input and output fields in\nthe message content.\n\nKey features:\n    - Structures inputs and outputs using field header markers for clear field delineation.\n    - Provides automatic fallback to JSONAdapter if the chat format fails."},
      {Dspy.Refine,
       "Base class for all DSPy modules (programs).\n\nA Module is a building block for DSPy programs that can contain predictors,\nsub-modules, and custom logic. Modules can be composed together to create\ncomplex pipelines and can be optimized using DSPy's teleprompters.\n\nAll DSPy programs should inherit from this class and implement a ``forward``\nmethod that defines the program's logic.\n\nArgs:\n    callbacks: Optional list of callback handlers for instrumentation\n        and monitoring.\n\nAttributes:\n    callbacks: List of registered callback handlers.\n    history: List of LM call history for this module.\n\nExamples:\n    >>> import dspy\n    >>> class MyProgram(dspy.Module):\n    ...     def __init__(self):\n    ...         super().__init__()\n    ...         self.predictor = dspy.Predict(\"question -> answer\")\n    ...\n    ...     def forward(self, question):\n    ...         return self.predictor(question=question)"},
      {Dspy.LMNotConfiguredError,
       "The LM is missing required provider configuration or credentials."},
      {Dspy.Lm15.Reasoning,
       "How much hidden thinking the model does before it answers (MAP-7).\n\n``effort`` is the one dial and is required.  Vocabulary: ``off``,\n``minimal``, ``low``, ``medium``, ``high``, ``xhigh``, ``max``.  Every\nprovider has this dial with these words; each model accepts a subset\nand rejects the rest loudly.  Adapters send the word verbatim where\nthe provider has levels; budget-only model classes (Anthropic 4.5 and\nearlier, Gemini 2.5) express it through one documented grading table.\nWords with no native level on a provider RAISE rather than downgrade.\n\nThere is no \"adaptive\" value: leaving ``config.reasoning`` unset is\nhow you let the model decide, on every provider.  ``effort=\"off\"``\nreaches the wire as the provider's disable or fails loudly where the\nprovider cannot disable (MAP-5).\n\n``thinking_budget`` is a token cap on providers that count thinking\nseparately (Anthropic manual class, Gemini); it RAISES where the wire\nhas no budget.  On budget-only classes the budget is the spelling on\nthe wire and ``effort`` is the intent.  ``total_budget`` was removed\n2026-09-02: ``Config.max_tokens`` is the ceiling.\n\n``summary`` is visibility: ``None`` = provider default, ``\"auto\"`` =\nshow the thinking where a knob exists (satisfied silently where the\nprovider always shows it), ``\"concise\"``/``\"detailed\"`` = OpenAI's\ndetail levels (RAISE elsewhere)."},
      {Dspy.Lm15.BatchEntry,
       "The fate of one request in a batch, in submission order.\n\nPartial failure is a first-class outcome: a ``completed`` job may mix\n``succeeded`` and ``errored`` entries. ``succeeded`` carries a full\ncanonical Response; ``errored`` carries a canonical ErrorDetail;\n``cancelled`` and ``expired`` carry neither."},
      {Dspy.Lm15.VideoGenerationRequest,
       "Text (and optionally an input image) in; a video JOB out.\n\nVideo is job-shaped on every wire that sells it (Sora, Veo,\ngrok-imagine): submission returns a ticket, not bytes.  ``seconds``\nmaps to the provider's duration knob and raises where the wire has\nnone; ``images`` are input frames (image-to-video), ordinary\nImageParts."},
      {Dspy.Predict.Flex,
       "Experimental: This class may change or be removed in a future release without warning (introduced in v3.3.0).\n\nA module whose implementation is optimizable code, not just a prompt.\n\nConstruct it like any module (``dspy.Flex(MySignature)``). It starts as a baseline that delegates\nto a single ``dspy.Predict`` over the signature — or ``dspy.RLM`` when ``tools`` are given, so the\nbaseline can call them. ``dspy.GEPA`` recognizes ``Flex`` instances by type and rewrites their\nsource — a single ``dspy.Module`` subclass, exposed as ``module_src`` — into decomposed predictors\nplus plain Python instead of only tuning instructions.\n\nThe optimizer-authored code runs inside an interpreter. ``Flex`` never\nruns it in the host Python process. ``interpreter_factory`` defaults to\n``dspy.PythonInterpreter`` (Deno/Pyodide) and must be a zero-argument callable returning a new\n``CodeInterpreter``. ``dspy.configure(interpreter_factory=...)`` replaces that default.\nFlex may validate or lower source and install its guest shim before execution; a custom\ninterpreter therefore defines the Python and standard-library subset available to that source.\nThe optimizer-authored glue runs isolated; only provided-tool calls, predictor construction,\nand predictor calls bridge back to the host, which makes the real LM calls.\n\nArgs:\n    signature: A ``dspy.Signature`` class or string declaring inputs/outputs.\n    tools: ``dspy.Tool`` instances or named callables.\n    interpreter_factory: Zero-argument callable returning a fresh ``CodeInterpreter`` for each\n        interpreter session. Defaults to ``dspy.PythonInterpreter`` (sandbox, requires Deno).\n        ``dspy.configure(interpreter_factory=...)`` replaces the default.\n    max_predictor_calls: Maximum number of predictor calls the optimizer-authored code can\n        make per ``forward`. ``None`` removes the limit."},
      {Dspy.ToolCalls,
       "Base class to support creating custom types for DSPy signatures.\n\nThis is the parent class of DSPy custom types, e.g, dspy.Image. Subclasses must implement the `format` method to\nreturn a list of dictionaries (same as the Array of content parts in the OpenAI API user message's content field).\n\nExamples:\n\n    ```python\n    class Image(Type):\n        url: str\n\n        def format(self) -> list[dict[str, Any]]:\n            return [{\"type\": \"image_url\", \"image_url\": {\"url\": self.url}}]\n    ```"},
      {Dspy.ChainOfThought,
       "A module that reasons step by step before producing the output.\n\nExtends a given signature with an additional ``reasoning`` output field that\nthe language model fills in before answering. This chain-of-thought process\nimproves accuracy on tasks that require multi-step logic or explanation.\n\nArgs:\n    signature: The DSPy signature defining the module's inputs and outputs.\n    rationale_field: An optional custom ``FieldInfo`` for the reasoning field.\n        If not provided, a default field with description ``${reasoning}`` is used.\n    rationale_field_type: The type annotation for the reasoning field. Defaults\n        to ``str``. Ignored if ``rationale_field`` is provided.\n    **config: Additional keyword arguments forwarded to the underlying\n        ``dspy.Predict`` module.\n\nExample:\n    >>> import dspy\n    >>> cot = dspy.ChainOfThought(\"question -> answer\")\n    >>> result = cot(question=\"What is the capital of France?\")\n    >>> print(result.reasoning)\n    >>> print(result.answer)"},
      {Dspy.Tool,
       "Tool class.\n\nThis class is used to simplify the creation of tools for tool calling (function calling) in LLMs. Only supports\nfunctions for now."},
      {Dspy.Streaming.StatusMessage,
       "Dataclass that wraps a status message for status streaming."},
      {Dspy.Lm15.FunctionTool, "Serializable function tool specification sent to the model."},
      {Dspy.Adapters.TwoStepAdapter,
       "A two-stage adapter that:\n    1. Uses a simpler, more natural prompt for the main LM\n    2. Uses a smaller LM with chat adapter to extract structured data from the response of main LM\nThis adapter uses a common __call__ logic defined in base Adapter class.\nThis class is particularly useful when interacting with reasoning models as the main LM since reasoning models\nare known to struggle with structured outputs.\n\nExamples:\n```\nimport dspy\nlm = dspy.LM(model=\"openai/o3-mini\", max_tokens=16000, temperature = 1.0)\nadapter = dspy.TwoStepAdapter(dspy.LM(\"openai/gpt-4o-mini\"))\ndspy.configure(lm=lm, adapter=adapter)\nprogram = dspy.ChainOfThought(\"question->answer\")\nresult = program(\"What is the capital of France?\")\nprint(result)\n```"},
      {Dspy.Lm15.BatchJobInfo,
       "A snapshot of a provider-side batch job — the ticket.\n\n``id`` is the provider's job id, a plain string; store it anywhere.\n``created_at`` is normalized to an ISO-8601 UTC string when the\nprovider reports a creation time. Raw job state stays verbatim in\n``provider_data``."},
      {Dspy.Lm15.DocumentPart,
       "A document (PDF, etc.), addressed by exactly one of data/url/file_id/path."},
      {Dspy.Evaluate,
       "DSPy Evaluate class.\n\nThis class is used to evaluate the performance of a DSPy program. Users need to provide a evaluation dataset and\na metric function in order to use this class. This class supports parallel evaluation on the provided dataset."},
      {Dspy.CodeInterpreter,
       "Protocol for code execution environments (interpreters).\n\nImplementations must provide:\n- start(): Initialize the interpreter (optional, can be lazy)\n- execute(): Run code and return results\n- shutdown(): Clean up resources\n\nThe interpreter maintains state across execute() calls within a session,\nallowing variables defined in one call to be used in subsequent calls.\n\nLifecycle:\n    1. Create instance (config only, no resources allocated)\n    2. start() - Initialize interpreter (explicit) or let execute() do it (lazy)\n    3. execute() - Run code (can be called many times)\n    4. shutdown() - Release resources\n\nExample implementations:\n    - PythonInterpreter: Deno/Pyodide WASM interpreter (local sandbox)\n    - LocalInterpreter: Persistent local CPython worker (not a sandbox)\n    - MockInterpreter: Scriptable responses for testing\n\nPooling:\n    For interpreter pooling, call start() to pre-warm instances, then\n    distribute execute() calls across the pool."},
      {Dspy.PredictClass,
       "Basic DSPy module that maps inputs to outputs using a language model.\n\nArgs:\n    signature: The input/output signature describing the task.\n    callbacks: Optional list of callbacks for instrumentation.\n    **config: Default keyword arguments forwarded to the underlying\n        language model. These values can be overridden for a single\n        invocation by passing a ``config`` dictionary when calling the\n        module. For example::\n\n            predict = dspy.Predict(\"q -> a\", rollout_id=1, temperature=1.0)\n            predict(q=\"What is 1 + 52?\", config={\"rollout_id\": 2, \"temperature\": 1.0})"},
      {Dspy.Lm15.OpenAIResponsesCompat,
       "Partial compatibility policy for OpenAI Responses-family APIs.\n\nNone means \"inherit\". Non-None values override parent profiles. The value\n\"auto\" means \"explicitly use adapter auto-detection\"."},
      {Dspy.Lm15.InvalidRequestError, "Bad request shape or invalid provider resource (4xx)."},
      {Dspy.Clients.Engines.LM15Engine,
       "One synchronous native attempt. Close after its active calls finish."},
      {Dspy.File,
       "A file input type for DSPy.\nSee https://platform.openai.com/docs/api-reference/chat/create#chat_create-messages-user_message-content-array_of_content_parts-file_content_part-file for specification.\n\nThe file_data field should be a data URI with the format:\n    data:<mime_type>;base64,<base64_encoded_data>\n\nConstruct from in-memory values only: raw ``bytes``, a data URI string, a\n``{\"file_data\"|\"file_id\"|\"filename\"}`` dict, or another ``File``. Construction and adapter\nparsing never access the filesystem; use :meth:`from_path` to read a local file,\n:meth:`from_bytes` for raw bytes, or :meth:`from_file_id` to reference a preuploaded file.\n\nExamples:\n    ```python\n    import dspy\n\n    class QA(dspy.Signature):\n        file: dspy.File = dspy.InputField()\n        summary = dspy.OutputField()\n    program = dspy.Predict(QA)\n    result = program(file=dspy.File.from_path(\"./research.pdf\"))\n    print(result.summary)\n    ```"},
      {Dspy.Lm15.BatchRequest,
       "A batch of model requests.\n\nEach nested Request carries its own model.  The optional top-level model is\nonly a routing/default convenience; when omitted, it is inferred from the\nfirst nested request and does not constrain the rest of the batch."},
      {Dspy.Lm15.CitationDelta, "A citation fragment arriving during streaming."},
      {Dspy.LMRateLimitError,
       "The provider rate-limited the request.\n\nCheck the `retry_after` attribute for a provider-suggested retry delay when\none is available."},
      {Dspy.LMStreamAssemblyError,
       "An incomplete or invalid stream cannot be accepted as a successful response.\n\n``partial`` carries salvageable content when available. It is not a success\nand must not be cached. Unknown usage remains unknown."},
      {Dspy.Lm15.VideoJobInfo,
       "A snapshot of a provider-side video job — the ticket.\n\n``id`` is the provider's job id, a plain string; store it anywhere\n(on xAI it is the ONLY copy — the wire has no list endpoint).\n``progress`` is a percentage when the provider reports one.  Raw job\nstate stays verbatim in ``provider_data``."},
      {Dspy.InferRules, ""},
      {Dspy.Image,
       "Base class to support creating custom types for DSPy signatures.\n\nThis is the parent class of DSPy custom types, e.g, dspy.Image. Subclasses must implement the `format` method to\nreturn a list of dictionaries (same as the Array of content parts in the OpenAI API user message's content field).\n\nExamples:\n\n    ```python\n    class Image(Type):\n        url: str\n\n        def format(self) -> list[dict[str, Any]]:\n            return [{\"type\": \"image_url\", \"image_url\": {\"url\": self.url}}]\n    ```"},
      {Dspy.Lm15.AudioFormat,
       "AudioFormat(encoding: 'AudioEncoding', sample_rate: 'int', channels: 'int' = 1)"},
      {Dspy.Provider,
       "Base class for language model providers.\n\nA provider is responsible for managing language model instances, including\nlaunching, killing, and fine-tuning models. Subclasses should implement\nprovider-specific logic for these operations.\n\nAttributes:\n    finetunable: Whether this provider supports fine-tuning.\n    reinforceable: Whether this provider supports reinforcement learning.\n    TrainingJob: The class to use for training jobs (subclass of `TrainingJob`).\n    ReinforceJob: The class to use for reinforcement learning jobs (subclass of `ReinforceJob`).\n\nExamples:\n    ```python\n    from dspy.clients.provider import Provider\n\n    class MyProvider(Provider):\n        def __init__(self):\n            super().__init__()\n            self.finetunable = True\n\n        @staticmethod\n        def is_provider_model(model: str) -> bool:\n            return model.startswith(\"myprovider/\")\n\n        @staticmethod\n        def finetune(job, model, train_data, train_data_format, train_kwargs=None):\n            # Implement fine-tuning logic\n            return \"fine_tuned_model_id\"\n    ```"},
      {Dspy.Lm15.ResponseStream,
       "Lazy stream-backed response assembler.\n\n    rs = ResponseStream(lm.stream(request), request)\n    for text in rs:              # text as it arrives\n        print(text, end=\"\")\n    rs.response                  # the same Response complete() returns\n\n``rs.events()`` yields the canonical StreamEvents instead — one\nvocabulary for raw and assembled streaming.  Accessors mirror\nResponse's own minimal set; everything richer is\n``rs.response.message.first(...)`` / ``.parts_of(...)``.\n\nTool calls are surfaced as data only; ResponseStream never executes\nanything."}
    ]
  end

  @doc false
  def __search__(query) do
    SnakeBridge.Docs.search(__MODULE__, query)
  end

  @doc false
  def doc(function) do
    SnakeBridge.Docs.get(__MODULE__, function)
  end
end
